# Retained deployable — `ckpt/pre-d-money-conf01-d5b790e56`

Train tip: `d5b790e56`  
SHA256 of both `order-manager.*.js` copies: see `SHA256SUMS.txt` (must match).

These are the last-known-good money-path module bytes before the next D product landing. Rollback = copy these files back over the live module paths (see `docs/plan3/CKPT01-D-MONEY-PATH-20260730.md`). Do not rebuild from source to "roll back."
