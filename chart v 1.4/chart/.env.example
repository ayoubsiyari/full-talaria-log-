# Finnhub is configured on the chart API server only (not in the browser).
# Set FINNHUB_API_KEY in the environment for the trading-chart service, e.g. root .env + docker-compose:
#   FINNHUB_API_KEY=your_token
# Then restart the trading-chart container.
# https://finnhub.io/register
