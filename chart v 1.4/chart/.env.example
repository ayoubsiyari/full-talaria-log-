# Must live in THIS directory (chart/), next to index.html — not the monorepo root.
# After editing: npm run sync-env   OR   node scripts/sync-chart-env.mjs
# Then hard-refresh the chart (or bump ?v= on chart-env.generated.js in index.html if cached).
# https://finnhub.io/register — key is embedded in modules/chart-env.generated.js (visible in DevTools).

FINNHUB_API_KEY=
