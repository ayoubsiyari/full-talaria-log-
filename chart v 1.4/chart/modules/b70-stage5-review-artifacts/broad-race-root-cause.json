{
  "schemaVersion": 1,
  "failureIdentity": "incremental pass posts an O(tail) transfer-list payload, commits, and clears busy",
  "failingAssertion": "chart._indicatorWorkerBusy must be false after the fake worker tail response",
  "testFile": "chart v 1.4/chart/modules/m19-i-indicator-tail.test.mjs",
  "testDataSeed": 101,
  "originalCommand": "$files = Get-ChildItem -LiteralPath \"chart v 1.4/chart/modules\" -File | Where-Object { $_.Name -like \"*.test.mjs\" } | ForEach-Object { $_.FullName }; node --test $files",
  "originalOrdering": "PowerShell Get-ChildItem filename order; m19-i-indicator-tail.test.mjs ran among the full module test set",
  "originalConcurrency": {
    "explicit": false,
    "nodeDefaultOnObservedHost": 15,
    "logicalCpuCount": 16
  },
  "originalResourceState": {
    "knownConcurrentWork": [
      "full broad suite including real-browser RED cells",
      "authenticated browser harness during the initial part of the broad run"
    ],
    "starvationWitness": "An overlapping isolated recheck still passed, but its deterministic math test took 2278420.8171ms, proving severe scheduler starvation.",
    "memorySnapshotAvailable": false
  },
  "mechanism": {
    "fakeWorkerReplyPhase": "setImmediate/check phase",
    "legacyFlushPhase": "setTimeout(25)/timers phase",
    "race": "If the process is descheduled for more than 25ms after both are queued, Node enters the timers phase with the timer already due. The test resumes before the queued worker immediate and reads busy=true.",
    "candidateSharedStatePossible": false,
    "reason": "Node test files execute in isolated child processes; b70 focused tests cannot share globals, FakeWorker, postSink, chart instances, or module cache with m19-i-indicator-tail.test.mjs."
  },
  "fix": {
    "scope": "test harness only",
    "change": "Replace elapsed-time flush with deterministic immediate/timer phase drains.",
    "assertionsWeakened": false,
    "regression": "fake-worker drain is deterministic after wall-clock preemption",
    "regressionProof": "The test first proves the legacy timer observes immediateRan=false after a modeled 40ms preemption, then proves the new phase drain observes immediateRan=true."
  },
  "deterministicComparison": {
    "command": "B70_BROAD_PAIRS=3 B70_BROAD_CONCURRENCY=1 B70_BROAD_SEED=7005 node \"chart v 1.4/chart/modules/b70-broad-paired-runs.mjs\"",
    "testOrder": "lexicographically sorted filenames; exact list persisted per run in broad-paired-runs.json",
    "concurrency": 1,
    "seed": 7005,
    "timezone": "UTC",
    "uvThreadpoolSize": 4,
    "pairs": 3,
    "verdict": "GREEN",
    "allBaselineCandidateFailureSetsExact": true,
    "allRunsMatchedHistoricalBaselineFailureSet": true
  },
  "hangDiagnosis": {
    "priorIsolatedElapsedMs": 2286498,
    "deadlock": false,
    "cause": "The prior runner had no per-test or per-run timeout and was launched while broad/browser jobs were consuming CPU. The deterministic tail math test eventually completed after 2278420.8171ms, then all 22 assertions passed.",
    "correction": {
      "perTestTimeoutMs": 240000,
      "perRunTimeoutMs": 420000,
      "termination": "Kill the full child process tree; taskkill /T /F on Windows.",
      "diagnostics": "Persist last completed test identity, last 40 output lines, PID, signal, resource snapshots, and timeout type.",
      "failClosedProbe": "broad-timeout-probe.json records both child runs timedOut=true and verdict=FAIL."
    },
    "freshBoundedPair": {
      "artifact": "broad-fresh-bounded-pair.json",
      "verdict": "GREEN",
      "baselineElapsedMs": 238324,
      "candidateElapsedMs": 235835,
      "timedOut": false,
      "failureSetsExact": true
    }
  }
}
