# Manager C evidence (citable slim extracts)

Raw `.scratch-*.json` at repo root embed heap snapshots (100MB+) and are not committed.
These slim extracts are the shippable grading surface.

| file | claim |
|---|---|
| `w72-heap-6cycle-deployed.slim.json` | 6-cycle live canary (b87): late-jump ABSENT; ESD script-source |
| `w71-b85-bisect.json` | four-fix leave-one-out heap effect |
| `w71-b85-ALL_ON/OFF.slim.json` | ALL_ON vs ALL_OFF heap floors |
| `w57d-m6-scheduler-plus9.slim.json` | scheduler deltas: eventListeners=+9, all else 0 |
| `w69/w70-retainer-*.slim.json` | holder paths (`_smartPrefetchCache`, ESD classBytes) |
