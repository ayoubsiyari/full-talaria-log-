# Manager C — W74 evidence (slim)

PO-workload HEAP-CYCLE calibration + replay interval budget.

| File | What |
|---|---|
| `w74-heap-po-workload-6cycle.slim.json` | Deployed MultichartGrid ×6 with indicators+order+live replay; floor-only snapshot policy |
| `w74-interval-budget.slim.json` | REPLAY-INTERVAL-BUDGET-V1 on same surface |

Canary drifted during the wave (b90 → b91 → b95). Grades are pinned to the buildId in each slim file.
