/**
 * Journal-backend subscription REST API (Flask), proxied from the homepage origin as
 * `/journal/api/*` — see `next.config.mjs` rewrites and `journal-backend/app.py` blueprint
 * prefix `/api/subscriptions`.
 */
export const JOURNAL_SUBSCRIPTIONS_API = "/journal/api/subscriptions";
