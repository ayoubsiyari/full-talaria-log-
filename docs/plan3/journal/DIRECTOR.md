# Director journal

**APPEND-ONLY.** Grammar: `docs/plan3/journal/FORMAT.md`

## Entries

- 2026-07-28T00:20+01:00 | RULING | DIR-001 | ROW=A11,A12,A13 | Three managers run continuously parallel. Single PO queue, never three streams. Managers dispatch to subagents and do not implement.
- 2026-07-28T00:21+01:00 | RULING | DIR-002 | ROW=A3 | Session-calendar fix and bucket-immutability fix are canary hard gates. Awaiting PO confirmation of the implied canary delay.
- 2026-07-28T01:50+01:00 | RULING | DIR-003 | ROW=journal-format | Manager C's implemented journal grammar is normative; the Director's earlier draft format is superseded. Parser to be extended with ASSUMPTION, VERDICT (SURFACE and COVERAGE mandatory), FALSIFIED, DISPATCH per FORMAT.md.
- 2026-07-28T01:52+01:00 | RULING | DIR-004 | ROW=territory; SUPERSEDES=C-NOTE-002 | Manager C's commit of the Director-owned territory manifest under a Manager: Director trailer is RATIFIED. C acted correctly: CI cannot read an uncommitted manifest and Manager B's merge gate depended on it. C flagged it rather than concealing it, which is the behaviour required.
- 2026-07-28T01:53+01:00 | RULING | DIR-005 | ROW=TB-3 | Fail-closed default CONFIRMED: unowned paths are RED for every manager. An unowned path is a Director assignment gap, resolved by ruling, never by a manager self-granting.
- 2026-07-28T01:54+01:00 | RULING | DIR-006 | ROW=TB-2 | CONFIRMED per A11.3: Manager B's order-module grant remains inferred and V6 stays ungranted until the call-site audit reports which file owns order-line paint, hit-testing and drag preview. If any of it lives in chart.js, V6 stays with Manager A.

2026-07-28T08:47Z | DIR | RULING | ref=B/B-0067,A/05:56-amendment | decision=B is correct; A's dead-code amendment is WITHDRAWN pending reachability | "All five interpolated substring selectors have live textual producers, read directly by B at 38558/42589/44047/38586/37012. Deleting the removal calls would strand live stepper and delete-badge elements with no cleanup path. NO manager deletes any removal call until the reachability dispatch reports. A and B are not necessarily in conflict: A may have meant reachability, B proved textual presence. Both must state which they mean."
2026-07-28T08:47Z | DIR | RULING | ref=B/B-0067 | decision=selector collision is promoted to a V6-P1 candidate mechanism | standing-rule=SEL-01 | "[class*=\"pending-tp-1\"] matches pending-tp-12 and pending-tp-100, so removing one order's parts can remove a sibling order's parts. That is a direct candidate for the PO's reported 'lines disappear' and 'part of them disappear'. Decisive cheap experiment: two live orders with prefix-related ids (1 and 12, or 7 and 70), remove the lower, observe the higher. Runnable in C's new browser runner. Fix idiom already in-file at 39146: rewrite [class*=\"X\"] to .X."

2026-07-28T09:40Z | DIR | RULING | ref=A/09:38-slice-a | decision=A13.1 is per CHANGE, not per commit; merge is in order | "Content byte-identical to 677cb7db2 which had a full top-tier adversarial review of these exact files. Re-reviewing identical bytes is ceremony, and A13 forbids delegation theatre. A correctly identified that only the subset property was unreviewed and verified it with a decisive mechanical test. Standing rule REV-01: a fresh adversarial review is owed per unreviewed change, not per commit; the manager must name what was unreviewed and how it was verified. A's disclosure is the model."
2026-07-28T09:40Z | DIR | RULING | ref=A/loader-slice-b | decision=fix the tripwire, do not load the megabyte | standing-rule=A4c-2 | "A's leaning is ratified as ruling. tripwirePasses() must not demand a provider tag on a shell that has no indicator consumer; that inverts the contract, which exists to detect a missing provider FOR AN EXISTING CONSUMER. Adding 989KB to chart-host.html to silence a badge during an open A9 memory investigation is a patch for appearance, which is the class the PO named this morning. Presence assertions are conditional on consumer presence."
2026-07-28T09:40Z | DIR | RULING | ref=A/loader-slice-d,C/W24-W26 | decision=C ships the inventory correction as a minimal packet AHEAD of gate hardening | "STOP-THE-LINE's remainder is in C's territory and sits on the critical chain with a 15:15 M1 deadline: legacy-index.html declared servable:false while api_server.py:26923 routes it, three further routed shells absent from the inventory, A14.3 exposure conditional unimplemented. C is currently hardening the gate (W24/W25/W26 foreign-element stack). Gate perfection does not close M1; the inventory correction does. Split it out and ship it first."

2026-07-28T09:52Z | DIR | RULING | ref=A/DIRECTOR-Q-cap-payoff | decision=A's default-in-force is RATIFIED; cap ships, updateChartDataFast stays, duplicate-renderer risk becomes a named open row | "My S3.1 premise is falsified by A's measurement and I withdraw it: the fast path is already unreachable on single-chart at any speed, and remains reachable on multichart 1D at 10x and below, so the cap does not make it dead code. Retiring a REACHABLE renderer would change multichart 1D replay behaviour 46 hours before a canary, on the surface we can least afford to destabilise. Declining a new risk under deadline is the same discipline as declining to relax a gate under deadline. The cap stands on its own merits per A's own reading."
2026-07-28T09:52Z | DIR | FLAG | ref=A/10:05 | "A's finding that the mode selector and the cadence DISAGREE on the finest-TF path is promoted to an investigation candidate for the multichart 1D cost, separate from any retirement. A selector choosing fast-mode by one criterion while ticks are driven by another is a mechanism, not a tidiness issue. Do not retire it; characterise it."
2026-07-28T09:52Z | DIR | RULING | ref=A/10:04-provenance | decision=Director-caused; standing rule TREE-01 | "Third occurrence today of an agent reading the main checkout, finding a symbol absent, and reporting it absent from 'the tree'. Cause is mine: the main working directory sits on manager-c/verification-infra because the Director has been committing there, in violation of A14.4. It has now corrupted a measurement - bespoke counters were authored because _mcDiag counters were reported absent while present on manager-a/critical-path. TREE-01: every claim of presence or absence names the BRANCH it is about and the branch the working tree is on; 'the tree' is not a location. A's standing correction to its own briefs is adopted project-wide."

2026-07-28T09:58Z | DIR | RULING | ref=A/S3.1-cap | decision=cap CONFIRMED by PO as a product decision, technical justification withdrawn | "PO keeps the hard 10x ceiling on product grounds - fewer offered speeds, less surface to test before Thursday, competitor parity - with the Director's architectural justification retracted at f990eb4b5. Requirement 3 (CPU at 100x vs 10x) is still owed and is the only remaining test of the PO's original suspicion, but it is NON-BLOCKING and must not displace chain work. Proceed with clamp, entry-point coverage and hidden-flag closure."
2026-07-28T09:58Z | DIR | RULING | ref=A/09:52-method | standing-rule=BRIEF-02 | "A routed its own load-bearing claim to a subagent as a HYPOTHESIS TO REFUTE rather than as a briefed fact, and the measurement destroyed it. Adopted project-wide: any premise a packet depends on that has not itself been measured is briefed as a hypothesis with a refutation criterion, never as background fact. Three wrong premises today - the Director's data-effect lag theory, the Director's fast-path retirement ground, and A's own 1D work-rate prediction - each would have produced authored work against a false premise if briefed as fact. The cost of framing is zero; the cost of building on a dead premise is a packet."

2026-07-28T10:50Z | DIR | RULING | ref=B/B-0088 | decision=NARROW GRANT to B on trade-identity paths in api_server.py; reachability FIRST | row=M4 | "Two id resolvers with different alias precedence either side of a DELETE is a live trade-deletion mechanism: 12359 builds the keep-set from tradeId|id while journal_trade_client_id resolves tradeId|trade_id|client_trade_id|id, and 12451-12455 deletes every row whose client_trade_id is absent from the narrow set. A row written under the wide vocabulary is deleted for not existing under the narrow one. B is the correct owner - it already owns M24 ledger identity canonicalisation and is the single migration owner per Layer 3, and routing money-path ledger work to A would load the critical-path manager. GRANT IS NARROW: trade-identity resolution and trade-deletion paths only, not api_server.py at large. ESTABLISH REACHABILITY BEFORE ANY EDIT - if unreachable this is a latent hazard, not a live defect, and the fix shape differs. If reachable it is a canary HARD STOP under Part 5."
2026-07-28T10:50Z | DIR | RULING | ref=B/B-0089 | standing-rule=HARNESS-01 | "A harness that DESTROYED two real trades while printing six PASS is the most dangerous artifact this project has produced. Standing rule: NO harness may mutate the ledger it verifies. Verification runs against an isolated ledger or a copy, never the live store. Second clause: a check that filters the ledger to the rows the harness itself just wrote cannot observe collateral damage and is structurally incapable of detecting the defect class it claims to cover - that filter is now a named review-rejection trigger. B's DO-NOT-RUN banner is ratified and the harness stays quarantined until both clauses are satisfied."
2026-07-28T10:50Z | DIR | RULING | ref=B/B-0090 | decision=revert RATIFIED; standing-rule=GUARD-01 | "The M10 guard reproduced the PO's exact symptom - Number(null) is 0, which is finite, so null passed the isFinite check and subtracted against zero to yield 464594 hours. GUARD-01: a validity guard must be proven to REJECT the exact input that produced the reported symptom, as a named cell, before the packet is reviewable. Coercion-based guards (Number(), parseInt(), truthiness) are review-rejection triggers unless a null/undefined/empty-string cell is present. Note the second-order damage B found: null newly reaching the early return silently dropped trading_session_id, savedAt, cost basis and excursion storage - a guard that changes control flow must enumerate what the new path skips."
2026-07-28T10:50Z | DIR | RULING | ref=A/10:41 | decision=A13.1 FAILS CLOSED; A is correct | "If the top-tier adversarial reviewer cannot be dispatched due to API capacity, the packet BLOCKS. It is not reviewed by a cheaper model and it is not merged on manager confidence. Substituting a cheaper reviewer for an unavailable top-tier one is the forbidden reviewer downgrade arriving through the back door of infrastructure failure. Queue the packet, log the capacity failure, proceed on other work. Under deadline this will hurt; it is the same discipline as A16.5 and it holds for the same reason."
