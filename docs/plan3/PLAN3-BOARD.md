# Plan 3 Board — mechanism rows

Status vocabulary: INTAKE → DIAGNOSED → FIX-LANDED → CLOSED-VERIFIED (member tickets close only on reporter/PO re-verify on their own surface at a named checkpoint, P5/L1). Recurrences use `RECURRENCE-A-PENDING` → `A-STALE` / `B-NEW-MECHANISM` / `STICKY`; see `STICKY-REGISTRY.md`.
Source: `tickets/support-export-full-all-7-20-26` — 32 threads / 36 distinct bugs (residuals as of 2026-07-20). Multi-bug threads split with letter suffixes.

## Execution status — 2026-07-20

Current provenance surface: CKPT-011 · `20260720b21` · `https://www.talaria-log.com` (Manager re-probed build id before dispatch).

**GLOBAL HARD STOP — updated 2026-07-21:** Manager re-probe found public shell/embed/SW `20260721b23` while `chart.js` engine remains `20260721b12`. The public surface is not a D-034 checkpoint. M19 is the only active engineering row, but no live verdict or P5 close is valid until an exact-digest restore or named successor SHA/build-id/digest checkpoint passes the tripwire.

**Checkpoint readiness — retained deploy gate, 2026-07-23:** Final A–E checkpoint `20260723b04` is local-source/build GREEN at commit `ba38525baf1e9b35a7552a5215d179c9cbc8ec97` (parent C `c9700ebc8`). Its strict chart/homepage build, in-image layout/I8 and 29-check image tripwire are GREEN. Local digests are chart `sha256:174063019f8b013cd428f699f397ee9f564b6d6cb262deb43f7db65717da0344` and homepage `sha256:b80b0f9793b7fde537c00509cc7c2cd72de92c156fb502f1c8e9aad8b4080baf`; labels bind both to `20260723b04` and `ba38525ba`. Source/tag and images are not remote. GHCR package-write authorization still blocks publication, and production is unchanged. Deployment remains forbidden until remote digests and the exact-digest runtime tripwire are verified.

**PO GLOBAL PRIORITY OVERRIDE — effective 2026-07-22, extended 2026-07-23:** M19 progressive session degradation is the single #1 priority of the entire plan. **2026-07-23 extension: M19 is NOT closed by b57 (A–H). The surviving multiple-indicators-during-replay lag is M19-I (see Director ruling at the bottom of this file) and is the immediate next work item.** **2026-07-24 extension: a Director four-track audit found ~30 additional latent lag sources — see M20 (latent lag inventory + permanent anti-lag gate) at the bottom of this file. Order: M19-I → M20 QUICK-KILL → M20 A→B→D→C. Nothing else unparks before M20 closes.** All other ticket work, including M10/TAL-01800, is parked cleanly. All four lanes work only on M19 until its gated fixes are landed and the canonical soak is GREEN. Fixes ship independently in value order; Fix (a) must not wait for (b)–(e). Every deploy remains bound by D-031/D-034, and D-030/I16 remain binding for persistence work.

**Parked M10 state:** TAL-01815 and TAL-01798 remain PO-verified with permanent gates. TAL-01800 and all remaining M10 work are parked without partial edits and resume immediately after M19. No worker may modify an M10 path during the override.

**Current board pulse — FULL-CAPACITY CYCLE 1 (2026-07-24, Manager dispatch):** PO order binding. M0 reconfirmed sealed (`pre-lag-sprint-20260724` → `a8d5f721d`, build `20260723b57`, provenance present). Product baseline on test remains CKPT-019/`20260724b61` (PO RED on painted endpoint). Wave 1 dispatched simultaneously across five file-disjoint tracks. Serialization held: W4 `chart.js` Q1/Q2 before any W3 M21-1 product edit.

| Worker | In flight | ETA | Next queued | Blocker |
|---|---|---|---|---|
| W5 (meas/gates) | Measurement 1a zero-code protocol + Talaria capture scaffold; seed TradeZella baseline from brief §G1 | same cycle | Visible-frame painted-endpoint RED that fails on b61; M21-0 scorecard skeleton | PO: TradeZella side-by-side reconfirm if §G1 numbers not accepted as 1a target line |
| W4 (quick-kills) | Q1 + Q2 + Q8 RED-first (Q1/Q2 own `chart.js` critical path) | same cycle | Q5/Q6/Q7 (file-disjoint); idle-soak discriminator assist to W5 | none |
| W2 (retained set) | A1 screenshots→IndexedDB RED-first in `order-manager.js` (D-030/I16) | same cycle | Q4 trail_sl_path caps; retained-master release | none |
| W1 (compute) | Q9 consumer audit (no product edit yet) + visible-frame RED partnership with W5 | same cycle | Q9 stage-1 fix after audit; b62 exact-tail only after b61 RED exists | waits on W5 RED existence before b62 product edit |
| W3 (render arch) | M21-1 RED + TAL-01585 harden + **Q2 region-paint ghost oracle** (new dual-tree harness; W4 verification input); `PRELIMINARY-PENDING-FABLE-SIGNOFF`; **no `chart.js` product edits** | same cycle | M21-1 kill-switched fixes after Manager clears Q1/Q2 + Fable sign-off; W4 consumes Q2 oracle gap | serialized on W4 Q1/Q2 land; Fable API limit; Q2 geometry gate absent |
| W6 (render parallel) | Pre-build typed-array visible-window mirror + buffer pool (new modules only); must not edit W3 M21-1 harness/H1–H6 | same cycle | Indicator-layer migration onto W3 scaffold at M21-2 start | none (must not edit W3 worker bootstrap/contract) |

**Production QA data:** probes may create sessions/orders only under dedicated QA account ids supplied by the PO. Never use admin or real-user accounts; all QA records must remain filterable from customer data under I16.

**Recurrence triage — binding 2026-07-22:** nine unique reopen-signal tickets are recorded in `STICKY-REGISTRY.md`; the PO-stated tenth ID is missing from the listed IDs/exports. None is confirmed `STICKY` yet because every reporter-device URL/build/SW identity is absent. Triage step (a) runs before engineering. M19-R/TAL-01585 and M8/date-jump are first suspects; TAL-01617/01717/01723/01719 have strong stale-public-surface priors.

| Lane | First task | State |
|---|---|---|
| L1 — Rendering / checkpoint | M19(d), then M19(e) | FINAL A–E LOCAL-CHECKPOINT-GREEN — commit `ba38525ba`, build `20260723b04`, local D-031/D-034 tripwire GREEN; no publication/deploy |
| L2 — Sync + Replay | M19 acceptance instrument | ACTIVE — independently own baseline/per-fix soak execution, before/after slope and payload numbers, switch-OFF discrimination, evidence persistence, and flake classification; no product edits |
| L3 — Orders | M19(a), then (b), then (c) | FIX-A+B+C LOCAL-CHECKPOINT-GREEN — C commit `c9700ebc8`, strict build `20260723b03`, local tripwire GREEN. Lane 3 stands down; production unchanged; M10 parked |
| L4 — Interaction/UX | M19 state matrix | BLOCKED-AUTH — local b04 provenance GREEN; 12 cells did not run because QA env vars were unset. Rerun immediately after PO exports `TEST_EMAIL`/`TEST_PASSWORD`; no admin fallback or product edits |

TAL-01744a and TAL-01685 remain `NEEDS-PO-CLARIFY`; neither is dispatched. TAL-01736a is also `NEEDS-PO-CLARIFY` for one exact-action detail: does “switching to another layout while the mouse button is held” mean entering/focusing another existing panel, or changing the grid layout (for example 2→4)?

## Lane 1 — Rendering

### M4 — Axis/grid/tick correctness + render latency (P1) — 5 bugs
| Member | Symptom |
|---|---|
| TAL-01739 | Gridlines flicker (vanish/reappear) while dragging |
| TAL-01740 | Slow rendering; time/date scale misaligned |
| TAL-01734 | Custom TF (3m) + zoom out → gridline/time labels far apart (**recurrence of plan-2 A1 custom-interval tick basis — start from that diagnostic**) |
| TAL-01724 | Zoom-in per-candle gridlines survive Ctrl+R reset (stale tick cache not invalidated on reset) |
| TAL-01688 | Alert renders in a broken state (screenshot) |
Notes: likely 2 sub-mechanisms — (a) tick-builder basis/cache invalidation (01734/01724/01740-labels), (b) paint-order/flicker during interaction-lite render (01739). Diagnostic decides the split.

### M5 — Candle paint determinism / stuck-until-interaction (P1) — 3 bugs
| Member | Symptom |
|---|---|
| TAL-01726 | Candle colors change while slowly panning (video attached) |
| TAL-01725 | Same symbol+TF+zoom, different candle colors/shapes across panels |
| TAL-01709 | Symbol switch → partial candles, completes only after moving the chart (**RC-2 stuck-until-interaction family — plan-2 invalidation contract; check provenance first**) |
Notes: 01725/01726 suggest paint reads mutable state mid-pan (LOD path or resample seam) — determinism defect, not cosmetic.

## Lane 2 — Sync + Replay

### M1 — Sync-OFF coupling leak / sync-ON follow gaps (P1) — 4 bugs
| Member | Symptom |
|---|---|
| TAL-01743 | Moving one chart moves other layouts; manual rescale affects peers |
| TAL-01737b | Time/date-scale gesture on chart 2 moves chart 1 |
| TAL-01732b | (Darija, translated) split charts sometimes do NOT move together (sync-ON follow gap) |
| TAL-01744b | Crosshair customization not applied across layouts (settings-sync gap — spec: should apply to all) |
Notes: both directions of the same contract — coupling when OFF, missed follow when ON. Plan-2 T8/BL family is the reference; provenance check first (users may be on pre-fix surfaces).

### M2 — Replay cross-panel cadence + replay-time UI staleness (P1) — 2 bugs
| Member | Symptom |
|---|---|
| TAL-01733 | Visible lag between panels during replay play |
| TAL-01700 | Crosshair time label frozen during replay (should track replay clock) |
Notes: 01733 = plan-2 BL-11/13/finest-TF cadence family — verify build first, then extend H-S19/H-S83 REDs rather than writing new ones.

## Lane 3 — Orders

### M6 — Order-line anchoring, drag-follow, multi-TP, risk model (P0 — top of plan) — 8 bugs
| Member | Symptom |
|---|---|
| TAL-01696 | SL + entry lines shift left/right when chart or price label moves (anchoring) |
| TAL-01653 | SL/TP don't follow entry during drag, only on release (**A6-1 visual-follow clarification recurrence — verify staged fix reached this surface**) |
| TAL-01698 | Phantom TP1 appears during order drag, vanishes on release |
| TAL-01699 | Stacked TP1/TP2 stick together, can't be separated (hit-test needs z-order/cycle disambiguation) |
| TAL-01697 | PnL reads 0 while TP is being dragged, recalculates on release (provisional state not fed to PnL calc) |
| TAL-01683 | Fixed-$ risk: moving SL must hold risk constant and recompute quantity |
| TAL-01692 | Order label value precision mismatch (50 vs 500 — formatting/rounding truncation) |
| TAL-01658 | Add-Entry makes order+SL disappear; delete reverts to undeletable market order (**plan-2 T4 multi-entry residual — retest on current build first**) |
Notes: D-030 money-path ship-gate applies to every fix here. Likely 3 sub-mechanisms: (a) anchoring/coordinate resolve, (b) provisional-drag state model (phantom TP + PnL-0 + follow), (c) risk/quantity + formatting math.

### M7 — Cross-panel order sync asymmetry (P1) — 1 bug
| Member | Symptom |
|---|---|
| TAL-01691 | Order edits sync chart1→chart2 but not chart2→chart1 (panel edits don't route to host — the A6-4 host-canonical completion case) |

## Lane 4 — Interaction/UX + polish

### M3 — Pointer-capture / gesture ownership (P1) — 5 bugs
| Member | Symptom |
|---|---|
| TAL-01736a | Rescale gesture + switch layout with button held → chart uncontrollable |
| TAL-01736b | Same with an order on chart + release outside layout → chart permanently uninteractable |
| TAL-01735 | Dragging the time label makes the chart run away leftward until it disappears |
| TAL-01728 | Actions don't respond on first attempt (first-click family — verify against plan-2 T1 fixes/provenance first) |
| TAL-01714 | Text tool: selection stays on toolbar after draw instead of returning to crosshair (lifecycle store arm/disarm gap) |
Notes: 01736a/b + 01735 = one pointer-capture/release contract across tiles (plan-2 T3 Row 11 kin); the stuck-forever variant (01736b, order present) crosses into L3's hit-test — diagnose jointly, fix in one lane.

### M8 — Navigation + persistence (P2) — 3 bugs
| Member | Symptom |
|---|---|
| TAL-01747 | Symbol selection not persisted across refresh |
| TAL-01677 | Go To session (London→NY) shows error message consistently |
| TAL-01732a | (Darija, translated) following price on 1m at Jan 23, viewport lands at Mar 4 (goto/date-jump defect) |

### M9 — Polish batch (P3, fill work) — 4 bugs
| Member | Symptom |
|---|---|
| TAL-01744a | Crosshair snaps to candles — reporter says incorrect. **NEEDS-SPEC (PO):** is magnet-snap intended default? If yes → add free-crosshair option; if no → default free |
| TAL-01731 | Indicator-list arrow should appear only when indicators exist |
| TAL-01656 | Anchor tool shows too many control points (plan-2 A7b chrome residual) |
| TAL-01685 | Desktop app opens a new window per launch instead of reusing (app shell, not chart engine — PO may route outside plan) |

## Standing first action (all lanes)

Before any diagnostic: **provenance retest per D-034** — confirm the symptom on the current tripwire-verified build with build id recorded. At least six rows are recurrences of plan-2-fixed families (01653, 01658, 01709, 01728, 01733, 01734) — if the reporter was on a stale surface, the row closes by retest with zero engineering.

## Open PO questions

1. TAL-01744a — crosshair magnet-snap: intended default or defect?
2. TAL-01685 — desktop app window reuse: route into plan 3 (L4) or handle outside (it's the app shell/wrapper, not chart code)?

---

# Intake 2026-07-21 (export `support-export-full-all-21-07-26`, ~66 new tickets TAL-01748–01816 + 9 evidence-backed reopen signals; PO's tenth ID pending)

## NEW MECHANISM — M10: Execution & PnL correctness (P0 — OUTRANKS M6, Lane 3) — 11 bugs

The most serious family of the intake: **trades executing, closing, or marking at wrong prices/times without user action.** D-030 money ship-gate binds everything here.

| Member | Symptom |
|---|---|
| TAL-01815 (Arabic, translated) | Activating a market/pending order instantly exits at break-even as if a target was hit — SL and TP are far from price. **Worst bug on the board.** |
| TAL-01798 | Order gets CLOSED by changing TF in another layout |
| TAL-01800 | Moving chart / Ctrl+R closes orders and replaces them with new ones |
| TAL-01787 | TP1 computed at wrong level; TP2 never triggered |
| TAL-01788 | Multi-TP targets not executed at correct price levels |
| TAL-01786 | Duration calculation wrong; Net PnL not displayed with multi-TP |
| TAL-01809 | Balance goes negative when SL risk > balance (must clamp at 0) |
| TAL-01756, TAL-01810 | With spread enabled, SL/TP exit arrows render on the wrong candle (entry candle or spread-offset candle) |
| TAL-01807 | SL/TP arrows glitch after spread-SL + new order + replay; also wrong render after switching pair |
| TAL-01796 | Stray X/close marker on chart after TP hit |
Diagnostic guidance: 01798/01800 smell like one root — order lifecycle re-evaluated on chart-state changes that must not touch it (same class as plan-2's D-030). 01815 may share it (activation re-evaluates against wrong price). Diagnose 01815/01798/01800 as one candidate mechanism first.

Landed evidence (2026-07-22): current candle playback returned guard tick `-1`, so a newly placed/activated order could consume the already-rendered full-candle high/low and close at the current mark. `order-manager.js` now assigns each order a non-persisted canonical replay-event watermark, retains the fine execution feed across display-TF switches, advances every owned market event, rebases on TF/seek guard refresh, and uses a strict current-candle barrier. Host-owned replay-frame P&L projection is coalesced and bounded to live rows; canonical event timestamps/marks project across TFs; marker geometry updates once per live pan frame without a release snap. Permanent tests cover market/pending activation, 1m→1D source ownership, TF resample, pan/reset duplicates, bounded runtime P&L payloads, zero full-panel hot-path rebuilds, canonical marker projection/pan glue, restore baseline, replay-clock duration, non-blocking activation UI, legitimate future TP close, and RED-again switches. PO real-UI PASS: TAL-01815 exact 1m limit → 1D → Play; TAL-01798 A=1m/B=5m then 15m with synchronized P&L/current price/entry/SL/TP and continuous marker drag. TAL-01800 remains open.

## NEW MECHANISM — M11: Margin model (P1, Lane 3) — 3 bugs
TAL-01772, TAL-01776 (both Arabic — translated: NQ market + limit orders rejected "insufficient free margin" with $10,000 balance), TAL-01778 ($1,000 SL on $10k balance → insufficient margin). Futures contract margin computation is wrong or uses the wrong multiplier.

## NEW MECHANISM — M12: Order–symbol binding leak (P1, Lane 3) — 3 bugs
TAL-01777 (place-order panel stays open across pair switch; SL stays on first pair), TAL-01807b (SL/entry from another pair render after switching), TAL-01799 (order auto-appears on newly added layout). Orders must be bound to their symbol+session, and the order panel must close/rebind on pair switch. D-030 kin.

## NEW MECHANISM — M13: Futures tick-size quantization (P1, Lane 3) — 3 items
TAL-01774 (futures order inputs must snap to .25/.50/.75/.00), TAL-01808 (position tool moves in 0.25 steps but release shows a different value — quantize-on-release mismatch), TAL-01773 (spec request: ticks for futures, pts for forex — needs PO confirm as spec).

## NEW MECHANISM — M14: Tool-settings apply/persist (P1, Lane 4) — 5 bugs
TAL-01762 (Fib colors/settings revert), TAL-01813 (Fib level edits reverted on OK), TAL-01794 (Gann Box levels revert on save), TAL-01812 + TAL-01763 (Gann Box rejects levels >1 or <0 — validation too strict; TradingView draws them). Same apply-default/settings-write family as plan-2 TAL-01589/01594 — one settings-commit mechanism suspected.

## NEW MECHANISM — M15: Session/preference persistence (P1, Lane 4) — 5 bugs
TAL-01782 + TAL-01793 (backtest playhead resets to session start on exit/re-enter — **recurrence of plan-2 refresh-persistence fix**, provenance-check first), TAL-01792 (TF + favorite tools reset every enter/exit), TAL-01759 (layouts leak from previous session into new session — isolation, I16-adjacent), TAL-01687 (TF not retained on refresh). Fold existing M8 member TAL-01747 (symbol resets on refresh) into this row.

## NEW MECHANISM — M16: Sessions page CRUD staleness (P2, Lane 4) — 2 bugs
TAL-01767 + TAL-01679 (deleted session stays visible until refresh; second delete → "session does not exist"; can still open deleted session). Also TAL-01749 (link-screenshot-to-trade throws an error — trades page, same lane, own small row).

## NEW MECHANISM — M17: Cross-TF data consistency (P1, Lane 1) — 2 bugs
TAL-01802 (price differs between 1m and 5m of the same instant), TAL-01771 (1m and 5m render identical). Resample pipeline correctness — data-integrity class, not cosmetic.

## NEW MECHANISM — M18: Indicator compute staleness (P2, Lane 1) — 1 bug
TAL-01766 (Arabic, translated: FVG indicator not drawn on ES; drawn after page re-enter; NQ fine) — per-symbol indicator compute/invalidation gap.

## RECURRENCE ROW — M19-R: Drawing geometry ownership during TF transition (Lane 4)
TAL-01585: while the TF-change spinner is visible, dragging the chart moves the drawing layer with it. Plan 2 T5/RC-3 was PO-verified before the later `not sloved` follow-up. State is `RECURRENCE-A-PENDING`, not yet `STICKY`: capture the reporter's device URL/build/SW first. If current uniform provenance reproduces the exact spinner-visible drag cell, reopen the T5/RC-3 diagnostic and treat its former root cause as unproven.

## Member additions to existing rows
- **M6 (orders interaction):** TAL-01750 (hover sticks 2nd entry onto order), TAL-01805 (new order's SL/TP stick to previous order's), TAL-01795 (duplicate SL/TP levels), TAL-01751 (BE level shifts on place-order click), TAL-01760 (order+SL shake on chart move — 01696 dup), TAL-01780 (SL/TP labels keep original values after modify), TAL-01781 (order type not reclassified Market↔Stop/Limit on entry move — plan-2 T4 recurrence), TAL-01789 (entry level disappears; SL line broken), TAL-01791 (Arabic: sell-order box resizes but won't drag).
- **M8 (nav/viewport jumps):** TAL-01801 (Arabic: Go-To lands 14:00 instead of 8:00 NY), TAL-01806 (chart jumped to different date on zoom-out — reporter self-diagnosed), TAL-01761 (TF switch with order → viewport regresses half a day), TAL-01769 (TF switch after manual move → inconsistent position), plus reopens TAL-01707/TAL-01690 ("still jumping to far dates"). Also existing members TAL-01675/01677/01673 (session Go-To) — M8 is now the second-highest volume row.
- **M4 (axis/grid):** TAL-01755 + TAL-01745 (custom-TF zoom-out gridlines days apart / per-candle — 01734 duplicates), TAL-01770 (TF change misaligns gridlines), TAL-01741 (custom TF breaks time/date scale).
- **M3 (gestures):** TAL-01768 (price-scale rescale dead on first attempt — first-click kin), TAL-01779 (price-label drag during replay: double-click reset broken).
- **M2 (replay):** TAL-01738 (speed 50 vs 100 indistinguishable — cadence scaling), TAL-01765 (rollback depth too shallow), reopens TAL-01718 (tick-by-tick >30x falls back to candle) + TAL-01717 (2nd layout shakes in tick replay).
- **M1 (sync):** TAL-01790 (Arabic: helper chart sits in empty space far from price when main chart moves).

## Reopen-signal triage
Canonical evidence and state: `docs/plan3/STICKY-REGISTRY.md`.

- `RECURRENCE-A-PENDING`: TAL-01617→M6, TAL-01717/01718→M2, TAL-01723→M4, TAL-01585→M19, TAL-01719/01707/01690→M8.
- `DURABILITY-REVERIFY`, not present-evidence sticky: TAL-01584→M3 candidate; its `not sloved` message predates the later b07 PASS.
- `NEEDS-PO-CLARIFY`: TAL-01707 and TAL-01690 still lack the exact action that caused the far-date jump.
- Confirmed `STICKY`: none until a candidate reproduces on a reporter-device surface whose shell, engine, embed/SW, host, and panel IDs all match the named checkpoint.

## PO-MANUAL / NO WORKER DISPATCH
PO will handle these four manually; they consume no Plan 3 lane time and require no Manager clarification relay.

| Ticket | Parked context |
|---|---|
| TAL-01758 | (Arabic) "the arrow moves the whole screen when I want to return to the last candle" — which arrow (keyboard? go-to-realtime button?), and what was expected? |
| TAL-01804 | "place order percentage (EQ/BL) — check them plz" — check what, exactly? What's wrong with the EQ/BL numbers? |
| TAL-01816 | "scaling — drag up/down error" (from admin@talaria.io) — which scale, which chart, what error? |
| TAL-01797 | User's AI-assisted console-log findings about replay lag were referenced but the log content isn't in the ticket — ask reporter to paste the actual console excerpts. (M1 MacBook Air data point noted for the perf file.) |

## Polish / backlog / closed (out of the priority filter)
- Polish: TAL-01783 (entry line color), TAL-01803 (default 1% not 100%), TAL-01811 (entry price flashes red), TAL-01722 (order-line styling).
- Feature backlog (PO decisions): TAL-01784 (time-only presets), TAL-01785 (same indicator twice), TAL-01814 (SMC indicator request), TAL-01680 (YTD option), TAL-01773 (ticks/pts spec — see M13).
- Closed by reporter: TAL-01757, TAL-01764, TAL-01775, TAL-01704 (self-resolved), TAL-01748 (test ticket).

## NEW MECHANISM — M19: Progressive session degradation (P0 — PO-observed pattern, 2026-07-22; Director-researched, mechanism evidence already gathered) — Lane 3 lead + Lane 1 assist

**Symptom (PO):** after a few hours in one session (replay + orders + panning), ALL chart functions become laggy. Fresh sessions fast. Classic accumulation profile — confirmed by code research (Director exploration report, ranked evidence):

**Understanding sentence:** the user keeps replaying, panning, and trading in one session for hours, sees replay, chart interaction, and orders become progressively slower while a fresh session is fast, and expects per-tick work and persisted state size to remain bounded regardless of session age.

**Execution state (2026-07-23):** `GLOBAL-P0 / A–E FINAL-LOCAL-CHECKPOINT-GREEN / BROWSER BLOCKED-AUTH / REMOTE GATES PENDING`. Final source `ba38525ba` and local b04 runtime are uniform. Overall M19 remains open from the credential-blocked real-browser matrix, remote publication, exact-digest deployment/tripwire and PO verification. Production is unchanged.

| # | Sub-mechanism | Evidence anchor |
|---|---|---|
| a | `updatePositions()` → unconditional `updatePositionsPanel()` per replay tick: full trade-DOM rebuild + analytics + deep-clone persist, O(journal × ticks) | `order-manager.js:30083`, `42338-42958`; called from `replay-system.js:3798/5423/5854` |
| b | Unbounded per-bar excursion arrays (`bar_close_r`/`bar_high_r`/`bar_low_r`, post-exit variants) — no length cap; serialized on every persist | `order-manager.js:3719-3737`, appends at `29316/29379/28628/28654` |
| c | Session autosave stringifies full journal (incl. base64 screenshots + excursion arrays) on main thread every 4–8s | `persistJournal 4838-4861`, `flushSessionStateSave 12308-12344` |
| d | Journal-marker redraw walks FULL journal per replay frame; pan repositions all markers | `36708-36793`, `chart.js:27122` |
| e | Hot-path `console.log` per tick incl. full panel dumps | `29396`, `42352+` and siblings |

**Fix order (Director-recommended):** (1) dirty-flag/throttle the panel rebuild — per-tick path updates PnL text nodes only; (2) cap/summarize excursion arrays (keep MFE/MAE scalars + bounded tail) — **I16: additive schema change, no destruction of existing stored arrays**; (3) strip screenshots + heavy arrays from runtime persist patches; never embed base64 into per-tick `innerHTML`; (4) marker redraw only for newly-visible trades; (5) debug-flag the hot-path logs.
**RED (required before fixes):** a soak scenario — scripted replay of N-thousand ticks with ~50 closed trades + 2 open positions, assert per-tick frame cost stays flat (bounded slope) and persist payload stays bounded. RED today (cost grows), GREEN per fix, switch-OFF discriminator per fix.
**Gates:** D-030 binds (b) and (c) — persist-path changes touch order state; every fix its own switch. Member tickets: TAL-01797 (M1 MacBook replay lag — reporter's console logs pending), TAL-01633/01578 (lag/freeze reports likely late-session), future recurrences fold here.

## PO OVERRIDE (2026-07-22 21:45) — M19 IS THE SINGLE #1 PRIORITY, EFFECTIVE IMMEDIATELY

The PO has explicitly ordered M19 to the top of the entire plan, above M10 and all ticket work. All lanes pivot to M19 NOW: Lane 3 stops current ticket work and takes sub-mechanisms (a)/(b)/(c); Lane 1 takes (d)/(e); the soak RED (already built: `chart v 1.4/chart/modules/m19-progressive-session-soak.test.mjs`) is the acceptance instrument. Other rows resume only after M19 fixes are landed and the soak is GREEN. M10 is the immediate next after M19.

## Global priority override (binding)
1. **M19 is the sole #1 priority across all lanes.** Recurrence triage, M10, M17 and every other row are parked until M19's gated fixes are landed and the canonical soak is GREEN.
2. Lane 3 fix order is binding: (a) per-tick panel rebuild → (b) excursion-array bound → (c) persist-payload trim. Each is one independently switchable, measurable fix.
3. Lane 1 runs (d) marker redraw delta scope and (e) hot-path log guard in parallel with Lane 3. It must not concurrently edit Lane 3-owned `order-manager.js`; overlapping hunks queue for controlled integration.
4. Fix (a) ships at the first D-031/D-034-compliant opening and must not wait for later fixes. D-030 binds (b)/(c): switch-OFF proves today's persisted-record bytes. I16 requires additive schema and clean restore of old sessions.
5. Every cycle starts with a board pulse: mechanism in flight, exact soak before/after metrics, and blockers. M10 resumes immediately after M19.

---

# Director ruling 2026-07-23 (late) — M19-I: indicator replay compute budget (NEW, binding next M19 sub-mechanism)

## Decisions on the manager's 23-July progress report
1. **b57 commit + push: AUTHORIZED.** Follow the D-034 provenance chain exactly (commit SHA → strict build → image digests → tripwire).
2. **Test-environment deployment: AUTHORIZED.** Direct-origin AND public-origin build IDs must both read b57 (Cloudflare stale-surface check is mandatory, not optional).
3. **One-panel and four-panel loaded replay validation: AUTHORIZED** — run it on the deployed test build, not locally.
4. **Production deployment stays gated** on PO validation + public/direct parity, per the manager's own recommendation. Endorsed.
5. **M19 is NOT closed by b57.** The PO's surviving complaint — lag with multiple indicators while replay runs — lives in a code path M19 A–H never touched. That path is M19-I below, and it is the immediate next work item, above everything else.

## M19-I — Progressive/steady replay lag with multiple indicators (P0, Lane 1 lead + Lane 2 acceptance)

**Understanding sentence:** the user adds several indicators and presses play; every bar advance triggers indicator recompute work whose cost scales with the FULL loaded dataset and with main-thread synchronous passes, so with 8 indicators on 90 days of 1m data the session stutters even though M19 A–H are green.

**Why M19-H didn't cover it:** M19-H fixed overlapping *timeframe-switch* work (atomicity, stale-worker rejection, SVG reuse). The per-bar-advance recompute pipeline during steady play is a different path (`scheduleReplayIndicatorRecalc` → `recalculateIndicatorsIncremental`), and its verification scenario (20 TF switches) does not measure per-tick cost during continuous play.

**Code-verified sub-mechanisms** (Director inspection, `chart v 1.4/chart/modules/chart-indicators-full.js` + `indicator-performance.js` + `replay-system.js`):

| # | Sub-mechanism | Evidence anchor |
|---|---|---|
| I-a | **Full-array pack + structured clone per bar advance.** `recalculateIndicatorsIncremental` calls `packBarsCompact(chart.data)` — allocates a fresh Float64Array over ALL bars (90d of 1m ≈ 4.5 MB) and posts it with an EMPTY transfer list, so the buffer is cloned to the worker on every pass. Per-bar-advance at replay speed = constant multi-MB alloc + clone + GC churn. This is the prime suspect for the observed CPU/memory pressure. | `chart-indicators-full.js:8806-8823`, `indicator-performance.js:66-80` |
| I-b | **Sync-only indicator types force a FULL main-thread recompute per pass.** If ANY active indicator is in the sync-only list (sessions, killzones, all ICT*, openingrange, talariafvg/ratiogap/weeklymap/smc), `recalculateIndicators()` runs synchronously over the entire data array on every incremental pass — O(all bars) on the main thread per bar advance. One FVG or sessions indicator poisons the whole set. | `chart-indicators-full.js:8758-8775` |
| I-c | **Broad worker-skip list falls back to full sync recalc.** dema/tema/hma/supertrend/adr/volume/custom are excluded from the worker; if the active set is all-skip (or the worker is unavailable), the full synchronous `recalculateIndicators()` runs per pass. | `chart-indicators-full.js:8779-8804`, `:8749-8752` |
| I-d | **`force:true, immediate:true` bypasses the fingerprint dedupe.** Replay step/pause/restore/multichart paths call `scheduleIndicatorRecalc(..., {force:true, immediate:true})`, skipping the snapshot short-circuit — recalc runs even when bars/params did not change (e.g. repeated pause events, multichart re-sync). | `chart-indicators-full.js:8389-8396`; call sites `replay-system.js:3663, 3689, 3237, 8482` |
| I-e | **Multichart panels: full per-panel recalc every 18 ticks.** `pc.recalculateIndicators()` fires per panel on the live-tick loop — N panels × full recompute, main thread. | `replay-system.js:5957, 6076-6081` |
| I-f | **Full-length tail merges + repaint fan-out.** Worker results are merged back over full-length arrays and every pass bumps the indicator render version → indicator-layer repaint of all N indicator series per bar advance. Cost is bounded by visible bars but multiplies with indicator count; measure before optimizing. | `chart-indicators-full.js:8829-8850`, `:8694-8715` |

**Fix order (binding, one gated switchable fix each):**
1. **I-a — retained packed buffer + windowed tail send.** Keep a persistent Float64Array in the chart, append only new bars, and send the worker ONLY the tail window (`fromIndex − lookback` onward) instead of the whole history; transfer or reuse buffers instead of cloning. Kill-switch `__TALARIA_DISABLE_M19I_TAIL_SEND_V1`.
2. **I-b — incremental sync-only recompute.** Sessions/ICT/FVG types get a bounded-tail recompute during play (they are windowed/period structures; full-history rescan is only needed on TF change or data replacement, which M19-H already made atomic). Kill-switch `__TALARIA_DISABLE_M19I_SYNCONLY_TAIL_V1`.
3. **I-c — shrink the worker-skip list.** Port dema/tema/hma/supertrend/adr to the worker (they are pure series math; no DOM). Volume/custom may stay sync but must go through the same bounded-tail path as I-b. Kill-switch `__TALARIA_DISABLE_M19I_WORKER_PORT_V1`.
4. **I-d — replace `force` with reason-scoped invalidation.** Pause/step/restore paths pass a reason; the scheduler dedupes by bar fingerprint unless the reason genuinely invalidates (TF change, param change, data replace). Kill-switch `__TALARIA_DISABLE_M19I_FORCE_DEDUPE_V1`.
5. **I-e/I-f — panel recalc budget + repaint delta** (only if the soak still shows slope after 1–4): panels share the host's computed tails where symbol/TF match; repaint only indicator panes whose data changed. Own switches.

**RED (required before any fix):** extend the canonical soak (`m19-progressive-session-soak.test.mjs`) with an 8-indicator cell — mix must include at least one sync-only type (FVG or sessions), one worker-skip type (supertrend or hma), and worker-eligible types (SMA/EMA/RSI) — replay at 100× on 90d of 1m data, NO timeframe switches. Assert per-bar-advance main-thread cost stays flat and bounded, worker `postMessage` payload bytes per pass are O(tail) not O(history), and heap growth stays within the M19-G bound. RED today, GREEN per fix, switch-OFF discriminator per fix.

**Gates:** D-031/D-034 bind every deploy. Lane 1 owns the fixes (indicator files are Lane 1 territory; no `order-manager.js` edits). Lane 2 owns the soak instrument and before/after numbers, no product edits. The M19-H one-panel/four-panel manual validation continues in parallel on the b57 test deploy — it does not block M19-I work starting now.

**Closure rule:** M19 closes only when BOTH the A–H soaks AND the M19-I 8-indicator soak are GREEN on a deployed, tripwire-verified build, and the PO confirms the loaded-replay feel on the test environment. Lag is a hard red flag — no other plan-3 row unparks before that.

---

# Director ruling 2026-07-24 — M20: latent lag inventory + permanent anti-lag gate (P0, binding)

**PO directive:** find every lag source that has not surfaced yet, kill it, and make sure it never comes back. Lag is a project-killer.

The Director ran a four-track code audit of the entire engine (`chart v 1.4/chart/`): (1) timers/rAF/listener accumulation, (2) unbounded memory growth, (3) render/DOM hot-path costs, (4) cross-window messaging + storage/network. ~30 sources were verified by code reading with file:line anchors. They are grouped below as M20-A..D. **Every anchor below was verified against the live source; workers re-verify before editing (lines shift).**

**Sequencing (binding):** M19-I remains the immediate active item (it is the PO's *felt* lag today). M20 starts in parallel where lanes are free and takes over as the sole engineering focus the moment M19-I's fixes are landed. Within M20, the QUICK-KILL list ships first — highest value per line of code — then families in the order A → B → D → C. Nothing else on the board unparks before M20 closure.

## QUICK-KILL list (small, gated, high-value — ship first, one switch each)

| # | Fix | Anchor | Why first |
|---|---|---|---|
| Q1 | Kill/gate the permanent 600ms full-DOM span poll (`_v9TimeSyncTimer` — `querySelectorAll('span')` + full walk, never cleared). Replace with event/MutationObserver on the settings panel only. | `chart.js:1554`, `:31307-31334` | ~6k full-DOM scans/hour even when idle |
| Q2 | Stop idle ~1Hz full renders from the bar-close countdown inside the forever `animate()` rAF loop; render only the countdown region or suspend when idle/hidden. | `chart.js:1398`, `:28268-28300` | ~3.6k full paints per idle hour on host |
| Q3 | Strip base64 screenshots from `open_positions`/`pending_orders`/`closed_positions` in the local-backup serializer (journal is already slimmed; these are not) and from full multichart order snapshots. **D-030/I16 bind: strip is serialize-time only, never destructive to stored session data.** | `chart.js:11125-11225`, `order-host-store.mjs:123-154` | Multi-MB synchronous `localStorage` writes every 20s playing / 5s idle |
| Q4 | Cap `trail_sl_path` per-bar array + `sl_modifications` log exactly like M19-B capped excursion arrays (bounded tail + scalars). I16 additive schema. | `order-manager.js:29931-29932`, `:30244-30245`, `:4066-4077` | Same uncapped-per-bar class M19-B fixed; sibling was missed |
| Q5 | Gate outbound crosshair + live-drawing `postMessage` on effective sync mode BEFORE sending (today panels serialize+post even with all sync OFF; the manager drops it after the cost is paid). | `sync-bridge.js:1526-1550`, `:1386-1411` | Continuous wasted cross-window traffic × panels |
| Q6 | Remove document `mousemove`/`mouseup` listeners when the floating replay-toolbar clone closes / replay exits (currently leak one permanent document listener per float cycle). | `replay-system.js:1406-1445`, `:1390-1396`, `:3214-3217` | Pointer-move cost grows for the rest of the session |
| Q7 | Store and clear the same-pair `replayEnter` mirror interval on the panel chart (rapid re-enter can stack unbounded 180ms intervals). | `panel-cmd-bridge.js:2769-2779` | Interval stacking under multichart replay churn |
| Q8 | Alert checker: don't run the 500ms interval with zero alerts; clear-before-restart; wire `destroy()`. | `alert-system.js:894-898`, `:318` | Permanent 2Hz wakeups for every session |
| Q9 | *(added 2026-07-24, advisor-brief finding)* Replay per-tick prefix copy: every bar advance allocates `fullRawData.slice(0, currentIndex+1)` — a fresh copy of the ENTIRE session history per tick, in both normal and fast paths. Replace with a bounded view/index-window (render reads `[0, sliceEnd)` without materializing a copy) or reuse a single growing array. Kill-switch `__TALARIA_DISABLE_M20_PREFIX_SLICE_V1`. High regression risk (many consumers assume an owned array) — Fable Lane 1 owns it, consumer audit required before edit. | `replay-system.js:3804, :5526, :6129, :8706` | O(session bars) allocation per tick — top GC-pressure source on mature sessions |

## M20-A — Idle drains: timers, rAF, listeners (Lane 4)
Beyond Q1/Q2/Q6/Q7/Q8: no `Chart.destroy()` exists (animate loop, viewport listeners, `_v9TimeSyncTimer` are permanent); `DrawingToolsManager` installs document pointer/mousedown handlers with no teardown (`drawing-tools-manager.js:804, :6744, :6789-6790`); `timezoneManager.addListener` is push-only and called per chart init / Go-To setup / replay setup (`timezone-manager.js:309-311`; `chart.js:1538, :20666`; `replay-system.js:316`); favorites toolbar holds a permanent document mousemove (`favorites-manager.js:812-830`). **Deliverable:** a real destroy/teardown contract — every subsystem registers its timers/listeners/observers in a per-chart registry that teardown drains; a census probe (below) proves counts stay flat.

## M20-B — Unbounded memory growth (Lane 3 for order files, Lane 1 for engine caches)
Beyond Q3/Q4: in-memory `tradeJournal`/`closedPositions` keep base64 screenshots forever (`order-manager.js:5801, :9978-9982, :10150-10153` — externalize or cap); `tickPathCache` keyed by every candle timestamp, no LRU (`replay-system.js:67-68, :5609-5643`); `_orderExecutionSeriesByFileId` retains whole superseded master arrays by reference per TF with no per-file TF cap (`order-manager.js:1507-1529`); shared bar store allows every TF per file up to 200k bars each (`chart.js:3172-3225`); `_miSeriesByFileId` ~20k-bar entries with no global cap (`order-manager.js:575, :2344-2365`); trade-marker SVG glow `<filter>` defs never removed on prune (`order-manager.js:37970-37982, :41488-41532`); propfirm `allTrades`/`dailyTrades` unbounded (`propfirm-tracker.js:179, :192`); local backup deep-clones the ever-growing `closedPositions` per write (`chart.js:11139-11164`). **D-030 binds every order-file change; I16 additive schema; switch-OFF must reproduce today's bytes.**

## M20-C — Render/DOM hot-path costs (Lane 1)
Ranked: (1) **full SVG `redrawAll()` on every pan/replay/zoom frame** — clears and rebuilds every drawing's DOM per frame; the CSS pan-transform fast path `_applyPanDrawingsLayerTransform` exists but is **never called** (`chart.js:36513-36545, :27854, :28557-28561`; `drawing-tools-manager.js:12691-12773`) — wire the transform during pan, `redrawAll` only on pan-end/scale-change; (2) economic-calendar markers do a **linear scan of ALL bars per event per paint** (`chart.js:33556-33642, :33436-33465`) — binary search + skip on fast-pan; (3) crosshair mousemove: `getBoundingClientRect` + `querySelector` + possible canvas `resize()` + unconditional style writes per pointer frame (`chart.js:28088-28098, :37263-37792, :32585-32645`) — cache refs/rects, dirty-check writes; (4) indicator legend `innerHTML` rebuild per crosshair move (`indicator-ui.js:2867-2928`; `chart-indicators-full.js:11858-11894, :16489-16516`) — mutate text nodes; (5) `checkDrawingProximity` per hover move samples SVG geometry up to ~220 `getPointAtLength` calls (`drawing-tools-manager.js:2626-2638, :16697-16816`) — throttle + spatial index; (6) indicator paint layer cache is skipped entirely during interaction/replay fast render (`chart-indicators-full.js:8662-8668`); (7) `measureText` margin sync every `drawAxes` (`chart.js:28816-28819, :30282-30346`); (8) Heikin-Ashi full-series recompute per replay step when active (`chart.js:31764-31766`; `replay-system.js:3794-3796`).

## M20-D — Cross-window, storage, network (Lane 2)
Beyond Q3/Q5: independent-pair mirror catch-up can attempt a `/bars` fetch per coalesced frame until a 3-strike breaker, and play can clear the cooldown → refetch storms (`panel-cmd-bridge.js:1421-1533`); P&L fan-out applies ~20/s × peers and each apply runs `JSON.stringify` over all open+pending rows for the visual shape (`order-host-store.mjs:411-434`; `panel-cmd-bridge.js:176-272`) — cache/hash the shape; `syncReplayFromHost {force:true}` every ~2.5s per panel can re-mirror/seek even when already aligned (`panel-cmd-bridge.js:3669-3706`); same-pair cover sync can `master.slice()` the entire host array (`chart.js:6665-6722`) — bound or share by ref; drawing `saveDrawings` sync-writes full drawings JSON to localStorage per geometry commit (`drawing-tools-manager.js:13462-13478`).

## Permanent anti-lag gate (the "never appears again" mechanism — binding, Lane 2 owns)
1. **Idle soak (new RED):** chart open, replay OFF, no input, 10 minutes — assert near-zero full renders, zero DOM polls, zero network chatter beyond allowed keepalives, flat heap. Today this is RED (Q1/Q2/Q8 fire constantly).
2. **Loaded soak (extend canonical soak):** add cells for 50 closed trades WITH screenshots + 100 drawings + trailing-stop active + 4-panel multichart play. Assert flat per-tick cost, bounded heap slope, bounded localStorage write bytes/min, bounded postMessage msgs+bytes/min.
3. **Census probe:** instrument counts of live intervals/timeouts/rAF-loops/document+window listeners/observers; soak asserts the census is FLAT across replay enter/exit ×10, TF switch ×20, panel open/close ×10, toolbar float/close ×10. Kills the whole leak class, not just today's instances.
4. **Byte budgets as regression rows:** localStorage write ≤ a named budget per write and per minute; cross-window message payloads ≤ budget; any budget bump requires a Director-ratified cost-cell note (D-039 precedent).
5. **CI ratchet:** the idle soak, loaded soak, and census run in the standing gate; any regression is a RED that blocks bless, same as functional rows (I9).

## Execution model for the lag sprint (M19-I + M20) — PO-ratified 2026-07-24

- **Worker models — Director/PO correction 16:15:** Fable/GPT remain accountable lane owners; Grok/Composer are restored for small, contract-specified delegated tasks such as gate/soak runs, mirror checks, boilerplate, log triage, evidence collection, and exact-anchor mechanical sweeps. Every dispatch names its Fable/GPT supervisor and acceptance check in advance. Lower-tier output cannot author risk-file product changes, issue an accepting verdict, or enter a commit until the named supervisor independently verifies and signs it. The specific failed cycle-16 slices remain quarantined.
- **M0 checkpoint (mandatory, before ANY edit):** after the b57/M19 A–H integration lands on main — commit clean tree, tag `pre-lag-sprint-<date>`, record commit SHA + build ID + chart/homepage image digests here on the board, verify the D-031/D-034 tripwire on that exact build. Rollback path = checkout tag + rebuild + digest verify. No worker starts before M0 is sealed and recorded.
- **M0 SEALED — CKPT-015 (2026-07-24):** lightweight rollback tag `pre-lag-sprint-20260724` resolves remotely to source `a8d5f721d4ba27e2fda1c7ffd18f91d3cc5a8bbc`; build ID `20260723b57`; chart image `ghcr.io/ayoubsiyari/talaria-trading-chart@sha256:f1057e01b021e1ad98f7418bf4464ccfb4868a205072d7d33c01a5fcf9a9d99f`; homepage image `ghcr.io/ayoubsiyari/talaria-homepage@sha256:9cbcd51c080cc6eef56c0058ed9fa6889fe736c2551b888bcbf6610b8ad47056`. Source/proof preflight is GREEN (33 uniformity checks), exact-digest image tripwire is GREEN (29 layout/label checks), and provenance manifest SHA-256 is `11fb4f120d52cbaef5c6841efe88982d3ddf86df731f375e7dc53df871e3c219`. Rollback remains b56 (`ef54bc45e236532925ce094271bdd19a1bf6dec4`) at its recorded immutable digests.
- **M1 CANDIDATE — CKPT-016 (not yet a milestone claim):** source tag `m1-m19i-20260724b58-source` resolves remotely to `9cacd3ec8db3f428fbc433725145aceb173eb27d`; build ID `20260724b58`; chart image `ghcr.io/ayoubsiyari/talaria-trading-chart@sha256:d29174d6c8ee5d32c15ecebd7d30c74fe681448b8ee29b21099a2226609acefc`; homepage image `ghcr.io/ayoubsiyari/talaria-homepage@sha256:63cdc998487642d08e4c83af6ec669150558e2ddac59b06f06588e2091fff678`. Local source/proof preflight is GREEN (33 checks), exact-digest image tripwire is GREEN (29 checks), and manifest SHA-256 is `07389b5b9d69f6d550bc6ab4120a16dfdec9bddd2790867f7192722884920834`. M19-I local acceptance is GREEN, but M1 remains OPEN until exact-digest test-environment deployment, tripwire, and deployed soak/switch evidence pass.
- **M1 CANDIDATE — CKPT-017 (superseded by CKPT-018; not a milestone claim):** source tag `m1-m19i-20260724b59-source` resolves remotely to `254051afe5c2b58f85c53763256a9641229f0656`; build ID `20260724b59`; chart image `ghcr.io/ayoubsiyari/talaria-trading-chart@sha256:270b4475642cac9166e9c5071a85f1328697f7c7d208a29b97d9192a57b6c99b`; homepage image `ghcr.io/ayoubsiyari/talaria-homepage@sha256:86162180e6759621660281f28056b5efb378c99612b06e84c315d86ba0a28c3c`. Source/proof preflight is GREEN (33 checks), exact-digest image tripwire is GREEN (29 checks), and provenance manifest SHA-256 is `cd1ccfa7ac5f79a3eca86eff1dbbfbf33f8bc21688e3133f80fe02e3079b654a`. Local M19-I compute and M19-I-f presentation gates are GREEN; I-f at 100x recorded 0 stale frames, 0 maximum bar delta, 0 ms p95/max catch-up lag, and 0 uncovered/rejected/full-async bridge fallbacks. PO feel on deployed b59 was still RED for SMA/EMA/WMA/DEMA/TEMA at high speed in tick mode → M19-I-g.
- **M1 CANDIDATE — CKPT-018 (superseded by CKPT-019; not a milestone claim):** source tag `m1-m19i-20260724b60-source` resolves remotely to `c9c5ee679ac41c79a6f32bb1dc697ca4d4c58707`; build ID `20260724b60`; chart image `ghcr.io/ayoubsiyari/talaria-trading-chart@sha256:40c8ab08ef91a73c83b944a9fab96baa72546abd64d5b00d0a86dc0938c8d945`; homepage image `ghcr.io/ayoubsiyari/talaria-homepage@sha256:0689f9654a4c55df1cca03f0093ce32dd9174e36bde996ed0cf7110ee8051c14`. Source/proof preflight is GREEN (33 checks); provenance manifest SHA-256 is `e70e37d48dc8080970ce810641b08d93321a8e76dd168fb9d1902d283b00ea57`. Added M19-I-g forming-tip refresh, but I-g2 proved a separate default tick-playback failure: the internal 2× speed multiplier silently forced UI 60×/100× into commit-only fast mode.
- **M1 CANDIDATE — CKPT-019 (deployed to test; PO RED; not a milestone claim):** source tag `m1-m19i-20260724b61-source` resolves remotely to `469778e3f182645da6578d1c845ca9dd85599235`; build ID `20260724b61`; chart image `ghcr.io/ayoubsiyari/talaria-trading-chart@sha256:4b05eb06de35797215a218b275103c7e0d726db4a479fe64de3b7b117da31f54`; homepage image `ghcr.io/ayoubsiyari/talaria-homepage@sha256:24ebce3b74e2021a2edb77ae5b1881eef169654357e5fb9379f463b5fc03144f`. Source/proof preflight is GREEN (33 checks), exact-digest image tripwire is GREEN (29 checks), and provenance manifest SHA-256 is `0e465e00ec4848d23ec9af721716054c8e02e3c7be25db73c5b63cb029f4800f`. Adds M19-I-g2 label-coherent tick speed and frame-budgeted forming paints through 100× (kill-switch `__TALARIA_DISABLE_M19I_TICK_SPEED_COHERENCE_V1`). Exact-digest deployment is healthy and serves b61; the deployed five-MA browser gate reports zero stale/value-mismatch/frozen frames and zero fast-mode ratio. **PO retest on confirmed b61 still sees indicators lagging price at high speed. Automation GREEN is therefore non-accepting evidence with a proven visual blind spot; M1 remains OPEN and is paused for Director workflow review / M19-I-g3 diagnostic redesign.**
- **Milestones (manager reports each to the PO, who relays manually):**
  - **M0** — checkpoint sealed (SHA/build/digests on the board, tripwire green).
  - **M1** — M19-I green: 8-indicator soak flat, per-fix switch-OFF discriminators, deployed to test env.
  - **M2** — QUICK-KILL Q1–Q8 landed: idle soak RED→GREEN is the acceptance signal.
  - **M3** — families M20-A + M20-B landed: census flat across cycle tests, loaded-soak heap slope bounded, D-030 switch-OFF proofs for every order-file change.
  - **M4** — families M20-D + M20-C landed, anti-lag gate (idle soak + loaded soak + census + byte budgets) permanent in the standing CI gate, PO feel-check on deployed build. M20 closes here.
- Each milestone report must contain: build ID + SHA, soak before/after numbers, kill-switch inventory delta, and any Director-escalation-worthy deviation. No milestone is claimed on local-only evidence; M1+ require the deployed test surface.

## M20 rules
- One gated, switchable fix per sub-item; state-matrix per fix (idle / paused / playing / multichart on-off / sync on-off); RED-first via the relevant soak or census row.
- Lane ownership as marked; no cross-lane edits of `order-manager.js` outside Lane 3.
- Do NOT ship fixes for M20-C(1) (redrawAll/CSS transform) without re-proving M19-R/TAL-01585 (drawing layer must not visually detach from candles mid-pan) — that ticket is the known regression risk of exactly this optimization.
- Closure: M20 closes when the QUICK-KILL list + families A-D are landed or explicitly Director-waived with a written cost note, and the anti-lag gate (idle soak + loaded soak + census + budgets) is GREEN on a deployed tripwire-verified build, PO-felt-confirmed.

---

# Director ruling 2026-07-24 (b) — Advisor v3 adopted: parity mandate, M21 rendering architecture, sprint staffing

The PO widened the mandate per Advisor Response v3: the end state is **matching then beating TradeZella's measured profile** on the identical 4-panel loaded scenario — tab ≤ 700 MB with ~zero slope over a 30-min loaded soak, main-thread CPU below one core, 60fps interaction, frame-accurate indicator endpoint. Advisor's Workstreams A and B map onto the existing board (A = M20-B + Q3/Q4; B = M19-I + Q1/Q2/Q5-Q9 + M20-C/D). Workstream C is NEW and lands here as **M21**. Advisor's measurement asks fold into the standing measurement tasks. All prior rules (kill-switch per fix, RED-first, D-030/D-031/D-034, I15) bind unchanged.

## Director adjustments to the advisor text (binding)
1. **G2 endpoint fix direction:** the advisor's argument against paint-time extrapolation is ACCEPTED — a guessed indicator value at the decision point is unacceptable in a trading tool. The b62 package uses a **synchronous exact tail step at paint time**; the worker owns history. This supersedes the extrapolation option in brief §G2.
2. **C3b is the highest-regression item on the whole board.** Plan-1/Plan-2 history says multichart interaction contracts break under exactly this kind of consolidation. C3b may not start until C2 + C3a are scorecard-proven, and it inherits the full multichart regression harness as its acceptance gate, plus a PO interaction check.
3. **C4 lean endorsed:** in-house engine (worker-rendered, single-runtime) is the default road; the lightweight-charts spike must beat it on the scorecard to justify re-opening the settled design language. Spike runs during C2/C3 so the number is ready. [PO] decides.

## M21 — Rendering architecture (Workstream C), phases as advisor C0–C4
- **M21-0 Parity scorecard:** fixed scripted 4-panel loaded scenario, 30-min soak; records tab memory + slope, GPU-process share, main-thread CPU, pan/replay fps, painted-endpoint accuracy. TradeZella baseline from measurement 1a is the printed target line. Defined BEFORE any C-phase code.
- **M21-1 Render quick wins** (overlap M20-C first wave): dormant CSS-transform pan path invoked (TAL-01585 re-proof mandatory); `desynchronized: true` on main candle canvas (`chart.js:919`); resize-in-mousemove backing-store churn fix. Each kill-switched.
- **M21-2 OffscreenCanvas render worker:** candle layer first, then indicator layer; `transferControlToOffscreen`, render worker owns a compact typed-array mirror of the visible window (shared with B2's transferred-buffer pool). One layer at a time, kill-switch + scorecard row each. [PO] pixel-fidelity spot check per layer.
- **M21-3a Single data owner:** one context owns the bar store; panels consume transferred/shared views, no per-panel full copies.
- **M21-3b Single render runtime, four views:** one engine, one compute pipeline, one render scheduler, four cameras. Gated per adjustment #2. [PO] interaction check.
- **M21-4 Renderer core decision [PO]:** in-house (default) vs lightweight-charts spike, decided on scorecard numbers only.

## Landing order (advisor §6 adopted)
Measurement 1a (today, zero-code) → wave 1: A1 screenshots-to-IndexedDB + B3 idle drains (Q1/Q2/Q8) + M21-1 → Q9 stage 1 → b62 package (M19-I tail-send + exact-tail endpoint, after the visible-frame RED exists and fails on b61) → M21-2 layer-by-layer → M21-3a → M21-3b → scorecard re-measure → M21-4 [PO] → parity gate locked into CI (heap-slope budget, enter/exit/enter cycle assertion, census, endpoint probe, scorecard-vs-baseline ceiling). Escape hatch: if measurement 1a shows large/growing GPU memory on Talaria, pull M21-1 backing-store fix and M21-2 forward.

## Sprint staffing (PO-approved model budget: best available)
- **Manager:** GPT 5.6-class (unchanged).
- **W1 — Lane 1, compute pipeline (Fable 5):** b62 package = M19-I tail sends/transferred buffers/buffer pool + exact-tail paint step + visible-frame RED partnership with W5. Owns `chart-indicators-full.js`, indicator worker.
- **W2 — Lane 3, retained set (Fable 5):** A1 screenshots→IndexedDB (D-030/I16 bind), caps/LRUs, retained-master release, AbortController teardown scope for order-side listeners. Owns `order-manager.js`.
- **W3 — NEW Lane 5, rendering architecture (Fable 5):** M21-1 → M21-2 → M21-3. Owns render paths in `chart.js`, drawings layer glue, render worker. Runs the lightweight-charts spike during M21-2/3 idle time.
- **W4 — Lane 4, quick-kills (Fable 5; upgraded by PO at 15:48):** Q1/Q2/Q5/Q6/Q7/Q8, SVG defs hygiene, AbortController sweep outside order files. Precisely anchored work, but no lower-grade implementation or review is permitted.
- **W5 — Lane 2, measurement + gates (GPT 5.6-class, upgraded):** measurement 1a/1b/1c, allocation timeline, parity scorecard build, visible-frame RED, idle/loaded soaks, census, CI parity gate. Upgraded because dishonest probes are this plan's #1 historical failure mode (ESC-011, b61 blind spot) — the instrument builder must be as strong as the fixers.
- **W6 — Lane 5b, rendering parallel track (Fable 5), PO-approved 2026-07-24:** joins at M21-2 start. W3 keeps the candle layer + render-worker scaffold (the scaffold is single-owner: worker bootstrap, buffer contract, kill-switch plumbing); W6 takes the indicator layer migration onto W3's scaffold, then the M21-1 leftovers and the lightweight-charts spike so W3 goes straight to M21-3a. Before M21-2, W6 pre-builds the render-worker's typed-array visible-window mirror + buffer pool (shared with W1's B2 transfer work) so the scaffold lands ready. Hard rule: W3 owns the scaffold design; W6 never edits the worker bootstrap/contract without W3 sign-off in the report — two Fable workers in one file family need a single design owner to avoid divergent contracts. Expected critical-path saving: 2–3 days.
- Fable/GPT owners may delegate small mechanical tasks to Grok/Composer only under a written pre-dispatch contract and named supervision. Risk-file product edits remain Fable-authored and GPT-accepted; lower-tier output is never accepting evidence by itself. M0 checkpoint rule unchanged. Q9 stays with W1 (consumer audit) per the earlier ruling.

## Execution tempo — FULL-CAPACITY DIRECTIVE (PO order, 2026-07-24, binding on the Manager)
The PO has ordered a full-capacity attack: **minimum worker resting time, maximum safe parallelism, zero quality discount.** Manager obligations:
1. **No idle workers, ever.** Every lane keeps a pre-staged queue of next tasks. The moment a worker's report lands, the manager dispatches its next task in the same cycle — dispatch latency is a manager defect, not a worker defect. If a lane is blocked (waiting on deploy, PO check, or another lane's file), the worker is immediately re-pointed at its queue: RED-building for its next fix, consumer audits, report hardening, test expansion, or its lane's M20 backlog.
2. **Parallel-safe matrix (what may run concurrently):** W1 (`chart-indicators-full.js`, indicator worker) ∥ W2 (`order-manager.js`) ∥ W3/W6 (render paths per the scaffold-ownership rule) ∥ W4 (quick-kill files outside order files) ∥ W5 (instruments, no product edits) — these five tracks are file-disjoint by design and run simultaneously at all times. The ONLY serialization points: (a) integration/merge windows, (b) `chart.js` hunks shared between W3/W6 and W4's Q1/Q2 — W4's `chart.js` quick-kills land in wave 1 BEFORE M21-1 render edits begin, then W3/W6 own those regions; (c) D-034 checkpoint assembly.
3. **Safety is what makes the speed real:** parallelism never waives the rules — kill-switch per fix, RED before fix, one-toggle A/B, hunk-staged commits against a manifest (the ESC-019 lesson), independent verification before any GREEN claim. A false green costs more days than any idle hour saved. Full capacity means no *waiting*, not no *checking*.
4. **Continuous integration cadence:** do not batch a day of work into one merge. Integrate per-fix as each proof completes, checkpoint at every D-034-compliant opening (3–6/day per the standing checkpoint order), so no worker ever waits long on an integration window and a rollback never loses more than a few fixes.
5. **Board pulse every cycle:** per worker — task in flight, ETA, next queued task, blocker (if any) with the unblock action and owner. A worker with no "next queued task" listed is a reportable manager miss.
6. **Escalation fast-path:** blockers that need the Director or PO (credentials, deploy auth, spec calls, [PO] touchpoints) are relayed the moment they surface, batched never.
7. **Supervised delegation (Director/PO correction, 16:15):** Fable/GPT own contracts, risk-file implementation, and acceptance. Grok/Composer run only small, bounded delegated work with the supervisor named in the pulse, contract and acceptance check recorded before dispatch, and supervisor verification before use. API-capacity renewal remains available; delegation restores throughput but never substitutes for owner judgment. Failed cycle-16 slices stay quarantined.

### FULL-CAPACITY CYCLE 1 — Manager start log (2026-07-24)
- **M0:** RECONFIRMED sealed — tag `pre-lag-sprint-20260724` = `a8d5f721d4ba27e2fda1c7ffd18f91d3cc5a8bbc` (ancestor of HEAD); build `20260723b57`; evidence `CKPT-015-20260723b57-M0.provenance.json` present. Rollback path unchanged.
- **Measurement 1a:** DISPATCHED to W5 (zero product edits). Seed baseline from advisor brief §G1 pending formal capture package.
- **Wave 1 simultaneous dispatch:** W5∥W4∥W2∥W1∥W3(prep)∥W6 — file-disjoint. W3 product edits on `chart.js` gated until W4 Q1/Q2 report LAND.
- **PO escalations (instant):** (1) confirm whether brief §G1 TradeZella ~714 MB / GPU-share numbers are accepted as the printed measurement-1a target line, or re-run TradeZella capture today; (2) dedicated non-admin `TEST_EMAIL`/`TEST_PASSWORD` still required for any auth-bound soak cells — export into W5 shell only, no admin substitute.

### FULL-CAPACITY CYCLE 2 — capacity fallback + W4 integration review (2026-07-24)

Five premium workers (W1/W2/W3/W5/W6) hit the API-usage ceiling before producing work. Manager immediately dispatched Grok preparation/audit substitutes so no lane idles, but **no substitute may author premium-lane product edits or issue an accepting GREEN**; Fable/GPT sign-off remains mandatory.

| Worker | In flight | ETA | Next queued | Blocker / unblock owner |
|---|---|---|---|---|
| W5 | Preliminary measurement-1a package + visible-frame RED scaffold (no product edits) | same cycle | GPT-5.6 independent verification; M21-0 scorecard | **BLOCKED-PREMIUM-CAPACITY**; PO/Director restores model quota. Auth-bound cells separately need non-admin QA credentials |
| W4 | Q5 + Q7 RED→fix; Q6 staged only | same cycle | Q6 after W1 Q9 audit releases `replay-system.js` | none |
| W2 | A1 screenshot consumer/IndexedDB audit + RED fixtures only | same cycle | Fable-authored A1 implementation; then Q4 | **BLOCKED-PREMIUM-CAPACITY**; PO/Director restores Fable quota |
| W1 | Q9 consumer audit + exact-tail/visible-frame RED design only | same cycle | Fable-authored Q9 stage 1; b62 after b61 visible RED | **BLOCKED-PREMIUM-CAPACITY**; PO/Director restores Fable quota |
| W3 | M21-1 RED harnesses/hunk manifest only | same cycle | Fable-authored M21-1 after W4 integration | W4 integration window + **BLOCKED-PREMIUM-CAPACITY** |
| W6 | Typed-array mirror/pool test-reference prep only | same cycle | Fable/W3 scaffold sign-off; M21-2 indicator layer | **BLOCKED-PREMIUM-CAPACITY**; PO/Director restores Fable quota |

- **W4 integration review:** focused gate passed, but Manager rejected the first Q1 GREEN because its portal fallback observed `document.body` and could still trigger document-wide span scans. Manager narrowed Q1 to known settings roots only and added explicit no-body-observer/no-global-span-scan assertions; Q8 kill-switch was also corrected so new sync hooks do not amplify legacy interval stacking. Corrected gate, syntax, lint, and dual-tree parity are GREEN; independent review remains in flight before commit.
- **Serialization:** W3 still has no permission to product-edit `chart.js` until the corrected W4 commit lands. W4 Q5/Q7 remain file-disjoint; Q6 waits for W1's read-only Q9 audit.
- **Instant escalation:** premium GPT/Fable API capacity is now the critical resource blocker. Safe preparation continues, but accepting evidence and premium-lane product changes cannot advance without restored quota.

### FULL-CAPACITY CYCLE 3 — preliminary reports consumed; next queues dispatched (2026-07-24)

| Worker | In flight | ETA | Next queued | Blocker / unblock owner |
|---|---|---|---|---|
| W5 | Extend painted-endpoint probe from synthetic one-panel to exact four-panel HOST/peer + canvas-pixel scenario | same cycle | GPT-5.6 independent verify; live 1a scorecard fill | Synthetic b61 cell had 878/853 dense samples but **did not reproduce RED**; premium quota + non-admin QA credentials + PO 1a target decision |
| W4 | Q5/Q7 product work plus focused Q2 geometry-safe repaint correction | same cycle | Independent Q1/Q2/Q8 re-review → per-fix commit; then Q6 | Q2 remains integration blocker until changed-geometry oracle passes |
| W2 | Q4 trail/sl-modification consumer audit + RED fixtures (no product edit) | same cycle | Fable signs/lands A1, then Q4 | premium quota; durable screenshot tier policy remains PO/Director-open |
| W1 | Harden Q9 RED scaffold/consumer manifest; compatibility read with W6 | same cycle | Fable Q9 stage-1 land; b62 exact-tail after true W5 b61 RED | premium quota; b62 remains blocked because visible-frame RED is not yet reproduced |
| W3 | Independent Q2 pixel/geometry oracle + TAL-01585 RED hardening; no `chart.js` product edit | same cycle | Fable signs M21-1 H1–H6 and lands after W4 commit | premium quota + W4 integration |
| W6 | Transfer/subview/stale-tail compatibility tests against existing pack contract | same cycle | W3/Fable contract sign-off; M21-2 indicator migration | premium quota + W3 scaffold |

- **Preliminary evidence retained, not promoted:** W2 measured ~17.5 MB embedded screenshot retention versus ~24 KB projected slim (~719×); W1 Q9 audit permits only a narrow reusable owned prefix and rejects zero-copy; W3 M21-1 RED scaffold reports ten current contract failures; W6 reference pool tests are local-only. All remain pending premium-owner sign-off.
- **W5 ruling:** high sample density alone is not acceptance. The single-panel synthetic probe returned `NO-PAINTED-LAG-OBSERVED-INCOMPLETE`, therefore it neither unlocks b62 nor overturns PO RED.
- **Independent W4 review:** its Q1/Q8 findings were captured before the Manager corrections and are being re-probed; its Q2 ghost/overdraw finding is valid. Q2 now requires geometry-stable direct paint and full-render re-anchor on any y/axis/rect/spread change.

### FULL-CAPACITY CYCLE 4 — W6 compatibility consumed; no-idle requeue (2026-07-24)

| Worker | In flight | ETA | Next queued | Blocker / unblock owner |
|---|---|---|---|---|
| W5 | Four-panel HOST painted-endpoint/canvas-pixel extension | same cycle | GPT-5.6 verify → live measurement 1a | premium quota + QA credentials + PO target-line decision |
| W4 | Q2 geometry correction ∥ Q5/Q7 | same cycle | independent re-review/commit → Q6 | Q2 integration gate |
| W2 | Q4 RED/audit prep | same cycle | Fable A1/Q4 product land | premium quota + screenshot durable-tier policy |
| W1 | Q9 RED hardening + W6 descriptor compatibility read | same cycle | Fable Q9 stage-1 → b62 only after W5 RED | premium quota + missing true b61 visible RED |
| W3 | Independent Q2 oracle + TAL-01585 RED hardening | same cycle | Fable M21-1 after W4 commit | premium quota + W4 serialization |
| W6 | Existing in-repo lightweight-charts/single-runtime prototype audit against M21-0; no installs/product edits | same cycle | Existing-prototype scorecard dry-run → M21-2 wait queue | premium/W3 sign-off |

- **W6 compatibility ruling:** stride-6 layout and transfer ownership match W1 intent, but any capacity-backed buffer must carry explicit `byteOffset`/`byteLength`/`elementCount`; bare `new Float64Array(buffer)` is prohibited because it exposes stale tail capacity. Twelve hardened fixture tests pass; evidence remains preliminary.

### FULL-CAPACITY CYCLE 5 — Q2 correction complete; re-review + Q6 dispatched (2026-07-24)

| Worker | In flight | ETA | Next queued | Blocker / unblock owner |
|---|---|---|---|---|
| W5 | Exact four-panel HOST painted-endpoint RED extension | same cycle | GPT verify/live 1a | premium quota + QA credentials + PO 1a target |
| W4 | Q1/Q2/Q8 independent re-review ∥ Q5/Q7 ∥ Q6 listener teardown | same cycle | per-fix integration commit; M20-A teardown sweep | corrected bundle awaits independent APPROVE |
| W2 | Q4 RED/audit prep | same cycle | Fable A1/Q4 land | premium quota + durable-tier policy |
| W1 | Q9 RED hardening/descriptor contract | same cycle | Fable Q9 stage-1; b62 after W5 RED | premium quota + no true b61 RED yet |
| W3 | Q2 independent oracle/TAL-01585 hardening | same cycle | Fable M21-1 after W4 commit | premium quota + serialization |
| W6 | Existing renderer-spike scorecard audit | same cycle | scorecard dry-run | premium/W3 sign-off |

- **Manager Q2 verification:** stable geometry direct-paints with zero full render; changed/absent y/axis/rect/spread performs zero direct paint and schedules one full re-anchor; hidden/appear/kill paths discriminate. Combined W4 + independent W3 oracle run: 12 pass, 0 fail, 2 evidence-writer skips; syntax and all four dual-tree pairs match. Commit still waits for the independent re-review report.

### FULL-CAPACITY CYCLE 6 — four-panel index oracle consumed; value/Y oracle dispatched (2026-07-24)

| Worker | In flight | ETA | Next queued | Blocker / unblock owner |
|---|---|---|---|---|
| W5 | Four-panel HOST actual painted endpoint **value/Y** versus independent forming-OHLC expected Y | same cycle | Auth-bound PO cell → GPT-5.6 verify → live 1a | premium quota + non-admin `TEST_EMAIL`/`TEST_PASSWORD`/`TEST_VPS_URL` |
| W4 | Corrected bundle re-review ∥ Q5/Q7 ∥ Q6 | same cycle | per-fix commit → M20-A teardown | independent APPROVE |
| W2 | Q4 RED/audit prep | same cycle | Fable A1/Q4 land | premium quota + durable tier |
| W1 | Q9 RED hardening/descriptor contract | same cycle | Fable Q9 stage-1; b62 after true W5 RED | premium quota + RED absent |
| W3 | Q2 oracle/TAL-01585 hardening | same cycle | Fable M21-1 after W4 commit | premium quota + serialization |
| W6 | Existing renderer-spike scorecard audit | same cycle | scorecard dry-run | premium/W3 sign-off |

- **W5 evidence ruling:** the four-panel host probe met strict density (65/116 evaluated samples at 60×/100×) and observed worker-busy pressure, but painted endpoint **bar index** remained aligned. This is `INCOMPLETE`, not GREEN and not the required b61 RED. Next oracle compares actual final drawn endpoint value/Y against the independently computed expected indicator Y; b62 remains locked.

### FULL-CAPACITY CYCLE 7 — first integration opening; W3 serialization cleared (2026-07-24)

| Worker | In flight | ETA | Next queued | Blocker / unblock owner |
|---|---|---|---|---|
| W5 | Four-panel actual endpoint value/Y oracle | same cycle | Auth PO cell → GPT verify/live 1a | premium GPT quota + QA credentials |
| W4 | Q5/Q7 ∥ Q6 | same cycle | scoped integration commits → M20-A teardown | none |
| W2 | **Fable A1 product land** ∥ fallback Q4 RED prep | same cycle | independent A1 verify/commit → Fable Q4 | premium retry in flight; durable-tier policy only affects later server tier |
| W1 | Q9 RED hardening/descriptor contract | same cycle | Fable Q9 after Q6 releases `replay-system.js`; b62 after W5 RED | Q6 file window + true RED |
| W3 | **Fable M21-1 product land** | same cycle | independent verify/commit → M21-2 candle scaffold | premium retry in flight |
| W6 | Existing renderer-spike scorecard audit | same cycle | scorecard dry-run → M21-2 support | W3/Fable contract |

- **Integrated:** `f7700e00a` — scoped Q1/Q2 (`chart.js` + Q2 oracle mirrors); `5cd010bb8` — scoped Q8 (`alert-system.js` + combined gate mirrors). No other dirty-lane files entered either commit.
- **Post-commit verification:** combined Q1/Q2/Q8 + independent Q2 oracle: 12 pass, 0 fail, 2 evidence-writer skips. Q1/Q2 `chart.js` serialization is now **CLEARED** for W3.
- **Checkpoint cadence:** this is integration opening 1, not a D-034 checkpoint claim. The next exact-build checkpoint opening follows the independently verified Q5/Q6/Q7/M21-1 lands; assembly remains serialized by D-034.

### FULL-CAPACITY CYCLE 8 — Q6 review + next M20-A item dispatched (2026-07-24)

| Worker | In flight | ETA | Next queued | Blocker / unblock owner |
|---|---|---|---|---|
| W5 | Endpoint value/Y oracle | same cycle | Auth PO cell → GPT verify | QA credentials + GPT quota |
| W4 | Q6 independent review ∥ Q5/Q7 ∥ favorites-manager listener teardown | same cycle | scoped commits → timezone listener API audit | Q6 review only |
| W2 | Fable A1 product ∥ Q4 RED prep | same cycle | independent A1 review/commit → Q4 product | premium retry |
| W1 | Q9 hardening | same cycle | Fable Q9 after Q6 commit | Q6 shared-file window + W5 RED for b62 |
| W3 | Fable M21-1 product | same cycle | independent review/commit → M21-2 | premium retry |
| W6 | Dependency-free archived-renderer scorecard dry-run | same cycle | M21-2 acceptance contract | W3 scaffold |

- **Manager Q6 verification:** focused listener census plus M19-H and M19-I-g2 replay regressions: 8 pass, 0 fail, 1 evidence-writer skip; replay source/test mirrors match and Q9 slice anchors remain untouched. Commit waits for independent review.
- **Renderer decision evidence:** current tree has no live lightweight-charts product path; the archived prototype lacks replay/indicator/profile surfaces. W6 may model only supported ownership/sync cells and must mark absent scorecard rows NOT-MEASURABLE—never GREEN.

### FULL-CAPACITY CYCLE 9 — visible value/Y RED found; Q6 integrated; Q9 released (2026-07-24)

| Worker | In flight | ETA | Next queued | Blocker / unblock owner |
|---|---|---|---|---|
| W5 | GPT-5.6 independent value/Y RED verification ∥ 60× density diagnostics | same cycle | Auth PO cell → live measurement 1a with TEMA target | non-admin QA env vars |
| W4 | Q5/Q7 ∥ favorites independent review ∥ timezone listener audit | same cycle | scoped commits → remaining M20-A teardown | favorites review |
| W2 | Fable A1 product ∥ Q4 RED prep | same cycle | independent A1 review/commit → Q4 product | premium retry |
| W1 | **Fable Q9 narrow stage-1 product land** | same cycle | independent Q9 review/commit → b62 exact-tail after GPT RED acceptance | GPT RED acceptance only |
| W3 | Fable M21-1 product | same cycle | independent review/commit → M21-2 | premium retry |
| W6 | standalone OffscreenCanvas resize/CSP/multiframe browser preflight | same cycle | rebind acceptance harness to W3 scaffold API when landed | W3 scaffold |

- **Preliminary b61 RED:** value/Y oracle finally measures the reported boundary: TEMA endpoint error and stale-frame ratios exceed limits at 60×/100× with strict density. This is the first non-proxy reproduction and remains `PRELIMINARY-PENDING-GPT56-INDEPENDENT-VERIFY`; no fix/GREEN is claimed.
- **Integrated:** `2f0ce7831` — scoped Q6 replay floating-toolbar listener teardown and mirrored test. Post-commit Q6 + M19-I-g2 gates pass. The Q9 shared-file window is now **CLEARED** and Fable W1 is in flight.
- **b62 gate:** product exact-tail work remains locked until GPT-5.6 accepts the b61 value/Y RED. Q9 allocation stage 1 is a separate approved landing-order item and may proceed now.
- **PO measurement-1a decision:** TEMA is the binding primary painted-endpoint target. Credential source is local environment (`TEST_EMAIL`, `TEST_PASSWORD`, `TEST_VPS_URL`); all three were still missing at the 13:09 preflight and are escalated as the live-cell blocker. Never place their values in board/report/evidence.

### FULL-CAPACITY CYCLE 10 — M21-1 Fable land under review; M21-2 new-file scaffold starts (2026-07-24)

| Worker | In flight | ETA | Next queued | Blocker / unblock owner |
|---|---|---|---|---|
| W5 | safe pre-document b62 A/B flag hook + apply-tip observability | same cycle | exact-digest b62 painted A/B → authenticated measurement 1a | b62 product + PO env vars |
| W4 | Q5/Q7 ∥ favorites independent review ∥ timezone audit | same cycle | scoped commits → remaining M20-A | independent reviews |
| W2 | A1 independent review ∥ Fable Q4 RED/contract sign-off | same cycle | A1 commit release → Q4 product | independent A1 review |
| W1 | Q9 independent review ∥ b62 exact-tail contract prep | same cycle | Q9 commit → **authorized b62 product land** | independent Q9 review |
| W3 | M21-1 independent review ∥ M21-2 new-file worker/contract scaffold | same cycle | M21-1 commit release → M21-2 chart wiring | independent M21-1 review |
| W6 | M21-4 fail-closed renderer decision scorecard/evaluator | same cycle | M21-2 rebind to signed W3 API and accepted W1 tail contract | W3 API + mandatory live metrics |

- **M21-1 Fable land:** product/test/report/evidence are present uncommitted in both trees; no blocker reported by author. Independent review owns the accepting verdict.
- **Manager gate:** scoped syntax/diff checks plus M21-1, Q1/Q2/Q8, Q2 pixel-safety oracle, and Q5/Q7 suites: 22 pass, 0 fail, 4 evidence-writer skips. This is supporting evidence, not independent acceptance.
- **Serialization:** `chart.js` remains locked. W3 may build only new M21-2 module/worker/test files until M21-1 is independently approved and committed; W6 remains test-fixture-only until W3 signs the scaffold API.
- **Q9 Fable land:** narrow owned-prefix product/test/report/evidence are present uncommitted in both replay trees. Manager Q9/Q6/M19-I-g2/Q5/Q7 gate: 24 pass, 0 fail, 2 evidence-writer skips. Independent review owns acceptance; replay product remains locked until commit.
- **W5 repeat matrix:** predeclared fixed-20s TEMA runs completed without gate changes; 100× failed the painted endpoint in all three density-valid runs, while 15× was mixed. Evidence remains preliminary. Auth variables were rechecked at 13:25 and remain missing; causal tracing replaces further blind repeats.
- **A1 Fable land:** IndexedDB screenshot externalization product/test/report/evidence are present uncommitted in both order-manager trees. Manager A1/Q4/excursion gate: 57 pass, 0 fail; additional order lifecycle/frame/owning-panel run: 24 test pass, 0 fail (owning-panel internal census 29/0). Independent review owns acceptance; `order-manager.js` stays locked.
- **Binding W5/GPT verdict:** `ACCEPT-B61-VALUE-Y-RED`. The correct painted TEMA value/Y boundary now reproduces the PO failure on byte-verified b61 with density-qualified repeats and low-speed calibration. This clears the b62 evidence prerequisite. Q6 is already integrated at `2f0ce7831`; b62 product work is authorized immediately after Q9 commits the shared replay baseline.

### FULL-CAPACITY CYCLE 11 — b61 RED accepted; M21-2 scaffold under review (2026-07-24)

| Worker | In flight | ETA | Next queued | Blocker / unblock owner |
|---|---|---|---|---|
| W5 | worker/result/render/paint causal trace | same cycle | authenticated TEMA measurement 1a | PO env vars |
| W4 | Q5/Q7 ∥ favorites independent review ∥ timezone audit | same cycle | scoped commits → remaining M20-A | independent reviews |
| W2 | A1 independent review ∥ real-browser IndexedDB durability/lifecycle harness | same cycle | A1 commit → Q4 product | independent A1 review |
| W1 | Q9 blocked-fix correction ∥ **b62 quarantined correction pending** | same cycle | Q9 re-review/commit → corrected b62 independent/W5 verification | Q9 + b62 independent BLOCKs |
| W3 | **M21-1 blocked-fix correction** ∥ M21-2 scaffold review/browser harness | same cycle | M21-1 re-review/commit → M21-2 `chart.js` wiring | follower/pinch/resize defects |
| W6 | M21-2 `chart.js`/multichart wiring RED integration gate | same cycle | hand gate to W3 → indicator layer after W1/W3 gates | M21-1 + scaffold approvals |

- **M21-2 new-file scaffold:** Fable-owned bridge, same-origin worker, API contract, and RED-first gate are present uncommitted with no `chart.js`/CSP/bootstrap edits. Manager scaffold + M21-1 gate: 29 pass, 0 fail, 3 evidence-writer skips. Independent review owns acceptance.
- **Serialization:** M21-2 browser harness may add only new fixture files. Existing scaffold modules remain review-locked, and all `chart.js` wiring remains blocked until M21-1 commits.
- **b62 root-cause RED:** W1's real-product RED prep isolates a same-bar stale forming-close worker reply accepted by the b61 length+TF tail guard. All planned product hunks are indicator-file-only, so implementation proceeds in parallel while Q9 replay review finishes; integration order remains Q9 commit first. Independent GPT mechanism review is in flight and W5 alone owns painted GREEN.
- **Q9 independent BLOCK:** reusable prefix identity exposed `ChartDataPipeline` incremental resampling to a previously playhead-trimmed coarse bucket, producing stale prior OHLC; Q9 tests also imported excluded untracked W6 fixtures. No Q9 integration is allowed. Fable correction must invalidate the affected chart resample cache on each reusable-prefix mutation, add an end-to-end replay→pipeline→trim oracle, remove the W6 dependency, and prove a clean scoped checkout before re-review.
- **W5 causal correlation:** GPT verdict `INCONCLUSIVE` with high confidence. The trace is compatible with RC1 but never observes or causally links the applied TEMA tip; “current” meant only seq-match/event presence. The one compute classification is also inconclusive. `ACCEPT-B61-VALUE-Y-RED` remains intact, while b62 mechanism acceptance remains BLOCKED.
- **M21-1 independent BLOCK:** live follower pan left drawings 18–28 px detached with a persistent transform; pan→pinch performed two redraws; deferred resize fired after intervening resize/detach; switch-OFF evidence was synthetic and the manifest inconsistent. No M21-1 commit or M21-2 `chart.js` wiring is allowed. Fable correction and permanent browser/runtime discriminators are in flight.
- **b62 GPT mechanism BLOCK:** RC1 is plausible but not accepted. The RED suite copied/stubbed the real apply path, R1–R4 were marker checks, identity omitted volume/params/pair-dataset generations, merges were non-atomic, paint fingerprints could publish before success, synchronous workload was unbounded, and probe/control provenance drifted. Concurrent product edits are quarantined: no b62 GREEN/commit/deploy. Re-review requires a stable b61 snapshot; actual delayed-worker recalc→bridge→apply→coalesce→render tests; complete generation + atomic merge; worker/main parity; workload evidence; and W5-approved executable ON/OFF provenance.

### FULL-CAPACITY CYCLE 12 — independent blocks drive corrective wave (2026-07-24)

| Worker | In flight | ETA | Next queued | Blocker / unblock owner |
|---|---|---|---|---|
| W5 | **v2 two-hook A/B harness + full transitive/product pin correction** | same cycle | GPT v2 fresh-baseline acceptance → corrected b62 A/B | harness/provenance review |
| W4 | Q5/Q7 ∥ favorites review ∥ timezone audit | same cycle | scoped commits → remaining M20-A | independent reviews |
| W2 | **A1 F1–F4 browser-RED correction** | same cycle | fresh independent review/commit → Q4 product | identity/logout contract if absent |
| W1 | **full b62 GPT-block correction** | same cycle | GPT mechanism re-review → W5 exact-digest A/B | atomicity + provenance |
| W3 | M21-1 correction ∥ M21-2 bridge fatal/buffer correction ∥ worker correction | same cycle | combine gates → fresh independent reviews | two blocked snapshots |
| W6 | make M21 fixture gates hermetic/no-write by default | same cycle | stable-hash R1/CSP/IB combined rerun | W3 correction |

- **W5 harness status corrected:** the standalone strict allowlisted flag parser is safe, but the composed pre-document function closes over a Node variable and OFF mode sets the product flag without installing value/Y instrumentation. ON/OFF discrimination is invalid. W5 must register flag and probe as two ordered pre-document hooks and prove the actual sink in both cells.
- **M21-2 browser harness:** 36/0 scaffold checks passed in real headless Edge, but unsupported `transferControlToOffscreen` constructs then leaks one worker. The scaffold remains non-integrable until independent review plus R1 correction; R3 reload-only post-transfer kill recovery requires explicit policy confirmation, and R4 teardown ACK loss must be documented.
- **M21-2 R1 confirmed BLOCK:** unsupported or throwing transfer constructs one orphan worker per attempt; three retries peak at three live workers and `destroy()` cannot recover all orphans. W3 correction must feature-detect before construct and terminate/release on transfer throw while preserving construct-before-transfer fallback. The prior scaffold review snapshot is superseded; no integration before fresh review.
- **A1 independent BLOCK:** real-browser and independent probes confirm transaction-abort capture loss, cross-account ref collisions/readability, no logout/retention cleanup, and blocked upgrade/delete. Additional release blockers: async durable saves can reorder and route an old snapshot to a new session; runtime kill after prior externalization persists null blobs; missing/corrupt IDB still emits unrecoverable ref-only durable/export rows; and open `splitGroupId` legs can externalize before aggregation. A1 is quarantined. W2 correction must also close lazy-rerender/cache/scan-bound/content-validation/object-URL gaps, regenerate real-browser evidence, and receive a fresh independent review; Q4 remains locked.
- **M21-2 independent BLOCK expanded:** besides R1, worker errors/message errors and frame post failures do not enter one fatal state; worker ACK/`CANDLE_ERROR` messages are ignored; queued/same-buffer/coalesced ownership can detach or reclaim incorrectly across kill/destroy; descriptors remain mutable after validation; empty frames cannot clear stale candles; null-context INIT reports success; and `Infinity` can poison generation ordering. Worker-only fixes run file-disjoint now; bridge fatal/ownership corrections remain mandatory before fresh review.
- **b62 local land remains quarantined:** the completed +137-line indicator change and 19/19 local suite do not supersede GPT's earlier BLOCK because they were produced concurrently with it. W1 is reworking the land around stable b61/current snapshots, actual delayed-worker end-to-end execution, complete volume/data/pair/params generations, atomic staged commits, success-only fingerprint publication, real parity/failure injection, workload ceilings, and W5's single frozen probe provenance.
- **W5 provenance gate:** the historical accepted probe blob `dbcfff6f…` is absent from Git and exact bytes are not yet recovered. Frozen v1 is not acceptable yet: it includes the broken composition, twin contracts differ by timestamp, transitive harness/server dependencies are unpinned, and the manifest does not enforce every threshold/dataset/browser/product-asset hash. Any current 20s/30s output is quarantined until a corrected externally signed manifest runs unchanged on b61, b62-ON, and identical-byte b62-OFF with pre/post checks.
- **Q9 integrated:** original reviewer approved both corrections. Commit `f8ef6a001` contains the exact 11-file manifest, including forced ignored evidence/report files. The stable-prefix allocation win remains while every consumer preserves legacy full-resample semantics. The replay serialization hold is cleared; this does not clear b62's separate GPT/W5 gates.
- **M21-2 R1 correction:** unsupported transfer now exits before worker construction and transfer throws terminate exactly once with explicit fallback/canvas-loss states; browser leak census is flat. This clears R1 only. Independent bridge blockers (fatal transition/message consumption, post failures, descriptor immutability, pending/transferred/reclaim ownership, empty-frame clear, ACK accounting) remain under active correction, alongside worker-only edge fixes.
- **M21-2 worker correction:** null-context INIT now fails non-active, empty frames clear and remain clear through resize, generations require safe monotonic integers, and malformed frames cannot advance retained state. Manager worker-only gate: 24 pass, 0 fail across both mirrors. Bridge must still consume these error/ACK contracts before combined acceptance.
- **W5 20s/30s bridge remains quarantined:** the immutable probe reproduced density-qualified b61 failures, but historical source continuity is unrecovered and 15× behavior changes materially with dwell. More importantly, the run does not cure GPT's broken composed OFF instrumentation or incomplete transitive/product pinning. W5 is building deterministic v2 with separate ordered flag/probe hooks, actual sink proof in both arms, and full dependency/dataset/browser/asset hash enforcement before GPT sees the baseline.
- **W6 independent bridge snapshot:** 19 hash-pinned probes found 14 GREEN / 5 RED on a stable pre-correction bundle; the five failures are crash→submit, kill-before-flush/microtask, ignored worker ACK/error, and empty-vs-detached zero buffer. These map directly to the active W3 bridge correction and are not acceptance. W6 now removes default evidence-write side effects before the stable-hash combined rerun.
- **Supersession notice:** the next two worker-authored b62 entries record historical preparation/local-land output only. Both are quarantined by the binding GPT/W5 gates above and must not be read as current acceptance, GREEN, or integration authorization.
- **W1 b62 RED-prep (test/contract only, `PRELIMINARY-PENDING-GPT56-RED-ACCEPTANCE`):** new dual-tree suite `m19i-b62-exact-tail-red.test.mjs` fails R1–R4 on unchanged b61 for the product reason — the `CALCULATE_TAIL` commit guard (length+timeframe only) accepts a reply computed from an older forming close of the same bar, and the real worker+merge product is shown overwriting the fresher tip (B1, 0.389 value-unit regression). One-toggle plan: `__TALARIA_DISABLE_M19I_EXACT_TAIL_PAINT_V1`. W5-oracle A/B adapter added without touching W5 files or thresholds. Report `W1-B62-EXACT-TAIL-RED-PREP-20260724.md`; evidence `W1-B62-EXACT-TAIL-20260724-red.json`. Zero product edits; b62 product land only after Manager commits Q9 and releases the GPT gate. `ab-off` browser cell blocked pending a W5 predoc-flag hook.
- **W1 b62 PRODUCT LAND (`FABLE-B62-PRODUCT-LANDED-PENDING-INDEPENDENT-AND-W5-PAINTED-GREEN`, per Manager release directive):** B62-0…B62-3 landed dual-tree in `chart-indicators-full.js` ONLY (+137/−1, byte-identical, syntax clean) behind the single default-ON toggle `__TALARIA_DISABLE_M19I_EXACT_TAIL_PAINT_V1`; switch OFF restores exact b61 stale-overwrite/stale-blit behavior (verified). B62-1 rejects same-length/TF worker tails on a post-time t|o|h|l|c last-bar fingerprint before merge, setting only the boolean coalesce; B62-2 is a re-entrancy-guarded bounded `O(estimateTailLookback)` forming-tail bridge as the first statement of `drawIndicatorsOptimized` (before the layer cache decision) via the shared `_m19ifApplyCoherentBridge`; B62-3 is write-only counters. Hardened suite: 19/19 — G/K tests now execute the REAL product module + REAL worker replies (addresses the mechanism block's copied-apply-path and marker-check findings), including the actual stale-overwrite discriminator ON/OFF and exact worker/main bridge parity (Δ<1e-9 over the identical lookback window). Broad gate (v1.4 canonical): 120/120 non-skip pass incl. M19 I-f/I-g2/I-tail/H, indicators ×5, Q9, Q6, replay ×5, W3/W4 neighbors; Q5/Q7 5 pass. Evidence: `W1-B62-EXACT-TAIL-20260724-{green,kill}.json`; manifest `W1-B62-20260724-HUNK-MANIFEST.json`; report §9 addendum. NOT claimed: painted GREEN (W5-owned; predoc hook now READY in W5's frozen contract) and independent mechanism re-review items outside local reach (stable-snapshot delayed-worker browser end-to-end, W5-approved executable ON/OFF provenance, atomic-merge/generation adjudication). No commit/push/deploy; order stays Q9 → b62.

### FULL-CAPACITY CYCLE 13 — Q9 integrated; high-risk corrections continue (2026-07-24)

| Worker | In flight | ETA | Next queued | Blocker / unblock owner |
|---|---|---|---|---|
| W5 | Composer immutable v2.1 correction ∥ density-short diagnosis | same cycle | GPT v2.1 acceptance → exact-digest b62 A/B | provenance + 15-cell protocol |
| W4 | Q5/Q7 ∥ favorites review ∥ timezone audit | same cycle | scoped commits → remaining M20-A | independent reviews |
| W2 | complete A1 independent-block correction + secure logout wiring | same cycle | fresh browser/independent review → commit → Q4 | persistence/privacy gates |
| W1 | full b62 GPT-block correction | same cycle | GPT mechanism review → W5 exact-digest A/B | atomicity + W5 provenance |
| W3 | M21-1 strong re-review ∥ M21-2 scaffold strong re-review | same cycle | accepted manifest commits → M21-2 `chart.js` wiring | two independent verdicts |
| W6 | hermetic gates + stable-hash combined runner | same cycle | corrected R1/CSP/IB rerun → indicator after b62 | W3/W1 gates |

- **Historical unsupervised outputs remain quarantined:** the 16:15 correction restores future supervised delegation but does not rehabilitate any failed cycle-16 product, test, harness, report, evidence, GREEN, or verdict.
- **W1 b62 quarantine correction delivered (`PENDING-FRESH-GPT-REVIEW-AND-W5-PAINTED-GREEN`; the 19/19 stamp is withdrawn as accepting evidence):** all seven mandated re-proof items are implemented dual-tree in `chart-indicators-full.js` + a rebuilt suite. (1) b61 cells execute the immutable git blob `71981143…` @ `2f0ce783…` with an anti-contradiction hash guard; b62 cells hash the working tree per run. (2) Delayed-worker cells run the ACTUAL `recalculateIndicatorsIncremental` post path, real in-process `indicator-worker.js` with held replies, real `_applyIndicatorWorkerResults`, real coalesce/finish rerun and the real `drawIndicatorsOptimized` blit-vs-redraw decision, ON and OFF. (3) The post-time token now carries last-bar t/o/h/l/c/**v**, dataVersion, TF, active-set+params hash, and the explicit monotonic `_m19iB62DatasetGen`; volume-only mutation, same-shape dataset swap, param edit, add, remove and TF invalidation each proven to reject. (4) `_m19ifApplyCoherentBridge`/apply commits are staged and atomic with bounded snapshot/rollback (object-pack partial-mutation restored byte-exactly under injected failure); `_m19iExactTailLastFp` publishes only after complete success. (5) Worker/main parity ≤1e-9 measured end-to-end for sma/ema/wma/rsi/macd/stoch/atr/**mfi/cmf**/tema + param variants. (6) Workload distributions measured (typical 876 staged points ≪1 ms; 8.5k-point mix p50 ≈8 ms rejected by data) → product ceiling `M19I_B62_MAX_STAGED_POINTS = 4096` with b61-window budget-skip fallback. (7) W5 frozen pin consumed read-only; painted verification `BLOCKED-PROVENANCE` pending GPT56 baseline acceptance; the 15× "maxY≈0.5 px" wording is withdrawn for frequency/stale-ratio calibration against W5's `15X-ELEVATION-MIXED-PAINTED-AND-OTHER` class; W5's post-freeze adapter v2 left untouched. Local: suite 31/31 both trees; broad v1.4 gate 479 discovered / 0 failures attributable to b62 (6 pre-existing W6 deliberate-RED/env gates; 2 load-flaky suites pass standalone); Q5/Q7 drains 5 pass. Report §10, hash-bound manifest + red/green/kill evidence regenerated. No commit/push/deploy.
- **A1 correction status:** real-browser F1–F4 now pass locally, but the original independent review's save ordering/session routing, runtime-kill-after-externalization, missing-ref durability, split-group protection, lazy race/cache/scan bounds, payload validation, URL cleanup, and secure logout integration remain mandatory before fresh review.
- **W5 v2 status:** two-hook instrumentation and transitive pins are locally ready, but 15×/60× were density-short. GPT owns acceptance or a predeclared immutable v2.1 protocol; Composer investigates density without editing the frozen package.
- **W6 handoff:** hermetic/no-write hardening is complete at preliminary status. A Composer worker now owns the stable-hash R1/CSP/IB/product-wiring combined run; any W3 hash drift invalidates that phase instead of mixing evidence.
- **M21-1 correction handoff:** W3 reports every prior independent defect corrected and hash-bound; a fresh GPT review now owns the only accepting verdict. H-S6 fetch ownership and H-S78/BL-16 micro-pan recenter failures reproduce on the clean `chart.js` baseline and have separate Composer root-cause audits; they are not silently waived or folded into M21-1.
- **M21-2 correction handoff:** W3 reports the bridge fatal/ACK/buffer/snapshot/generation defects corrected against the separately fixed worker contract, with no `chart.js` or W6 edits. A fresh GPT review owns acceptance. The W6 combined run must bind to this correction hash and retire its stale R1 expectation only through the exact former-defect discriminator.
- **W5 v2 binding BLOCK:** ordered pre-document hooks are fixed, but frozen v2 is not a provenance-complete multi-speed baseline. Dual-tree enforcement, checksum paths, full transitive/network/browser/dataset pinning, adapter digest refusal, and 15×/60× density all failed acceptance. Frozen v2 remains quarantined; Composer is building a new externally sealed v2.1 with a predeclared 5×3-speed, fixed-30s protocol. Historical `ACCEPT-B61-VALUE-Y-RED` remains valid only as separate evidence.
- **H-S6 owner-fetch root cause:** confirmed preexisting on clean HEAD/current: the host 1m→1h commit races interval fan-out, so peers still at 1m enter finer self-ownership and produce four panel fetches instead of one. Composer is preparing new-file real-browser RED evidence only. The strong `chart.js` product correction is serialized behind M21-1 acceptance/commit and must preserve legitimate panel-initiated finer ownership.

### FULL-CAPACITY CYCLE 14 — corrected mechanisms enter acceptance; W6 exposes residual scaffold gates (2026-07-24)

| Worker | In flight | ETA | Next queued | Blocker / unblock owner |
|---|---|---|---|---|
| W5 | immutable v2.1 build ∥ density diagnosis | same cycle | GPT v2.1 review → exact-digest b62 A/B | full provenance + density |
| W4 | Q5/Q7/favorites/timezone ∥ H-S6 RED prep ∥ H-S78b RED prep | same cycle | scoped integrations; both product fixes after M21-1 | shared `chart.js` lock |
| W2 | full A1 independent-block correction + logout wiring | same cycle | browser proof → fresh independent review | durability/privacy |
| W1 | **fresh GPT b62 mechanism review** | same cycle | accepted mechanism → W5 painted A/B | GPT + accepted v2.1 |
| W3 | M21-1/M21-2 GPT reviews ∥ read-only residual diagnosis | same cycle | residual ACK/harness correction after snapshot review | independent snapshots |
| W6 | combined result triage; historical R1 preserved | same cycle | corrected post-review gate → stable rerun | ACK + browser report |

- **W6 stable combined result:** the corrected W3 core hash stayed stable and hermetic double-runs passed, but acceptance is withheld: malformed-worker reject accounting has one behavioral RED, the W3 browser harness timed out under the W6 runner, and the historical R1 oracle is stale against the fixed bridge. Product-wiring RED rows remain expected until M21-1 clears `chart.js`. Composer is diagnosing these without changing the snapshot under GPT review.
- **b62 correction handoff:** W1's seven-item correction is complete locally and remains non-accepting. Fresh GPT review now owns mechanism acceptance, with special scrutiny on full invalidation identity, rollback/liveness, and correctness above the 4096-point synchronous cap. W5 painted GREEN remains blocked on accepted v2.1 provenance.
- **H-S78 audit:** the current H-S78 known-failing sub-check is confounded by its earlier large drag, which already disables follow; it cannot serve as BL-16 RED evidence and is not an M21-1 regression. Composer is building a separate no-prior-drag real-browser H-S78b gesture-start discriminator. Any product correction remains serialized behind M21-1 and must preserve clicks, tools, touch/pinch, paused replay, and M21-1 drawing-pan behavior.
- **W5 density diagnosis:** fixed-20s shortages are capture-scheduling/jitter dominated rather than oracle rejection; a fresh fixed-30s diagnostic produced 5/5 density-qualified attempts at 15×, 60×, and 100× with the same probe/thresholds. This supports—but does not accept—the v2.1 dwell choice. GPT's binding protocol still requires exactly five retained attempts per speed with no stop-at-three optimization.

### FULL-CAPACITY CYCLE 15 — acceptance wave continues; W4 capacity refilled (2026-07-24)

| Worker | In flight | ETA | Next queued | Blocker / unblock owner |
|---|---|---|---|---|
| W5 | externally sealed v2.1 build and fixed 15-cell baseline | active | fresh GPT acceptance → exact-digest b62 A/B | complete package/provenance |
| W4 | Q5/Q7 correction ∥ favorites/timezone API reviews ∥ H-S6/H-S78b RED prep | active | accepted scoped commits; caller/product hunks after M21-1 | independent evidence + shared lock |
| W2 | complete A1 correction including secure logout | active | browser proof → fresh independent review | durability/privacy |
| W1 | b62 strong independent mechanism review | active | accepted mechanism → W5 painted A/B | GPT + v2.1 |
| W3 | M21-1/M21-2 strong reviews ∥ residual scaffold diagnosis | active | accepted commits or narrow corrections | independent verdicts |
| W6 | ACK/browser/R1 diagnostic handoff | active | fixture/product-disjoint corrections → stable rerun | snapshot verdict |

- **W4 dispatch policy:** Q5/Q7 and favorites are review-only against real product behavior; copied behavioral models cannot confer acceptance. Timezone work is limited to a backward-compatible unsubscribe/AbortSignal API in `timezone-manager.js`; the leak remains explicitly partial until Chart, Replay, Go-To, and news lifecycle owners wire returned handles after shared-file locks clear.
- **Favorites independent verdict:** manager `destroy()` correctly removes its named document pair, but the slice is API-only: production never calls it, drag-handle mousedown stacks, runtime switch transitions strand bindings, and late RAF safety is unproved. A file-local correction is active; full leak acceptance and lifecycle wiring remain blocked on the shared `chart.js` window.
- **Q5/Q7 independent BLOCK:** product gates are structurally plausible, but the behavioral suite copied bridge logic instead of executing it. Q7 also misses `drainPendingReplay` exit and chart/panel/layout teardown. A bridge-only correction now owns real VM/browser integration, timer identity/cleanup, and unload/destroy behavior; no commit is eligible before fresh review.
- **Timezone API handoff (quarantined):** a mirrored `subscribe()`/AbortSignal API is present while all caller files remain unchanged and leak-RED. Fresh GPT review owns API-only acceptance, including malformed signals, duplicate handles, runtime switch transitions, and bounded reentrant notification; no caller or full-leak GREEN is claimed.
- **Favorites correction handoff (quarantined):** manager-local handle identity teardown, kill-period recovery, and generation/toolbar-bound RAF safety are present with a separate lifecycle RED. Fresh GPT review owns API-only acceptance. Full production leak closure remains blocked until `chart.js` destroys a prior manager before replacement and during chart/panel teardown.

### FULL-CAPACITY CYCLE 16 — premium-only reset and impact quarantine (2026-07-24, 15:48)

| Worker | In flight | ETA | Next queued | Blocker / unblock owner |
|---|---|---|---|---|
| W5 | GPT rebuild of new immutable v2.2 package | active | separate GPT acceptance → exact-digest b62 A/B | complete sealed 15-cell result |
| W4 | Fable Q5/Q7 + timezone + favorites corrections ∥ GPT H-S6/H-S78 revalidation | same cycle | fresh GPT reviews; chart lifecycle after unlock | low-grade output quarantine |
| W2 | Fable A1 correction | active | GPT independent review → Q4 | none beyond existing privacy gates |
| W1 | GPT b62 mechanism review | active | W5 exact-digest painted gate | accepted v2.1 |
| W3/W6 | GPT M21 reviews; existing Fable-owned corrections | active | Fable corrections/wiring from verdicts | shared locks |
| Manager | repository-wide GPT low-tier impact audit | same cycle | purge/rebuild/retain by exact strong verdict | none |

- **Historical 15:48 reset — superseded prospectively at 16:15:** interrupted outputs remain quarantined, but new small Grok/Composer delegation resumes under a named Fable/GPT supervisor, written contract, and owner verification. Lower-tier output still cannot independently clear GREEN, acceptance, or commit.
- **DIRECTOR/PO CORRECTION (2026-07-24, 16:15) — the total ban is rescinded; the ORIGINAL delegation model is restored:** the PO never ordered banning Grok/Composer. The sprint's founding rule stands: lower-tier models ARE used for small, simple, well-specified tasks — soak/gate runs, file mirroring, test boilerplate from a signed contract, log triage, evidence collection, mechanical sweeps with exact line anchors — **always under a higher-tier owner's supervision**. What remains forbidden (and always was): low-tier authorship of product edits in risk files, low-tier accepting verdicts/GREENs, and low-tier output entering a commit without the supervising Fable/GPT owner reviewing and signing it. The cycle-16 quarantine of the specific polluted slices stands on its merits (those outputs failed review), but lanes must resume delegating per the original model — the correct lesson from cycle 16 is "supervise and verify delegated work before it counts," not "stop delegating." Dispatch rules going forward: every low-tier dispatch names its supervising owner in the board pulse; the supervisor defines the task contract and acceptance check BEFORE dispatch; delegated output is verified by the supervisor before any use. This restores full-capacity throughput without re-admitting unsupervised low-tier conclusions.
- **Impact assurance:** lower-grade outputs are preserved only for forensic comparison. Dedicated GPT audits now cover the entire affected diff plus W5 v2.1, favorites, timezone, and H-S6/H-S78. Strong reviewers must independently reproduce behavior; prior low-grade conclusions are hypotheses, not inherited facts.
- **Stopped-lane inventory:** v2.1 has a sealed but incomplete 44-file draft with no 15-cell result; H-S78b never completed a browser RED; H-S6 draft RED and the M21 diagnostics are non-accepting; Q5/Q7 contains incomplete product/harness edits and a test seam; timezone/favorites Composer verdicts are ignored. Fable has restarted Q5/Q7 from HEAD/contract, while GPT determines retain/rebuild/discard for every other slice.
- **Strong timezone BLOCK:** GPT reproduced unbounded reentrant notification livelock, malformed-signal half-registration, and invalid RED/GREEN/kill provenance. The API is not commit-eligible. Fable is rebuilding atomic signal setup, bounded/coalesced notification, and an honest immutable pre-fix→current→kill evidence chain; caller lifecycle remains RED and locked.
- **Strong W5 verdict:** v1/v2, probe, and threshold bytes are intact, but v2.1 is `REBUILD-REQUIRED`: it executed the live `Date.now()` server, did not reset attempts, did not enforce served network/runtime pins, rewrote package bytes during sealing, and had a broken digest adapter. Its retained cells are diagnostic only and also fail the 15× control. W5 GPT is building a fresh v2.2 from frozen product/server/data bytes; v2.1 remains untouched quarantine.
- **Strong favorites BLOCK:** Chromium disproved the nominal harness GREEN: kill mode suppressed legacy callbacks, stale RAF completion could clear a newer token, active-drag visual state survived destroy, detached/null rebind leaked, and teardown removed from the wrong document identity. Evidence/manifest replayability was also invalid. Fable is rebuilding the manager-local slice and executable pre-fix/current/kill evidence; full chart lifecycle remains RED.

### FULL-CAPACITY CYCLE 17 — supervised delegation restored (2026-07-24, 16:16)

| Delegated task | Lower-tier scope | Named supervisor | Predeclared acceptance check |
|---|---|---|---|
| D17-W4-Q57 | read-only Q5/Q7 VM/browser gate + raw logs | Fable W4 `9cbe18f7…` | stable start/end hashes, zero writes, raw counts only; supervisor reruns/decides |
| D17-W4-TZ | read-only malformed-signal/reentrancy/census matrix | Fable timezone `7d853ece…` | stable hash, no evidence writes, raw failures only; no verdict |
| D17-W4-FAV | read-only listener/RAF/kill census | Fable favorites `b1c43ab4…` | stable hash, exact callback counts, zero writes; no verdict |
| D17-W2-A1 | read-only existing A1 browser/unit matrix + log triage | Fable W2 `dc455bac…` | product hash stable per phase, no evidence writes, failures mapped only |
| D17-W5-PINS | protected v1/v2/v2.1/probe/threshold hash watch | GPT W5 `d04f5433…` | exact expected hashes, no file mutation, drift report only |
| D17-W3-GATES | read-only M21-1/M21-2/W6 gate execution | GPT M21 `d130f384…` / `0ad623d8…` | bound product hashes, hermetic/no-write run, raw RED/GREEN rows only |
| D17-ROOT-SHADOW | inventory nested `chart/docs/plan3` shadow only | GPT impact audit `b66fb734…` | paths/hashes/origin comparison, zero writes; supervisor decides cleanup |
| D17-M21-POST-CLEAN | read-only M21-1 root/gate confirmation | GPT M21-1 `d130f384…` | no shadow paths, stable hashes, zero writes, raw gate rows only |

- **Delegation boundary:** these workers may run commands and collect raw evidence only. They may not modify product/tests/harnesses, repair failures, reinterpret contracts, issue GREEN/acceptance, or stage commits. Any hash drift makes that phase stale. The named supervisor must independently review and sign before a result is used.
- **W5 provider retry:** the first GPT v2.2 implementation request was rejected by the model provider's prompt classifier before work began. The task was reissued as a strictly local deterministic browser-performance fixture with no external systems, credentials, deployment, or production access. v2.1 remains unchanged quarantine; no evidence or product state was affected by the rejected request.
- **D17-W4-TZ raw result (supervisor pending):** stable-hash delegated run returned 19 pass / 3 fail / 1 skip. Failures are canonical/homepage mirror drift, stale provenance expectations, and the external-change-after-reentrancy-budget contract. Malformed-signal, census, kill, timeout, and DST rows passed on that snapshot. This is not acceptance; Fable supervisor `7d853ece…` must reconcile mirrors/contract and independently rerun before any evidence use.
- **D17-W2-A1 raw result (supervisor pending):** both unit trees ran 31/31, but `order-manager.js` mirrors already differed and the browser probe was skipped because it writes evidence unconditionally. Required save-order/session-switch, runtime-kill-after-externalization, missing/corrupt-ref, payload-validation, and object-URL-cleanup tests are absent. Fable W2 must close these gaps and independently rerun; unit counts alone confer no acceptance.
- **Strong H-S6/H-S78 disposition:** H-S6 is a real clean-HEAD/current owner-I/O race, but the Composer prep mislabels request targets/initiators and has stale pins, so it must be rebuilt by Fable after the `chart.js` lock. H-S78/H-S78b is `NO-PRODUCT-BUG`: 0/1/4 px stays a click with zero viewport movement; 5/6 px commits the pan; Q9 and M21-1 do not alter the boundary. H-S78b draft is canceled quarantine and gets no product handoff.
- **D17-W5-PINS raw result (supervisor pending):** protected v1/v2/v2.1 mirrors, probe, thresholds, and strong anchor hashes were stable; v2.2 used sibling paths only. The known v2 relative threshold path remains unresolved from package cwd. GPT W5 `d04f5433…` must verify before using this watch result.
- **D17-W4-Q57 raw result (supervisor pending):** product mirrors were stable/equal and syntax passed, but test/harness mirrors differ, VM loading fails on `runFourPanelCrosshairBurst` versus exported `runFourPanelBurst`, and the browser harness timed out without reporting. Three temp profiles remained, with no orphan Edge process. Fable W4 must fix, clean, and independently rerun; no behavioral row executed.
- **D17-W3-GATES raw result (supervisor pending):** all product hashes stayed stable. M21-2 scaffold/worker and both real-browser trees ran 51/0; W6 hermetic/IB/CSP rows executed without write. Historical R1 still has 5 stale-expectation failures. M21-1 did not execute because a nested `chart v 1.4/chart/docs/plan3` shadow makes `findRepoRoot` resolve the wrong root. GPT supervisors must classify all rows and own any cleanup; delegated counts are not GREEN.
- **D17-W4-FAV raw result (supervisor pending):** manager mirrors were stable/equal and isolated VM rows improved, including exact kill callback counts, but static tests are stale, homepage path resolution is broken, and the sequential Chromium probe accumulates prior listeners and still shows active-drag/RAF visual residue. Fable supervisor must use isolated browser scenarios, fix paths/contracts, and independently rerun; no API acceptance follows.
- **D17-ROOT-SHADOW cleanup complete:** GPT confirmed both in-repo shadows were ignored wrong-root artifacts with canonical counterparts and no unique authority. The two allowlisted JSON files and their empty parent directories were removed. Canonical `docs/plan3` and outside-repo `C:\Users\user\Desktop\talaria1\docs\plan3` were untouched. Root-walker/writer hardening remains queued under Fable/GPT ownership.
- **Fable timezone correction handoff:** mirrored product is now `877ecce9…`; Fable reports hostile-signal rollback, same-timezone idempotence, and an eight-pass bounded trailing notification contract with honest committed-HEAD RED/current GREEN/kill evidence. The earlier delegated mirror/reentry failures captured a pre-final snapshot. Fresh GPT review owns API-only acceptance; caller leak remains RED and locked.
- **Global low-tier impact verdict:** `LOW-TIER-IMPACT-BLOCK` until writers quiesce and one final stable hash-bound audit reruns. No low-tier writes were attributed to indicator/order/M21 product ownership, but strong A1 mirrors currently differ; Composer W5/timezone/favorites/Q5-Q7 evidence is rejected or stale; and historical W6/H-S artifacts need rebuild/disposition. Current Fable/GPT rewrites continue. The audit's older H-S78 rebuild recommendation is superseded by the later focused GPT `NO-PRODUCT-BUG` verdict.
- **Committed quick-kill assurance:** focused tests for committed Q1/Q2/Q8/Q6 pass, but the global audit requires fresh strong assurance—especially real-browser Q2 pixels and exact kill behavior. A dedicated GPT review is active; this is verification, not a rollback recommendation.
- **Fable favorites correction handoff:** product mirrors are now `b7ac7f42…`; kill uses an isolated legacy path, fix-ON owns RAF tokens/targets, and executable Node/Edge/lifecycle evidence was rebuilt. Fable found the prior `fb7eac…` pre-fix hash unverifiable and pins raw HEAD blob `091e2467…`; fresh GPT review must resolve that provenance dispute and independently verify kill equivalence before API-only acceptance.

## PO STANDING ORDER — API-capacity exhaustion protocol (2026-07-24, 17:25, binding on the Manager)

When premium (Fable/GPT-class) API usage is exhausted, the Manager must **NOT** silently downgrade that work to lower-tier models. The binding sequence is:
1. **PAUSE** the affected premium-owned task(s) exactly where they are, preserving state/contracts for clean resume.
2. **NOTIFY the PO immediately** (same cycle, instant escalation — never batched) that capacity is exhausted, naming which lanes are paused, so the PO can refill the quota.
3. **RESUME from the original contracts** once capacity is restored (as done in cycle 18).
Lower-tier models remain limited to their pre-agreed supervised small-task scope (cycle-17 delegation table rules). Capacity exhaustion is never a reason to expand that scope — premium-owned implementation, root-cause, provenance, and acceptance work waits for the refill. Idle-but-safe delegated prep may continue in parallel where it already complies with the supervision rules.

### FULL-CAPACITY CYCLE 18 — premium capacity restored; strong gates resumed (2026-07-24, 17:12)

| Lane | Strong owner resumed | Binding next result |
|---|---|---|
| W5 | GPT v2.2 + sanitized auth/live-cell owner | auth preflight → measurement 1a; sealed v2.2 before painted A/B |
| W4 | Fable Q5/Q7; GPT timezone/favorites/quick-kill reviews | corrected gates or exact BLOCK |
| W2 | Fable full A1 correction | all ten durability/privacy gates before fresh review |
| W1 | GPT b62 mechanism review | mechanism ACCEPT/BLOCK, separate from painted acceptance |
| W3/W6 | GPT M21-1 and M21-2 reviews | exact scoped ACCEPT/BLOCK; `chart.js` wiring remains locked |

- **Credential handling / I16:** W5 was instructed to consume `TEST_EMAIL` / `TEST_PASSWORD` / `TEST_VPS_URL` only from its inherited shell, never echo or serialize their values, suppress/delete transient raw runner output, scan newly generated repo artifacts for exact-value leakage without printing matches, and keep every QA-created session/order under that account. Login success is not product GREEN. Painted A/B is explicitly a distinct harness cell, not falsely labeled authenticated.
- **D17-M21-POST-CLEAN raw result (supervisor pending):** both nested shadows are ENOENT, canonical board is present, scoped hashes were stable, and no-write M21-1 runs from repo root and arbitrary temp cwd each returned 9 pass / 0 fail / 2 skip. This clears the wrong-root execution blocker only; browser/pixel and evidence-writer skips still require strong review.
- **Capacity policy:** interrupted GPT/Fable tasks were resumed from their original contracts. No Grok fallback conclusion can clear acceptance, GREEN, commit eligibility, or deployment.
- **Fresh timezone verdict — `BLOCK-TIMEZONE-API`:** strong GPT reproduced an observable half-registration during hostile `addEventListener` (listener census 1 and one callback before failed `subscribe` returned), a homepage test root resolving `homepage/homepage/public/chart` with 3 failures, and mixed/mislabeled RED plus incomplete evidence sealing. Passing bounded-notify, census, DST, browser, product-parity, and raw-HEAD hash rows do not outweigh those blockers. Fable owner `7d853ece…` is correcting two-phase opaque signal commit, validated canonical-root discovery, and immutable fully sealed provenance. Caller lifecycle remains separately RED/locked; no commit eligibility.
- **Q5/Q7 Fable correction handoff:** author reports immutable-HEAD RED 7/7, working GREEN 86/86 (14 real-browser rows plus 100-cycle census), kill legacy parity 71/71, and neighbors 47/0; burst-helper naming, mirror drift, 404 root, timeout cleanup, CSP-safe external bootstrap, and manager teardown were addressed. This is `PENDING-GPT-INDEPENDENT-REVIEW`, not GREEN. Fresh GPT owner `a733ee50…` is binding to the 31-file `fa5017e4…` manifest and independently checking actual bridge paths, kill equivalence, browser failure cleanup, CSP/security, root/evidence hygiene, and panel-removal coverage.
- **Fresh M21-2 verdict — `BLOCK-M21-2-SCAFFOLD-CORRECTION`:** direct dual-tree Node/Edge gates pass, real malformed-worker rejection is correct, and the prior browser timeout did not reproduce; however, adversarial current-hash probes showed one posted frame accepting duplicate/invalid/unposted/stale success ACKs (`framesApplied` 1→5) and partial-transfer canvas loss failing to enter the permanent idempotent fatal machine (second transfer constructs again; callback fires twice). The manifest also overstates 63/63 versus generated 61/61. W3 Fable owner `5077b201…` is correcting exact posted-generation ACK correlation and routing canvas-loss through one fatal state. W6 R1 remains stale fail-as-fixed and F4 wording stays W6-owned; product wiring remains separately RED behind M21-1.
- **Fresh M21-1 verdict — `BLOCK-M21-1-CORRECTION`:** the exact follower browser pixel oracle passes at micro-pixel error, but one real source pan still performs two authoritative drawing redraws, an unchanged-geometry `resize()` returns before invalidating a queued deferred callback while teardown state is never wired, and CSS-pan kill still arms snapshot/watchdog lifecycle. Stored RED binds obsolete/non-equal bytes and cannot replay; current RED mode is unexpectedly GREEN. W3 Fable owner `196ea7ee…` is correcting per-pan finish idempotence, real resize/destroy invalidation, exact legacy kill ownership, and a new immutable executable RED package. `chart.js` remains non-commit-eligible and locked; H-S6/H-S78 stay with their existing owners.
- **Fresh favorites verdict — `BLOCK-FAVORITES-API`:** strong Node/Edge probes found a kill-period RAF that remains queued through fix-ON recovery/destroy and later mutates position, plus a replaced-toolbar target that remains `.dragging` with `transition:none` because recovery cleans the originally bound toolbar rather than the actually mutated one. Existing 52/52 and 42/42 suites omit these transitions. Provenance language is also false: `fb7eac…` is a reconstructable Composer intermediate (not the authentic pre-fix Git blob), and whole-method “verbatim exact legacy” is overstated despite callback-body/ordered-log parity. Fable owner `b1c43ab4…` is adding transition-safe legacy RAF/target ownership, browser regressions, and truthful provenance/kill wording. Full `chart.js` favorites lifecycle remains separately RED/locked.
- **Timezone round-2 handoff:** Fable reports two-phase invisible-until-commit signal attach (hostile attach census/hits remain 0), strict four-marker canonical-root resolution from both entrypoints/arbitrary cwd, and single-source RED 7 / GREEN 101 / kill 8 / browser 22 evidence under a closed 16-artifact external seal. Product mirrors are now `3aa25883…`; round-1 `877ecce9…` remains rejected history. Status is still `PENDING-FRESH-GPT-REVIEW`; independent owner `6fbf8ad3…` is re-running opacity/race, root-decoy, bounded-notify, immutable provenance, and seal-closure checks before API-only acceptance. Caller lifecycle remains separately RED/locked.
- **Committed quick-kill strong verdict — Q1/Q2/Q8/Q6 all `BLOCK-REWORK`:** normal optimization paths and kill-parent discrimination mostly pass, but release contracts do not. Q1 lacks a real lifecycle caller for observer teardown; Q2 is not pixel-equivalent to full render and misses visibility/DPR re-anchor; Q8 loses state↔interval consistency when UI rendering throws; Q6 loses teardown ownership on remover/stop errors and has no old-instance lifecycle cleanup. No rollback is performed automatically; commits `f7700e00a`, `5cd010bb8`, and `2f0ce7831` remain under correction hold. Strong file-disjoint Q8 (`9bf346da…`) and Q6 (`b9dd49e0…`) correction lanes are active. Q1/Q2 product work is serialized behind the M21-1 `chart.js` lock; strong test-only RED prep `6f43f292…` is running against immutable commit bytes.
- **Fresh b62 verdict — `BLOCK-B62-MECHANISM-CORRECTION`:** current hash `497ab779…` still partially publishes a mixed TEMA/MassIndex bridge failure, suppresses retries after an 8,528-point budget skip without guaranteeing a fresh paint, passes only 43/52 full-structure worker/main parity cases, mutates generation/memos under kill instead of exact b61, and misses same-shape multichart data replacement while using signed-32-bit generation coercion. Manifest workload arithmetic is also stale (876 claimed versus 1,596 current). W1 Fable owner `ec8d93f2…` is rebuilding whole-paint atomic commit, cap-to-fresh-worker liveness, exact-or-async family parity, zero-side-effect kill, and strict complete identity. The indicator hunk is non-commit-eligible; W5 painted A/B remains forbidden until mechanism and v2.2 are independently accepted and exact b62 ON/OFF digests are pinned.
- **Fresh Q5/Q7 verdict — `BLOCK-Q5-Q7-CORRECTION`:** stable 31-file `fa5017e4…` snapshot is not commit-eligible. Actual product probes show stale Q7 callbacks creating timers after cut/pagehide/unload, out-of-order seeks, non-terminal destroy, runtime-kill divergence from immutable HEAD, no Q5 teardown API, RAF-exception scheduling wedge, and a failed drawing guard leaving stale queued work that sends later. Homepage evidence resolves/writes under the wrong root; browser runs leak harness profiles; runtime identity/seal metadata is incomplete. Fable owner `9cbe18f7…` is correcting seek generations/terminal lifecycle, exact kill shape, Q5 teardown/error isolation, canonical-root sealing, and fail-loud profile cleanup. Manager panel-removal coverage and CSP-safe external bootstrap remain valid supporting rows only.
- **M21-2 R6 handoff:** W3 reports exact posted-generation ACK consumption and a unified permanent canvas-loss fatal machine. New adversarial RED is 61/72 with the intended 11 failures; current is 72/72 both trees; Edge is 54/0; prior R5 count overstatement is marked superseded. Bridge is now `5ec027be…`, worker remains `eeb54666…`. This is still `PENDING-FRESH-GPT-REVIEW`; independent owner `0ad623d8…` is testing >512 ledger eviction, late/error/fatal ACKs, partial-transfer retry census, RED replay and manifest truth. Wiring remains blocked on M21-1.
- **A1 full-release correction handoff:** W2 reports all ten durability/privacy gates implemented: 35/35 unit GREEN+KILL, 9/9 RED guard, 32/32 real Chromium, seven neighbor suites, and restored order-manager mirror parity. Secure logout uses same-origin BroadcastChannel because the shell navigates rather than retains a chart window; bulk delete is refused without a verified durable server ack, leaving owner-locked retention as the residual policy decision. Fresh GPT owner `e2cc3d2e…` is independently reviewing ordering, runtime-kill recovery, corrupt refs/groups/cache/sweep, image/URL safety, transport forgery/timeout, I16/D-030 and evidence truth. Q4 remains locked.
- **Timezone round-2 verdict — `BLOCK-TIMEZONE-API-ROUND2`:** opacity during hostile code is fixed, seal/provenance/root/bounded rows verify, but a reentrant post-attach `aborted` getter can invoke the retained handler, return false, and still commit because `pendingAbort` is copied before the getter and not reread. The 22/22 browser gate also leaks both current profiles, leaves 11 total, silently swallows removal errors, and retains an uncleared timeout (~66s). Fable owner `7d853ece…` is adding sticky pre-commit abort state with a final no-hostile-call check, clear/unref timeout ownership, bounded fail-loud Windows profile cleanup, and exact-prefix stale-profile cleanup before round-3 resealing. API remains non-commit-eligible.
- **A1 review provider retry:** the first fresh review request was rejected by the provider classifier before execution; no file/test/evidence state changed and no verdict exists from that attempt. The same acceptance scope was reissued as a strictly local source/IndexedDB/loopback-browser lifecycle review with no external systems, credentials, installs, or deployment. Independent Fable owner `da77e9ff…` now holds the gate; Q4 remains locked.
- **Favorites correction handoff:** Fable reports both fresh-review defects closed RED-first: kill-period RAFs are ledger-owned/inert after recovery or cancel failure, and recovery follows/restores the toolbar actually mutated across A→B→C replacement. Author evidence now reports Node 76/76 both mirrors, Edge 58/58 with 33-entry A/B parity, corrected immutable-byte provenance, truthful ordered-behavior kill wording, and byte-identical product mirrors; this remains `PENDING-FRESH-GPT-REVIEW`, not self-accepted. Independent GPT owner `f6a3cf2f…` is rerunning the adversarial transitions, provenance reconstruction, RED discrimination, browser/Node counts, mirror/seal integrity and manifest-scoped commit check. The separate `chart.js` lifecycle RED remains locked.
- **Q8 correction handoff:** strong owner reports fix-ON committed mutations now synchronize alert-checker ownership from a `finally` path while switch-OFF remains trace-identical to immutable commit `5cd010bb…`; author evidence reports immutable RED 10/10, current GREEN 59/59, kill RED 14/14, 44-row fault matrix, and 100-cycle zero-leak stress across byte-identical mirrors. This is `PENDING-FRESH-GPT-REVIEW`, not self-accepted. Independent GPT owner `024325d4…` is rerunning exception-preservation, reentrancy, timer ownership, exact kill traces, immutable provenance and manifest truth. The pre-existing combined-test Q1/Q2 root defect remains separately scoped.
- **Fresh M21-2 R6 verdict — `BLOCK-M21-2-R6`:** the ledger became ACK-eligible before `postMessage` succeeded, generation-less error/drop replies could retire the wrong survivor after 512-entry eviction, and partial canvas-loss fatality could be relabeled as main-thread recovery by kill-switch/API paths. The browser report was stale and prior RED source provenance was not replayable. Official Node/Edge rows passing did not outweigh these causal blockers.
- **M21-2 R7 convergence handoff:** W3 bridge and worker-protocol owners corrected the R6 block file-disjointly, then converged under one seal. Author evidence reports post-success ledger admission with staged reentrant replies, exact-generation success/error/drop correlation, permanently sticky canvas-loss fatality, scaffold 77/77, worker-edge 20/20, Edge 55/55 both trees, complete W6 supporting battery, scratch cleanup, and a canonical replay package binding the exact rejected R6 bridge `5ec027be…` (71/77 RED) against current `58415505…` GREEN. The stale browser report was replaced. This is `PENDING-FRESH-GPT-REVIEW`, not accepted; original GPT owner `0ad623d8…` is rerunning all three blockers, replay provenance, browser/W6 gates, seal closure and commit scope. Product wiring remains separately blocked on M21-1/`chart.js`.
- **M21-1 second-correction handoff:** W3 Fable reports all four fresh-GPT findings closed RED-first on sealed/pinned bytes: the source panel's own sync echo armed a watchdog that released mid-drag (finish=2→1 via a per-pan-generation authoritative finish contract), `resize()` now invalidates the queued deferred callback at its prologue before every early return with teardown wired to real `pagehide`/`beforeunload`/`pageshow`, CSS-pan kill is exact legacy at its owning entry points with synchronous ON→OFF sweep and new-pan-only OFF→ON, and a one-command immutable RED (`m21-1-immutable-red-replay.mjs`) executes the pinned HEAD blob `e1c7a2de…` plus the sealed blocked snapshot `6e20965a…` and still REDs 8/8 after the fixes (old stored RED marked DIAGNOSTIC/SUPERSEDED, historical bytes not claimed). Author evidence: node 12/0, browser 14/0 incl. real-pointer F1/F2/F3 rows, GREEN 63/63, kill discrimination RED, follower pixel oracle preserved at 3.2e-6/5.8e-7 px, Q/Q9/M19 neighbor gates 0 fail, H-S6/H-S78 signatures unchanged (RECHECK; the `unload` console violation is `panel-cmd-bridge.js`/W4-Q7 lane). Product mirrors are now `2d351f1c…`; this is `PENDING-FRESH-GPT-REVIEW`, not self-accepted, and `chart.js` remains non-commit-eligible until the fresh verdict.

### FULL-CAPACITY CYCLE 19 — interrupted gates resumed from stable checkpoints (2026-07-24, 20:04)

- **A1 accepted — `ACCEPT-A1-LOCAL-RELEASE`:** independent local review held all 16 manifest hashes stable, reproduced RED 9/9, GREEN+KILL 35/35, Q4 RED 14/14 in both trees, and real Chromium 32/32 with zero findings. The 16-file A1 packet is manifest-scoped commit-ready. Bulk logout deletion remains deliberately refused without a verified durable acknowledgement; owner-keyed 30-day/512/128-MB retention is the accepted D-030/I16-compatible default. Q4 may unlock only after the A1 packet is committed; no commit was performed.
- **Q8 fresh verdict — `BLOCK-Q8-TRANSACTION`:** start/stop faults can mask the original operation exception and leave ownership inconsistent; toggle/update lack the committed-mutation transaction; failed push/splice can restart an unchanged checker; nonempty mutations churn timers; and switch-OFF adds an early observable switch read. The packet also lacked a Q8 manifest and understated the shared homepage-root defect. Strong owner `9bf346da…` resumed from its interrupted RED-first rework and canonical-root gate.
- **Q6 fresh verdict — `BLOCK-Q6-LIFECYCLE`:** retry-safe floating-listener cleanup passes, but switch-OFF exposes fix-only API/fields and altered traces, destroy omits playback/RAF/viewport/timezone ownership, replacement/page teardown leaves old instances live, constructor partial failure leaks, stored RED is stale (19/13 versus fresh 23/14), and no Q6 manifest exists. Strong owner `b9dd49e0…` is rebuilding full fix-ON lifecycle ownership with exact immutable OFF shape and no `chart.js` edit.
- **Favorites pass-3 handoff:** after `BLOCK-FAVORITES-API`, manager-level earliest-baseline ownership now restores stacked A→B/A→B→C targets once and mouseup terminally resolves a target across all bindings. Author evidence reports Node 86/86, Edge 70/70, exact kill/provenance retained, current `chart.js` baseline `2d351f1c…`, and a truthful 13-file source/test/harness commit scope while ignored evidence remains reproducible but non-commit-eligible. Original GPT owner `f6a3cf2f…` resumed fresh acceptance.
- **Timezone round-3 handoff:** Fable reports the sticky pre-commit cancel race closed with an ATTACHING→LIVE→SETTLED machine, Node GREEN 112/112, Edge 24/24, six cleanup modes, cleared timeouts, bounded fail-loud run-owned profile removal, and zero residual among 11 prior harness profiles. The first GPT continuation was classifier-interrupted before a verdict; a newly worded local-only reviewer `2b62ad35…` now holds the 16-artifact API gate. Caller lifecycle remains separate RED.
- **Q5/Q7 correction review resumed:** correction author reports HEAD RED 7/7, GREEN 58/0 with Edge 17/17, KILL 56/0, zero residual profiles/processes/ports and a closed 33-file seal. The interrupted fresh review had already observed unresolved direct-path behavior—seek results after terminal transitions, near-generation exhaustion retaining a timer, Q5 kill exposing fix-only API/listeners, and Q5 destroy listeners still sending. Reviewer `f1a8eaca…` is finalizing the binding verdict from stable hashes before another correction dispatch.
- **M21-1 review resumed:** the interrupted reviewer completed product-path runs but reached a preliminary block over a non-reproducing neighbor claim, a hash-bound diagnostic naming the wrong switch, and ignored/untracked required artifacts. Reviewer `65fea2d4…` is issuing the exact stable-hash verdict; `chart.js`, Q1/Q2 product work and M21 product wiring remain locked.
- **Q1/Q2 RED prep:** immutable Edge signatures remain valid (Q1 lifecycle leak; Q2 pixel differences 12/1268/76; kill 0), but the current phase is correctly `STALE` because M21-1 moved `chart.js` from `5c30…` to `2d35…`. No product work starts until the chart lock clears.
- **W5 continuation:** the configured QA/v2.2 owner resumed after correcting the v2.2 browser-smoke identity and rebuilding. Remaining work is sealed reproducibility plus a sanitized preflight/measurement status. Painted A/B stays blocked by the independent b62 mechanism rejection and missing accepted exact ON/OFF digests regardless of v2.2 readiness.
- **M21-2 accepted — `ACCEPT-M21-2-R7`:** fresh review held all 21 sealed inputs stable and independently cleared successful-post staging/throw discard, 600-post exact correlation, permanent canvas-loss fatality, near-MAX generation, authentic frozen-R6 replay, scaffold/worker/Edge/W6 gates, mirrors, cleanup and report truth. The R7 scaffold/worker packet is manifest-scoped commit-ready. Product wiring remains separately RED and locked on M21-1/`chart.js`; W5 product pixel/performance evidence remains pending. No commit was performed.
- **B62 corrected mechanism handoff:** W1 reports whole-paint atomic commit, coalesced fresh-paint liveness, 52/52 sync-or-safe-fallback structure parity, exact zero-write b61 switch-OFF, safe complete identity, and corrected 2,926/3,728/6,524 workload arithmetic. Product mirrors are `367ef03c…`; focused tests report 89/89 each and broad mechanism neighbors are green apart from preexisting Q1/Q2/Q8 rows reproduced on clean HEAD. This is `PENDING-FRESH-GPT-MECHANISM-REVIEW`, not accepted; independent owner `43716bdc…` holds the mechanism gate. Painted A/B remains forbidden pending mechanism acceptance, v2.2 acceptance and exact ON/OFF digests.
- **Q5/Q7 direct-path rework:** repeated reviewer connection failures do not erase completed stable probes. Those probes reject the current packet because seek results escape after terminal transitions, near-generation exhaustion retains a superseded timer, Q5 switch-OFF exposes fix-only API/listeners, and Q5 destroy leaves two listeners that still send. Fable owner `9cbe18f7…` is correcting these exact paths RED-first and will reseal before a new reviewer. The earlier 58/0 and Edge 17/17 packet is superseded.
- **M21-1 acceptance serialization:** the first reviewer completed core product probes but connection-failed while reporting preliminary packet/provenance blockers. A replacement final review is deferred until the active Q5/Q7 neighbor files stabilize, preventing another knowingly stale neighbor/seal result. `chart.js`, Q1/Q2 and M21 product wiring remain locked.
- **Favorites pass-3 verdict — `BLOCK-FAVORITES-API-P3`:** single-manager stacked A→B/A→B→C, terminal mouseup and RAF rows pass, but two managers sharing one target still capture baselines independently (`all 0.3s` then transient `none`). Recovering the first disrupts the still-live peer, and final teardown can leave `transition:none`; existing isolation rows check ownership census rather than visual state. The report also falsely calls direct evidence writes atomic. Fable owner `b1c43ab4…` is replacing per-manager target state with module-wide per-target leases/refcounts, terminal cross-manager mouseup resolution, teardown-order Edge/Node rows and tested temp+rename evidence writes. Chart lifecycle remains separate RED.
- **Timezone round-3 verdict — `BLOCK-TIMEZONE-API-R3`:** the sticky phase machine and documented Node/Edge/cleanup gates pass, but the final commit still calls mutable `this.listeners.push`. A side-effecting getter can replace that method so cancellation executes during insertion, or insertion succeeds then throws, leaving a live registration after failed subscribe. The sealed evidence also carries two stale deferred caller hashes. Fable owner `7d853ece…` is moving commit to a captured genuine/private store plus primordial insertion with terminal rollback on failure, adding dynamic commit/removal-failure rows, and removing changing caller hashes from the API acceptance seal.

### FULL-CAPACITY CYCLE 20 — W5 packet closed; API-limited lanes resumed (2026-07-24, 20:26)

- **W5 final packet:** `AUTH-PREFLIGHT-BLOCKED` / `MEASUREMENT-1A-BLOCKED` because `TEST_EMAIL` / `TEST_PASSWORD` / `TEST_VPS_URL` were MISSING in the inherited shell (login not attempted). v2.2 package self-validation PASS (15/15 + browser smoke; package digest `abee6b9b…`, seal `b5dfb797…`), but `V2.2-ACCEPTANCE-BLOCKED` because the single exact baseline attempt exited probe status `2` and protocol forbids a replacement run. Protected v1/v2/v2.1 trees unchanged. `PAINTED-AB-BLOCKED` pending b62 mechanism acceptance, v2.2 acceptance, and exact ON/OFF digests. No commit/push/deploy.
- **Capacity resume:** favorites P4, timezone R4, Q5/Q7 R2, Q8 seal finish, Q6 full-lifecycle rework, and B62 mechanism review were interrupted by API-limit/connection failures. All six were requeued from their last stable checkpoints under original strong owners — no Grok acceptance substitute.
- **Capacity exhausted again (20:29):** the six resumed strong owners immediately returned `API usage limit reached` (provider switched them to Grok). Per standing policy they are **PARKED**, not accepted via Grok. Resume from original contracts only after premium capacity is restored. No product/acceptance state changed by the failed relaunch.

### FULL-CAPACITY CYCLE 21 — premium capacity restored (2026-07-24, 20:39)

- Original strong owners resumed from their exact checkpoints: Favorites P4, Timezone R4, Q5/Q7 R2, Q8 final seal, Q6 full lifecycle, and independent B62 mechanism review. Grok fallback output remains non-accepting. Binding next results are exact correction handoffs or fresh `ACCEPT`/`BLOCK` verdicts.
- **Fable policy gate:** the Favorites, Timezone and Q5/Q7 resumes were blocked before execution because Fable requires user acknowledgement of its data-retention policy. No acknowledgement was made on the PO's behalf. To preserve capacity and avoid Grok, fresh GPT strong owners took over the current partial files: Favorites `83fffb60…`, Timezone `5c9470e2…`, Q5/Q7 `0c016b6c…`. Q8/Q6/B62 continue under their existing strong owners.
- **Fresh B62 verdict — `BLOCK-B62-MECHANISM`:** stable `367ef03c…` still publishes observable stats before whole-paint commit, can retain pending fresh ownership after synchronous/paused completion, and its 52/52 fallback assertion accepts `committed || tipMatchesFresh`; stored TEMA(50) and ADX rows mismatch independent fresh endpoints. Full-worker identity omits params/window/dataset generation, so a held period-20 TEMA reply crosses a period-50 edit; MAX_SAFE same-shape replacement memo-collides, and signed-32 coercions remain. Evidence/report counts and workload comments are also stale. GPT correction owner `72885507…` is fixing these exact mechanisms RED-first. The four-file packet is non-commit-eligible; painted A/B remains forbidden.
- **Q8 final correction handoff:** Q8 owner reports products `76c1c0f3…`, replayable RED 38/38, GREEN 109/109, exact kill 22/22, and a nonignored HEAD-bound packet (`f7a12f39…`) under `modules/m20-q8-transaction-packet`. The shared homepage combined-root defect and volatile replay lane are documented without edits. This remains `PENDING-FRESH-GPT-REVIEW`; independent owner `3c9a48e1…` is rechecking original-error preservation, committed-mutation ownership, churn/reentrancy, exact OFF traces, immutable RED and packet closure.
- **Q8 accepted — `ACCEPT-Q8-TRANSACTION`:** fresh review reproduced RED 38/38, GREEN 109/109, KILL 22/22, dedicated 11/0 (+1 skip), and canonical combined 5/0 (+1 skip), with all scoped hashes stable and no lint/syntax failures. Product mirrors are `76c1c0f3…`; nonignored packet manifest is `f7a12f39…`. The Q8 source/test/packet scope is commit-ready. The homepage combined-root defect remains external and separately owned; no commit was performed.
- **W5 fresh-shell retry:** PO reports the three QA settings are now Windows user-level environment variables. A newly launched W5 owner `7022d3f9…` was instructed to import only those named values into its process without printing/serializing them, run the existing sanitized auth preflight, then measurement 1a under the same QA identity with I16-filterable ownership, temp-only raw output, cleanup and exact-value leak checks. v2.2/painted A/B are out of scope for this retry.
- **Q5/Q7 R2 handoff:** GPT takeover reports terminal seek/result suppression, near-generation retirement, exact Q5 OFF shape and listener teardown corrected in scoped files. Current anchors: sync `e7d2e0a2…`, panel `77ccdd6a…`, manager `c53f44fe…`, manifest `66576e6b…`; focused 56/0 (+9 skip), GREEN/Edge 59/0 (+6 skip), kill confirmed, external seal OK. Status remains `PENDING-FRESH-GPT-REVIEW`; independent owner `bc39de18…` is replaying the preserved correction-RED and direct product paths.
- **Favorites P4 handoff:** GPT takeover reports shared-target leases and atomic writers sealed at product `5a93659b…`, Node 102/102 both trees and Edge 80/80. Its only requested neighbor failure is concurrent Timezone R4 (24/2/+1 skip, live hash moved), not a Favorites edit. Favorites remains `PENDING-FRESH-GPT-REVIEW`; independent owner `bc72cd27…` is reviewing P4 on stable Favorites scope while Timezone proceeds separately.
- **Timezone R4 handoff:** GPT takeover reports private WeakMap-backed listener storage and non-dispatchable atomic commit at product `94ae7fa2…`, RED 7/7, GREEN 120/120, kill 8/8, browser 34/34, four cwd/entry permutations, and dynamic removal-failure exit 3 with no profile residue. Accepting source hashes are API-only; callers are deferred metadata. This remains `PENDING-FRESH-GPT-REVIEW`; independent owner `1d5845c7…` is rechecking commit hijack/failure semantics, browser cleanup and seal truth.
- **Favorites accepted — `ACCEPT-FAVORITES-API-P4`:** fresh review reproduced Node GREEN/KILL 102/102 each tree, 48-entry exact A/B, lifecycle RED 11/11, Edge 80/80 and atomic evidence replay with stable anchors. Product mirrors are `5a93659b…`; teardown/lifecycle tests and harness/prefix/SHA files are source-scope commit-ready. Gitignored evidence remains reproducible but non-commit-eligible. Full `chart.js` Favorites lifecycle remains separately RED/locked; no commit was performed.
- **Q5/Q7 R2 verdict — `BLOCK-Q5-Q7-R2`:** direct product/default/Edge/kill rows pass on stable hashes, but the supplied manifest anchor is stale, correction-RED now executes fixed homepage bytes instead of retained rejected R1 bytes, and all sealed docs artifacts are gitignored. The current generated scope hash and on-disk manifest/seal hashes are inconsistent with report claims. GPT owner `0c016b6c…` is repairing provenance only: recover/freeze exact rejected bytes or fail explicitly, build a nonignored replay packet, and close physical manifest/report/seal hashes while keeping dirty `chart.js` external. Product behavior remains provisionally green but non-commit-eligible.
- **Timezone accepted — `ACCEPT-TIMEZONE-API-R4`:** fresh review reproduced mirrored Node 26/0 (+1 skip), RED 7/7, GREEN 120/120, kill 8/8, browser 34/34 and truthful cleanup-failure exit 3 with no port/profile residue. Product mirrors are `94ae7fa2…`; API source/test/docs scope is commit-ready. Caller lifecycle remains deferred/non-accepting; gitignored evidence remains documented but outside normal commit scope. No commit was performed.
- **W5 auth retry result:** all three named settings were PRESENT, but the existing runner exited 2 with sanitized verdict `M21-VY-AUTH-PRODUCT-ERROR` before login/build/session aggregates; login was not established and measurement 1a remained blocked. No QA record was confirmed created, repo bytes stayed unchanged, leak scan was clean, and temp/raw outputs were deleted. W5 `7022d3f9…` is classifying the pre-login stage and may perform one safe retry only if it is a transient/local invocation issue.
- **B62 R3 correction handoff:** GPT owner reports focused B62 mirrors 91/91 at product `bbc17f6d…`, regenerated evidence/hash bindings and clean syntax/lints, but returns `BLOCK-SCOPED-NEIGHBOR-I-F-I-TAIL-DRIFT` because the combined neighbor run has ten I-f/I-tail failures. Independent owner `c713461c…` is testing current B62 ON, exact switch-OFF and immutable b61 to determine whether these are mechanism regressions, stale expectations or concurrent drift. B62 remains non-commit-eligible; painted A/B stays forbidden.
- **Q6 full-lifecycle handoff:** owner reports exact OFF parity and full ON chart/window registry ownership at product `e461cff7…`, immutable RED 23/14, current 37/0, applicable kill 23/0, Q6 78/78, Q9 19/19, replay 27/27 and lifecycle 47/0 (+3 intentional skips), with no `chart.js` edit. Manifest is `171598e8…`; ignored artifacts are disclosed. This remains `PENDING-FRESH-GPT-REVIEW`; independent owner `6c9f72a8…` is rechecking exact OFF surface, fault/retry drains, replacement/page teardown, constructor rollback and packet truth.
- **W5 diagnosed block — `login-page-load/browser-page-bootstrap-hang`:** runner invocation, build-pin path and Chromium launch completed, then hung before structured evidence or login submit. No safe local profile/port/invocation correction was identified, so the permitted retry was not consumed and measurement 1a was not run. Repository bytes were unchanged, exact-value leak scan was clean, and raw/temp data plus child processes were removed. W5 is parked pending external/product bootstrap resolution; no QA session/order was created.
- **B62 R3 rejected — `BLOCK-B62-MECHANISM-R3`:** stable-scope causality is decisive: enabled B62 passes 23/33 I-f/I-tail neighbors, while exact B62 OFF and immutable b61 each pass 33/33. The ten regressions cover endpoint length/order, forming-candle invalidation, busy release, cumulative fallback and missing tail posts. Focused 91/91 is insufficient; R3 is non-commit-eligible. GPT owner `3447236b…` is performing narrow RED-first R4 mechanism correction. Painted A/B remains locked.
- **Q5/Q7 R2 definitive provenance block — `BLOCK-Q5-Q7-R2-PROVENANCE`:** exact rejected R1 bytes for `sync-bridge.js`, `panel-cmd-bridge.js` and `multichart-manager.js` are unrecoverable after workspace, transcript/scratch, terminal, stash, unreachable-blob and reachable path-history searches. No frozen RED packet, manifest or seal was fabricated. Current direct behavior remains provisionally green, but R2 is non-accepting/non-commit-eligible under the existing evidence contract. Dirty mirrored `chart.js` is an unchanged external dependency with no Q5/Q7 matching diff lines.
- **Q6 fresh review — `BLOCK-Q6-LIFECYCLE` on packet truth only:** product mirrors, Q6 78/78, Q9 19/19, replay 27/27, arbitrary-cwd, syntax/lint and scoped hashes are stable/green. Independent lifecycle rerun produced 50 pass / 0 fail / 2 skip (25/0/+1 per tree), contradicting the packet's 47/+3 claim. Owner `b9dd49e0…` is correcting generated report/evidence/manifest counts and command provenance only; product and test bytes are locked.
- **B62 R4 handoff:** narrow fallback-classifier correction reports focused B62 91/91, I-f/I-tail green, I-g2 2/2, no-write/arbitrary-cwd and mirrored product `1c5bc18c…`. Acceptance is withheld because the reported neighbor aggregate is 31 rather than the R3 review's 33. Independent owner `520843e8…` is enumerating exact per-file TAP coverage and rerunning ON/OFF/immutable-b61 before classifying R4. Painted A/B remains locked.
- **B62 R4 review — `BLOCK-B62-R4` on packet/test truth:** current behavior remains green (B62 91/91), but rejected-R3 is hash-only/non-executable, the actual enumerated neighbor inventory is 34/34 rather than 31 or 33, the manifest still binds stale R3 product bytes, and mirrored B62 tests differ by one assertion message. Product `1c5bc18c…` is locked. Owner `3447236b…` is recovering exact hash-verified R3 bytes or failing provenance explicitly, reconciling test mirrors and rebuilding counts/hashes only. Painted A/B remains forbidden.
- **W5 retry reclassified/authorized:** PO independently verified the configured origin and normal-browser login are healthy, narrowing the prior bootstrap hang to the automated harness path. The one parked retry is now being consumed by W5 `7022d3f9…` through temp-only sanitized browser instrumentation and a bounded looser readiness condition; repository source, security/build-pin checks and credential secrecy remain locked. Measurement 1a runs only after successful non-admin/active preflight under the same QA identity.
- **B62 R4 packet repaired:** current product remains locked at mirrored `1c5bc18c…`; exact rejected R3 bytes were recovered from retained reverse-hunk history and hash-verified to `bbc17f6d…`, with executable S0b reproducing all ten causal failures. Mirrored tests are now byte-identical `8b32d51b…`, both trees and arbitrary-cwd pass 92/92, and neighbor inventory is explicitly 21+11+2 = 34/34. Final independent owner `a116dfa8…` is auditing reverse-hunk provenance, RED execution, packet hashes and full commit scope. Painted A/B remains locked.
- **W5 consumed retry — `LOGIN-STAYED-ON-LOGIN`:** temp-only new-headless/bounded-login handling reached a normal login UI and submitted once, proving the prior bootstrap hang was harness-side. The page did not leave login, so no authenticated/active session or I16 ownership confirmation exists and measurement 1a was not run. Build pin matched before submit; repository was unchanged, cleanup completed and leak scan was clean. Retry budget is exhausted; W5 is parked with no further auth attempt authorized.
- **Q6 provenance corrected:** prior 47/+3 arose from the wrong canonical-only command selection. Sealed mirrored lifecycle commands now reproduce 25/0/+1 per tree, aggregate 50/0/+2; Q6/Q9/replay remain 78/19/27 and protected source/test bytes are unchanged. Report `cfa0935f…`, provenance `5d2ee42c…`, manifest `05ff634a…`; artifacts remain ignored. Final independent owner `696e14d8…` is rechecking packet truth before acceptance.
- **B62 accepted — `ACCEPT-B62-R4`:** independent review reproduced both mirrored B62 suites 92/92, arbitrary-cwd 92/92, neighbor inventory 34/34 per tree, exact recovered R3 hash `bbc17f6d…`, current product `1c5bc18c…` and byte-identical tests `8b32d51b…`, with no drift and clean syntax/lints. Commit-ready scope is exactly the two mirrored `chart-indicators-full.js` files plus the two mirrored `m19i-b62-exact-tail-red.test.mjs` files. Unrelated dirty files remain excluded. Painted A/B is still forbidden pending W5 authentication.

- **DIRECTOR RULING (2026-07-24, 23:50) — PO-ASSISTED LOGIN AUTHORIZED for W5 measurement 1a (PO approved).** The automated-submit defect (login button produces no POST/navigation under automation — confirmed automation-hostile form, Lane 4 owns the fix) must not keep blocking measurement 1a. Protocol: W5 launches its instrumented browser **headed (visible window)** against `TEST_VPS_URL`, navigates to `/login/`, then PAUSES and signals the Manager, who tells the PO the window is ready. **The PO performs the login by hand once** (human pointer events; W5 never types/handles credential values in this mode). After navigation leaves `/login/`, W5 verifies the session is the expected non-admin QA identity (`role:user`, active — identity fields only, no secrets logged), confirms the build pin, then proceeds DIRECTLY to the full measurement-1a capture in that authenticated session. Constraints: this mode is for measurement runs only, not acceptance A/B cells (painted A/B still waits on its own gates); all I16 ownership/filterability, temp-only raw output, cleanup, and leak-scan rules bind unchanged; the assisted session must not be reused after the capture completes (logout/close at end). This does not waive Lane 4's obligation to fix the automation-hostile login form — the fix remains required so future soaks/CI cells can authenticate unassisted.
- **DIRECTOR RULING (2026-07-24, 23:40) — W5 auth: new bounded DIAGNOSTIC budget authorized.** The consumed retry produced decisive value (hang reclassified harness-side; login UI reached; single submit executed) and the PO independently verified server health and manual login with the same account, and the Director verified the same credentials succeed against `POST /api/auth/login` (HTTP 200, non-admin, active) via direct API call. The remaining defect is therefore in the harness login *interaction*, not credentials or server. W5 `7022d3f9…` is granted **2 instrumented attempts**, diagnostic-first: (1) capture the HTTP status + sanitized error body of the login POST triggered by the harness submit (values never logged); (2) verify the typed field values actually registered in the React controlled inputs before submit (length-only assertion, not values) — the known failure mode is automation typing outrunning controlled-input state; use per-keystroke delay and input-event dispatch; (3) confirm the auth cookie is accepted/persisted in the automated profile (httpOnly cookie presence flag only). If attempt 1's instrumentation identifies the cause, attempt 2 applies the targeted correction and, on successful preflight, proceeds DIRECTLY to measurement 1a in the same session. Credential secrecy, I16 ownership, temp-only output, cleanup, and leak-scan rules bind unchanged. If both attempts fail, W5 parks with the captured diagnostics and the login-interaction defect escalates as a product-adjacent issue (possible automation-hostile login form) for Lane 4 ownership.
- **Q6 accepted — `ACCEPT-Q6-LIFECYCLE`:** final review reproduced corrected lifecycle 25/0/+1 per tree (50/0/+2 aggregate), Q6 78/78, Q9 19/19 and replay 27/27 with stable mirrored product `e461cff7…`, binding `20dc8e49…`, clean arbitrary-cwd/no-write/syntax/lint and no drift. The old 47/+3 selection is retained only as historical/non-accepting. Eight Q6 source/test files are commit-ready; ignored report/evidence/provenance/manifest remain outside normal commit scope.
- **D-034 bundle assembly started:** read-only owner `d8a83def…` is inventorying accepted A1, M21-2 R7, Q8, Favorites R4, Timezone R4, B62 R4 and Q6 into the first integration checkpoint. It is validating acceptance hashes, exact stageable/ignored scope, mirror parity, cross-lane neighbors, overlap/drift and deterministic staging order. `chart.js`/wiring, Q5/Q7, M21-1, Q1/Q2, Q4, W5 and painted A/B remain excluded. No commit or deploy is authorized during assembly.
- **W5 two-attempt verdict — `AUTOMATION-HOSTILE-LOGIN-FORM`:** both Director-authorized attempts are consumed. Attempt 1 isolated failed controlled registration; attempt 2 achieved matching nonempty controlled state before submit, yet both attempts produced no login POST, redirect, navigation or network failure and no auth cookie. No authenticated session/I16 ownership exists; measurement 1a was not run. Cleanup and leak scan passed with repository unchanged. W5 is parked. Lane 4 owner `a7f573fb…` is now reproducing and correcting the form submission path hermetically with mocked network; no further live auth attempt is authorized before independent acceptance and deployment.
- **D-034 initial assembly — `D034-BUNDLE-BLOCKED`, scope decision resolved:** all selected lane gates and 58/58 syntax checks passed with stable accepted product hashes, but exact A1/Q8 manifests require four support tests previously covered by broad Q4 and Q1/Q2 exclusions. PO authorizes those four test files only; Q4/Q1/Q2 product changes remain excluded. Owner `d8a83def…` is finalizing the exact 77-file bundle, reconciling stale ignored manifests, rerunning combined gates and producing explicit-path staging order. No staging, commit or deploy yet.
- **Lane 4 login-form correction handoff:** local source owner identified uncontrolled DOM/FormData submission plus render-state request gating, and changed only canonical homepage auth source, a typed deterministic sign-in helper and hermetic regression test. Focused RED was 5/1; current and arbitrary-cwd are 6/6 with no-install TypeScript clean. No live auth or external access occurred. Independent owner `7f3ab16a…` is auditing runtime/helper parity, controlled-input/autofill/IME semantics, immediate duplicate locking, security/accessibility and deployment scope. W5 remains parked until acceptance and deployment.
- **D-034 final preflight — `D034-BUNDLE-BLOCKED` on one support test:** exact 77-file scope is reconciled and hash-stable; all accepted product/lane gates pass, including 64/64 syntax. The waived homepage `m20-q1-q2-q8-idle-drains.test.mjs` alone fails 1/4/+1 because it resolves `homepage/homepage/public/chart/...`; canonical support is green. Owner `a692578d…` is correcting repository-root resolution in the two support entrypoints only, with product/Q1/Q2/Q8 implementation bytes locked. No staging, commit or deploy.
- **Director 23:50 PO-assisted W5 session active:** measurement-only headed browser is opening at the configured login page for the ready PO to authenticate manually. W5 `7022d3f9…` will not type or submit credentials; after manual login it verifies expected QA identity, non-admin/active state, session persistence and build pin in memory, then runs full measurement 1a in the same session. Painted A/B remains gated and Lane 4 still owes the independently accepted/deployed automation fix.
- **Lane 4 accepted — `ACCEPT-LANE4-LOGIN-FORM`:** independent review found no blockers across the exact four-file homepage auth scope; focused and arbitrary-cwd tests pass 6/6, extra hermetic helper matrix passes 10 assertions, no-install TypeScript and syntax pass, diagnostics are clean and hashes are stable. Noninteractive lint remains unavailable because the project prompts for first-time ESLint setup; no config was created. The four files are scoped commit-ready, but automated live auth remains forbidden until an accepted build with these hashes is deployed.
- **D-034 support-root correction:** both authorized combined support entrypoints now resolve canonical repository root safely and are byte-identical `4f2d5985…`; each passes 5/0/+1 from repo root and arbitrary cwd with product hashes unchanged. The separately red homepage Q2 countdown oracle has the same wrong-root defect but is explicitly outside the 77-file bundle and remains deferred with Q1/Q2. Final owner `d8a83def…` is rerunning the exact bundle gates against updated support hashes; no scope expansion, staging or deploy.
- **W5 PO-assisted result — auth pass, 1a awaiting data:** headed manual login succeeded and verified expected QA identity, non-admin/active state, same-context persistence and build pin; password was not read and I16 ownership is confirmed. Measurement 1a entered the same session but found no product chart, replay, bars or sink, so produced zero samples with `READY-AWAITING-SESSION-DATA`. Cleanup/leak scan passed and repo was unchanged. W5 `7022d3f9…` is now performing local/no-auth source diagnosis to identify the exact QA-owned session/replay readiness flow before any further headed authorization.
- **W5 1a readiness root cause/corrected run:** local source confirms the wrapper navigated directly to chart without `sessionId`, `fileId` or preloaded backtesting state, so product correctly redirected before chart/replay/sink readiness. Correct flow is manual QA login, Standard Backtesting setup, select one available QA-usable dataset, create/load a QA-owned personal session, then verify chart/replay/bars/sink and sample. A corrected headed session is active now; PO manual login is required again, after which W5 performs the bounded QA-owned setup and full 1a capture. No painted A/B.
- **D-034 first bundle ready — `D034-BUNDLE-READY`:** exact stageable scope is 77 files: A1 12, Q8 12, Favorites 13, Timezone 10, B62 4, Q6 8 and M21-2 18. Final read-only preflight ran 26 Node entrypoints / 572 tests: 560 pass, 0 fail, 12 intentional skips; 64 syntax checks and scoped lints are clean, paired mirrors match and no files drifted. `chart.js`/wiring, W5/painted A/B, Q5/Q7, M21-1, Q1/Q2/Q4 product work, ignored evidence and unrelated dirty files remain excluded. Bundle awaits explicit-path staging, commit and test-deploy authorization; none was performed.
- **W5 corrected headed run — `SESSION-DATA-BLOCKED`:** PO-assisted auth again passed identity/non-admin/active/session/build-pin checks. Files are visible, but none exposes metadata-backed usable bounds for deterministic selection, so no QA session was created and chart/replay/bars/sink were not reached; measurement 1a did not sample. Cleanup/leak scan passed and repo stayed unchanged. Authentication is closed as a blocker; W5 now requires a supported QA-owned dataset seed/backfill. Local owner `faf2d398…` is deriving the minimum schema, safe workflow and I16 rollback contract without touching the test origin.
- **W5 existing-session path authorized:** PO confirms several backtesting sessions already exist and authorizes selecting one, opening it and pressing Play. This supersedes the synthetic-dataset plan; no admin seed/upload is needed. A fresh headed session is active for manual QA login, then W5 `7022d3f9…` filters to an owner-matching QA session only, verifies chart/replay/bars/sink, starts playback once and runs full measurement 1a. No customer session, order creation, painted A/B or dataset mutation is permitted.
- **W5 existing-session run — `NO-QA-SESSION` under strict row proof:** auth/build checks passed, but the wrapper refused to select because returned rows did not expose an explicit owner match; no session/customer data was opened, Play was not pressed and cleanup/leak scan passed. Since the sessions endpoint may already be server-scoped to the authenticated non-admin user while omitting owner fields, W5 `7022d3f9…` is verifying that backend ownership contract locally before another headed attempt. I16 is not being weakened.
- **W5 ownership contract verified:** local backend audit proves non-admin `/api/sessions` filters `TradingSession.user_id == authenticated user.id`; per-session read/state endpoints re-enforce the same predicate, and no shared/global sessions are exposed by this path. Therefore rows returned in the verified QA context are QA-owned even though public rows omit owner identity. A fresh headed owner-scoped run is active to select an eligible personal/backtest row, open it, press Play and capture 1a. PO manual login is required again.
- **W5 measurement 1a passed — `PASS`:** PO manually prepared an owner-scoped QA session with visible candles; W5 verified identity/non-admin/active/session/build pin, chart, replay, 2,679 ready bars and sink, then pressed Play and captured 8,000 ms. Result: 2,184 sample bars and replay index delta +495, with no orders/customer identity and I16 ownership/filterability true. Cleanup/leak scan passed and repo was unchanged. Indicator tip stroke count remained zero/indicator tip unobserved, so painted A/B is not implicitly unlocked and still requires its separate gates.
- **DIRECTOR AUTHORIZATION (2026-07-25, 00:40) — FIRST BUNDLE: STAGE, COMMIT, TEST-DEPLOY.** With `D034-BUNDLE-READY` (77 files, 560/560 non-skip pass, hash-stable) and measurement-1a baseline captured, the Director authorizes, per the standing landing order and checkpoint cadence: (1) explicit-path staging of exactly the 77-file manifest — hunk-checked, nothing outside it, dirty files excluded; (2) commit + tag with D-034 provenance (SHA, build ID, image digests, manifest hash recorded on this board); (3) deploy to the TEST environment only, with the exact-digest runtime tripwire verified on the deployed surface, including inside a panel iframe; (4) include the four accepted Lane 4 login-form files in this same bundle if the manifest reconciles cleanly, else next checkpoint — after deploy, Lane 4's automation-hostile-login fix becomes live and W5's unassisted auth can be re-validated; (5) immediately after tripwire-green: re-run the same measurement-1a scenario on the new build (before/after row), then notify the PO for the feel-test (4-panel, several indicators, order running, 60×/100× replay — indicator endpoint + task-manager memory curve). Production deploy remains forbidden pending PO validation + public/direct parity, per standing rules.
- **W5 manual indicator follow-up — `manual-tema-readiness-timeout`:** manual login was detected, but the bounded session never observed TEMA period 20, chart/replay/bars/candles and advancing playback as one verifiable tuple, so no indicator-tip capture ran. Cleanup/leak scan passed and repo was unchanged. Painted A/B remains correctly gated: v2.2 digest, exact ON/OFF, build-pin/provenance and exact-session gates are all non-green. Do not spend another headed retry until those non-session gates are prepared.

- **D-034 checkpoint provenance — `CKPT019-TEST-BLOCKED` / TEST deploy only:** explicit checkpoint scope was 82 paths: D-034 first bundle 77 files (A1 12, Q8 12, Favorites 13, Timezone 10, B62 4, Q6 8, M21-2 18), accepted Lane 4 login-form scope 4 files, and the pre-commit board entry. Lane 4 anchors matched acceptance (`auth-fuse.tsx` `68b926eb…`, helper `cbc40b5f…`, declaration `316f7fac…`, test `be8ccd2c…`) and focused gates were 6/6 plus no-install TypeScript and syntax clean. D-034 final pre-commit lane gates were 27 Node entrypoints / 578 tests / 566 pass / 0 fail / 12 skip, plus 66 JS/MJS syntax checks, no-install TypeScript and clean scoped lints; A1 whitespace-only cleanup in both `order-manager.js` mirrors removed trailing spaces at the accepted A1 restore hunk and reran A1 RED/GREEN/Q4 support green. Updated support anchors: Q8 combined support tests `4f2d5985…` both trees; A1/Q4 support tests `6989ff31…` both trees. External/excluded: dirty `chart.js`/wiring remains out of scope with accepted external hash `2d351f1c…`; Q5/Q7, M21-1, Q1/Q2 product, Q4 product, W5/painted A/B, ignored evidence, deleted docs, DashboardShell and unrelated dirty files remain excluded. Immutable build source is checkpoint commit `f38333b95617fb6600c5fe9ddbfe710822d8bcbb` only; board-only commits `26dd2ead…`, `d4598dd…` and this record are documentation and must not enter image source labels. Deploy branch: `d034-test` pushed to origin without force. TEST deployment target category: TEST only, production forbidden. CKPT-019 procedure recovered: source tag → strict Docker builds with `CHECKPOINT_BUILD=1`, `CHART_BUILD_ID`, `SOURCE_COMMIT_SHA` → digest-pinned chart/homepage images → provenance manifest + uniformity proof → `deploy.sh` pull/no-build deploy → exact running digest + direct/public/panel-iframe runtime probe. Deployment stopped before mutation because the local Docker daemon is unreachable, required TEST origins are absent, and no TEST VPS/SSH/registry handles are present in this shell. Build/artifact/image digest: unavailable. Manifest/tripwire/smoke: not run. Measurement-1a on new build: not run. PO's premature feel-test observed the old build and expected lag; it carries no D-034 signal. PO feel-test remains pending after a future tripwire-green NEW build.
- **D-034 b62 CKPT attempt — `D034-CKPT019-TEST-BLOCKED` before TEST mutation:** Docker and registry auth became available, so source tag `d034-20260725b62-source` was pushed at `f38333b95617fb6600c5fe9ddbfe710822d8bcbb`; strict linux/amd64 images were built with `CHECKPOINT_BUILD=1`, `CHART_BUILD_ID=20260725b62` and source label `f38333b…`, then pushed as immutable non-latest tags. Digest pins resolved to chart `45aa74ac1aab…` and homepage `bf37cefa7027…`; local image preflight was GREEN with strict labels and 29/29 in-image layout checks. A schema-valid CKPT-020 manifest was generated locally with hash `3a5674930ff8…`, rollback pinned to accepted b61 (`469778e3…`, chart `4b05eb06de35…`, homepage `24ebce3b74e2…`), but repository manifest preflight was RED before VPS mutation: clean source/tag evidence passed, while uniformity proof `956805afa1e8…` was not green because exact source `f38333b…` still carries b61 runtime/build markers and the D-034 module tree has canonical/homepage mirror deltas (`m20-a-favorites-harness/*`, `m20-q8-transaction-packet/*`, and `m20-q6-replay-lifecycle-binding.test.mjs`). No TEST VPS fetch/checkout, manifest transfer, `deploy.sh`, runtime tripwire, smoke, rollback or new-build measurement ran. The prior old-build feel result remains a non-signal; PO feel-test remains pending for a future tripwire-green NEW build.
- **D-034 b63 CKPT attempt — `D034-B63-TEST-BLOCKED` before TEST mutation:** source correction branch `d034-b63-source` and tag `d034-20260725b63-source` were pushed at `0048865cf0b58a9c4bc552e56822c914089fae52`; accepted extant-scope hash authority is `0bc1b8c818b98510d3f36af98c3f787705feffa60f2e03ab4edabce5c106b202`, with 43 status entries represented in commit as 42 changed paths because Git records the old/new Q8 fixture as a 100% rename. Source/proof preflight is GREEN: build `20260725b63`, manifest `3fd68b804d93…`, proof `1bbf6ae958c3…`, 33/33 source uniformity checks. Strict linux/amd64 chart/homepage images were built from the tag with source label `0048865c…`, pushed as unique non-latest b63 tags, and resolved to chart digest `714ca84fed3b…` and homepage digest `83b423fae6ba…`; image preflight is GREEN with strict labels and 29/29 checks. Rollback remains accepted b61 (`469778e3…`, chart `4b05eb06de35…`, homepage `24ebce3b74e2…`). Deployment stopped before mutation because no configured TEST SSH alias/env handle exists and sanitized noninteractive SSH probes to the authorized TEST host timed out on port 22; no VPS checkout, manifest transfer, `deploy.sh`, runtime tripwire, smoke, rollback or new-build measurement ran. Note: the source diff contains intentional accepted fixture/blob whitespace that makes `git diff --check` report trailing whitespace in mirrored evidence fixtures; those bytes were preserved to keep packet hashes stable. Prior old-build feel remains a non-signal; PO feel-test remains pending after a future tripwire-green NEW build.
