# DISPATCH — CONF-01 re-aim, all four managers

**Date:** 2026-07-30 14:30
**Authority:** `RULING-EVERY-MULTICHART-OPTIMISATION-IS-GATED-ON-SAME-PAIR-20260730-1430.md`
**Applies:** immediately, supersedes conflicting items in each manager's current queue

Common preamble for all four (`DISP-01`: this is the one pending Director message):

> The PO has ruled as product fact that users will run multichart with **different
> symbols and different timeframes**. Every four-panel performance measurement this
> campaign has taken was same-pair. Sixteen `_multichartSamePairAsHost` guards in
> `chart.js` mean the shipping configuration has **zero** optimisation coverage.
>
> Two new binding rules: **CONF-01** — the reference configuration for all performance
> measurement is four panels / four symbols / four timeframes / indicators loaded /
> orders open; same-pair carries no acceptance weight. **DUR-01** — a performance
> acceptance is a slope over a duration, not a reading at an instant.
>
> Read the ruling before acting. Work in your own worktree (`WORK-01`). Parallel
> subagents are the default (`PAR-01`), tiered by task (cheap for mechanical, mid for
> analysis, top only for money-path review).

---

## MANAGER A — own the 5.6x

**Standing fact.** Four different symbols, playing: TradeZella holds 104 MB live JS,
we hold 586 MB. Their heap *falls* when playing (133 → 104); ours rises 247 → 586.
Their script cache is twice ours. The gap is not code size, assets, or documents. It
is how we hold and move bar data per panel.

**Mechanism, already read for you.** With differing symbols, all sixteen same-pair
guards return false. Each panel independently retains a full 1-minute base series in
`_panelFullRawData`, a resampled display array, and a second copy in
`replay.fullRawData`. `_mcRawDataCopyLimit()` returns 200,000 (`chart.js:3530`) — for
any realistic range that is not a cap, it is the absence of one.

**Landing A1 — bound the base series (start here).**
A panel displaying 1h retains the entire 1m series as its resample source. Bound
`_panelFullRawData` to a residency window sized from the visible range plus replay
lookahead plus a stated margin, paging outward on demand. Flag
`__TALARIA_DISABLE_MC_BASE_SERIES_RESIDENCY_V1`, truthy-disables, `FLAG-01`
(test the ABSENT property), `FLAG-02` (flippable without reload), `FLAG-03` (verify
OFF against a working-product assertion, not merely "feature inactive").

Your own residency note said the columnar death certificate measured the wrong array.
`_panelFullRawData` is the right array. State plainly whether A1 removes anything
beyond viewport windowing, and if it does not, say so and move to A2.

**Landing A2 — compact the base series.**
One JS object per bar for a multi-year 1m series is the mass. Parallel typed arrays
for t/o/h/l/c/v behind an accessor boundary, so call sites are unchanged. Separate
flag from A1.

**Non-negotiable on both.** This is price data. Oracle first: a differential parity
oracle over the shipping configuration proving bar-for-bar identity against the
current path — numeric equality on o/h/l/c/v/t, and painted parity. `GATE-01`: show
the gate RED on a faithful reversal before you trust it GREEN. `SAFE-01`: the residency
check runs before allocation, not after.

**Order.** A1 oracle → A1 fix → measure → A2 oracle → A2 fix → measure. Do not batch.
If A1 alone closes most of the gap, tell me and stop.

**Also yours, unchanged but re-baselined to CONF-01:** legend-hover pointer sweep
(5 direct renders at 11.29ms evading the coalescer), `_reseedReplayFullRawFromLoadedData`
residual, GPU-process CPU at 51% vs their 17%.

**Withdrawn from your queue:** the 35-site `setProperty` flag. C's W92 census proved
zero of those sites fire at runtime; the writer is per-frame SVG add/remove via d3 plus
`_syncAdaptivePriceAxisMargin`. Do not build a no-op.

---

## MANAGER B — the unoptimised network path

**Standing fact.** With differing symbols, same-pair replay catch-up is off
(`chart.js:8030`). Each panel issues its own `/bars` requests while playing. Nobody has
ever measured what that means.

**B1.** Four panels, four symbols, four timeframes, thirty seconds of play. Report the
exact request count, total bytes, concurrency, and time-to-first-paint per panel. State
whether requests are coalesced, deduplicated, or serialised — and whether they queue
behind the window claim.

**B2 (P0, unchanged).** The window-claim hang. `POST /api/chart/windows/claim` and
`/release` never returning, 240s in flight, starving HTTP/1.1 connections browser-wide.
Reproduce, name the route, add timeouts that fail loudly, extend the gate.
`9fc8763f0` does not cover this route. This is a canary blocker on its own.

**B3.** Nginx buffering 100k-candle responses to disk. Under CONF-01 that is four
simultaneous large responses, not one.

**B4.** Served-tree hygiene: exclude the harness `node_modules` (puppeteer,
chromium-bidi), the 1,120 `.map` files, and `frozen/`. Their script cache is 64 MB to
our 32 MB and they are still faster, so this is hygiene and deploy size, not
performance. Low tier, no top-tier review needed.

Image assets: the logo resize landed and the image cache is now 5.6 MB against their
0.17 MB. Good enough. Stop there.

---

## MANAGER C — rebuild every instrument to CONF-01 and DUR-01

**Your instruments have been measuring the wrong configuration.** Not your fault — I
specified them. Re-aim.

**C1 (highest priority).** Re-baseline every gauge under CONF-01: four panels, four
symbols, four timeframes, indicators loaded, orders open. Renderer / GPU / browser CPU
separately, live JS after forced collection, tab footprint, node count, documents. This
becomes the only baseline that counts. Publish it as the reference row.

**C2 — the duration gate, replacing the single-pair soak as freeze gate.**
Two hours minimum, CONF-01 configuration, playback running, orders placed
periodically, indicators on. Sample memory, CPU and frame budget on a fixed cadence
and **fit a slope**. `DUR-01`: report the trend and its confidence, not the endpoints.
The question is not "did it crash" but "is it flat."

This is the gate that answers the PO's original crash reports, and it is the one I
never built. Give it teeth: `GATE-01`, show it RED against a known-growing input first.

**C3.** Grade A's A1 and A2 landings by flag A/B under CONF-01 — allocation rate,
renderer CPU, live heap, and resident footprint. The clone-cut trade-off question
(allocation traded for residency) is still open and CONF-01 is where to settle it.

**C4.** Break the 107% renderer and 51% GPU-process CPU into main thread / raster /
compositor under CONF-01. Their GPU process runs at 17%; 51% wants a name.

**Retire:** `SINGLE-PAIR-SOAK-V1` as a freeze gate — keep it running as a
diagnostic, but it is not the bar. The 62-invalidations-per-second census is closed and
accepted; good work, it saved A from shipping a no-op.

---

## MANAGER D — correctness in the shipping configuration

**D1.** Your gate audit found 13 of 51 "fixed" rows gate-vacuous. That stands, and
extend it: **any gate that exercises a single panel or same-pair multichart is
gate-vacuous for the shipping configuration.** Re-audit against CONF-01 and reopen
what does not survive. Report the honest fixed count.

**D2 (money path, top-tier review).** Rayan #2 (vanished order) and Rayan #8
(self-opening sell order), gate-first. Under CONF-01 — four symbols is exactly where
order/panel identity confusion would surface.

**D3.** TAL-01941 non-triggering SL/TP: randomised soak. Fold it into C's C2 duration
run rather than building a second long-running harness — coordinate with C.

**D4.** M24 order-ID instability: the PO saw trade IDs renumber across refresh
(#5 → #942) with your fix present in b103. Either the gate misses restore-time
renumbering or the fix is incomplete. Determine which, with a RED test.

**D5.** Assemble the 26 PO-eyes rows into the five scripts, and re-stage each script
to open four panels on four symbols and four timeframes. The PO's eyes are our only
instrument for smoothness and they should be pointed at the shipping configuration.

**Answered from the PO doc:** TAL-01891 (8 GB) was a report from ~10 days ago, not a
current measurement. It is a question of whether the path still exists, not an alarm —
C's C2 duration run answers it. TAL-01850 (keyboard shortcuts) is in canary scope;
report an owner.
