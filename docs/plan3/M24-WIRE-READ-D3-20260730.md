# D3 — M24 WIRE READ (MEAS-01)

Date: 2026-07-30  
Checkout: `C:\Users\user\Desktop\talaria1\manager-d-trade` @ `manager-d/trade-correctness`  
Reader tip at write: `9e59376e9` (train tip moved during session; M24 product SHA below is stable)

## Outcome

**A — fix is NOT deployed on the stamp the PO measured (routing / train timing failure).**

The PO `#5 → #942` observation on **b103** cannot be a gate gap or an incomplete fix on that stamp: the display-ID stability commit is absent from the b103 tree.

## Stamp / deploy how-to (this repo)

| Source | What it records |
|---|---|
| Served shell | `homepage/public/chart/dist-v9/index.html` sets `window.__TALARIA_CHART_BUILD_ID='…'`; corner badge + console `[Talaria] chart build <id>` (`docs/plan3/evidence/B-M4/release/PO-BUILD-ATTRIBUTION.md`) |
| MEAS-01 canary tool | `docs/plan3/evidence/B-M4/release/canary-meas01-stamp.sh` — HTTP GET `$BASE/chart/chart.js` + shell `/chart/dist-v9/index.html`; reports `served` / `pin` / `shell_http` |
| Module load path | dist-v9 loads `/chart/modules/order-manager.js?v=<stamp>` (not a bundled OM inside chart.js) |
| Local D tree stamp (not live) | this checkout’s dist-v9 still says `20260728b82` — repo artifact, not canary |

## M24 display-ID stability commit(s)

| Item | Value |
|---|---|
| Product commit | `2cc9493992fbc9dfd77be82b3fe3f6769b8a7982` |
| Date | 2026-07-30 11:59:54 +0100 |
| Subject | `fix(orders): ship M24 restore ID, visuals, and pending-clear packets` |
| Flag | `__TALARIA_DISABLE_M24_DISPLAY_ID_STABILITY_V1` (default ON) |
| Product path | `_resolveJournalDisplayTradeId` / `_resolveJournalDisplayTradeIdValue` in both OM mirrors |
| Gate | `chart v 1.4/chart/modules/m24-order-id-restore-stability.test.mjs` (+ homepage mirror) |
| Ancestor of D tip | yes (`git merge-base --is-ancestor 2cc949399 HEAD` → 0) |

Earlier M24 ledger commits (`b21d236d3`, `f1ddb2e64`, `1be37f1c7`, …) are allocator/reconcile/prune — not the display-ID restore stability path.

## b103 contents vs M24 SHA

From Manager B journal `B-0191` (`docs/plan3/journal/MANAGER-B.md` in `manager-b-plan3`):

| Field | Value |
|---|---|
| Stamp PO should read | `20260729b103` |
| Recorded tip SHA | `153c835e249b159d65b7a551f4045bd97239c459` |
| Tip date | 2026-07-30 00:05:25 +0100 |
| Tip subject | `fix(multichart): cut five panel-realm retainers on removeChart behind kill-switches` |

Measurements against that SHA in this git object store:

| Check | Result |
|---|---|
| `2cc949399` ancestor of `153c835e2`? | **no** (exit 1) |
| `_resolveJournalDisplayTradeId` / `DISPLAY_ID_STABILITY` in b103 `order-manager.js` | **0 matches** |
| `m24-order-id-restore-stability.test.mjs` at b103 | **absent** (`exists on disk, but not in '153c835e2'`) |
| Same symbols at D tip | **present** |

Chronology: b103 tip **00:05**; M24 display fix **11:59** same day — ~12h after the stamp cut. The PO renumber on b103 therefore predates the fix commit.

## Live / currently served stamp (MEAS-01)

Local `127.0.0.1:3000` / `:3001`: **unreachable** from this workstation.

Remote canary (B’s host, same URL family as B-0203 MEAS-01):

| Probe | Result |
|---|---|
| `http://31.97.192.82:3000/chart/dist-v9/index.html` | HTTP 200, `window.__TALARIA_CHART_BUILD_ID='20260730b113'` |
| `http://31.97.192.82:3000/chart/modules/order-manager.js` | HTTP 200, 2 475 390 bytes; `_resolveJournalDisplayTradeId` = **true**; `__TALARIA_DISABLE_M24_DISPLAY_ID_STABILITY_V1` = **true** |

B journal corroboration:

- `B-0202` ship note: b112 merged D’s lane; “M24 verified on the wire as `_resolveJournalDisplayTradeId`”
- `B-0203` / b113 wire check: “D’s `_resolveJournalDisplayTradeId` ×7”
- Manager-b tip `d4d881f2d` contains `2cc949399` as ancestor

## What is still unknown

- No browser DevTools session was opened; corner-badge human read was not repeated beyond HTTP body parse of `__TALARIA_CHART_BUILD_ID`.
- Whether a **fresh** PO Script 1 on **b113** still renumbers is out of scope for this wire-read (would discriminate B vs C on a stamp that actually carries the fix). The b103 failure is already explained by **A**.

## Verdict mapping

| Outcome | Fits b103 PO `#5→#942`? | Evidence |
|---|---|---|
| **A** fix NOT deployed | **YES** | b103 SHA `153c835e2` lacks M24 display commit/symbols; fix landed later as `2cc949399` |
| B fix deployed + gate gap | no | would require fix symbols on b103; they are absent |
| C fix incomplete | no | cannot judge incompleteness on a stamp that never had the fix |

Present-tense wire (b113) **does** carry the fix symbols; that is a later ship (b112+), not a rewrite of the b103 PO measurement.
