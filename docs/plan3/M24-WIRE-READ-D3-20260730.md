# D3 — M24 wire read (`#5 → #942` on b103)

**Tip at write:** `9e59376e9` (manager-d/trade-correctness)  
**Authority:** DISPATCH-D 15:15 §4 / MEAS-01  
**Live page from this workstation:** `http://127.0.0.1:3000/` unreachable — stamp taken from B’s recorded ship evidence, not a browser read.

## Outcome: **A — fix is not deployed on the stamp the PO measured**

| Fact | Evidence |
| --- | --- |
| PO stamp | `20260729b103` (B-0191: `PO_SHOULD_READ=20260729b103`) |
| b103 train tip recorded by B | `153c835e2` — `fix(multichart): cut five panel-realm retainers…` |
| M24 display-ID stability commit | `2cc949399` — `fix(orders): ship M24 restore ID…` (2026-07-30 11:59 +0100) |
| Ancestor check | `git merge-base --is-ancestor 2cc949399 153c835e2` → **false** (exit 1) |
| Flag in b103 tree | `git grep DISABLE_M24_DISPLAY_ID_STABILITY 153c835e2 -- order-manager.js` → **empty** |
| Flag on D HEAD | present (`_resolveJournalDisplayTradeId` / `__TALARIA_DISABLE_M24_DISPLAY_ID_STABILITY_V1`) |

b103 was shipped as the REALM-TEARDOWN memory cut alone. The M24 restore-time display-ID packet landed on D’s branch **after** that ship tip and is **not** an ancestor of `153c835e2`. The PO’s `#5 → #942` observation on b103 is therefore a **routing / deploy lag** failure, not a gate miss and not proof the tip fix is incomplete.

## What this is not

- **Not B (gate gap):** the restore gate + `.red.test.mjs` prove the tip mechanism; they were never on the b103 bytes the PO refreshed.
- **Not C (incomplete fix) on the evidence available:** tip has GREEN restore stability + RED under kill; wire never carried those bytes for the PO’s b103 session.

## Residual (honest)

Current live pin may have moved (B journals show b104 shipped; b105 staged). This workstation could not HTTP-read `SERVED_STAMP`. **Action for B:** when the next money stamp ships, include `2cc949399` (or later tip containing it) and re-run PO Script 1 / MEAS-01. Until a served stamp’s tree contains the flag, treat M24 display-ID as **not on the wire**.
