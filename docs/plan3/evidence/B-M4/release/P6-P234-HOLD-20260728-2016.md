# HOLD — P6 consumers exist; P2/P3/P4 present and unflagged on tip

**Authority:** `RULING-SHIP-GO-P6-HOLD-20260728-2012.md`  
**Artifact inspected:** assembled tip `f8a6c28a8` (ancestor of current `7349f7a9a`; same train, docs-only after).  
**Stamps:** untouched (`20260728b81` remains). **Push:** held.

---

## 1. P6 — does anything request `/chart/talaria-design/live/`?

### Live-surface-probe (test host `http://31.97.192.82:3000`, 2026-07-28T19:16:45Z)

| Shell | HTTP | State | Declared / stamps |
|---|---|---|---|
| `/chart/talaria-design/live/index.html` | **200** | PRESENT | `20260723b12` (+ `b50` in stamps) |
| `/chart/talaria-design/live/` | **200** | PRESENT | same |
| `/chart/dist-v9/index.html` | 200 | PRESENT | `20260726b75` |

Evidence file:
`docs/plan3/evidence/B-M4/live-surface-probe/observations/probe-2026-07-28T19-16-45-747Z-31.97.192.82_3000.json`

The path is a **live, unauthenticated, full chart shell** on the running surface today.

### C shell / servable census (tree)

| Inventory | Path | Status |
|---|---|---|
| `scripts/chart-shell-inventory.json` | `chart v 1.4/talaria-design/live/index.html` | owned-stamped (Vite **source**, not itself the URL) |
| `scripts/servable-surface-inventory.json` | same canonical | owned-stamped |
| Homepage twin `homepage/public/chart/talaria-design/live/index.html` | — | **ABSENT on tip** (`d071c858f`); **PRESENT on baseline** `39152ca7b` |

Nginx `homepage/nginx.local.conf`: `location ^~ /chart/ { try_files $uri $uri/ @chart_upstream; }` — any file under `public/chart/…` is served when present. No product JSX/HTML **href** to `/chart/talaria-design/live` was found; the consumer class is **direct URL / prior bookmarks / audits** (census 16:26Z already listed this as a fourth unauthenticated shell).

### Tip vs image

| Layer | Homepage twin |
|---|---|
| Git tip `f8a6c28a8` | **Deleted** (`d071c858f`) |
| Running test image (today) | **Still serving** stale b12/b50 (pre-train) |
| Assembled `homepage/Dockerfile` L90 | `COPY --from=chart_assets …/talaria-design/live` — would **reintroduce** the URL from canonical live at image build |

### P6 verdict

**Does not clear.** The route has consumers: probe HTTP 200 on the live surface; nginx catch-all would serve it whenever the file is in the image. Per ruling: **A restores `homepage/public/chart/talaria-design/live/index.html` and the deletion rides a later train.**

B does not restore A's file. B also notes the Dockerfile COPY is a related delivery fact — after A restores, that COPY stays coherent; if the intent were permanent de-route, that would be a separate Director decision after consumers are gone.

---

## 2. P2 / P3 / P4 — settle on tip `f8a6c28a8`, not by discussion

Baseline for "in the train": A's audit baseline `39152ca7b` → tip `f8a6c28a8`.

| Item | Product change on tip? | Evidence | Kill-switch on tip? | Verdict |
|---|---|---|---|---|
| **P2** order-line eviction rescope | **YES** | `drawing-tools-manager.js` +16/− lines vs baseline: chart-scoped filter `(l.chart \|\| orderManager.chart) === ch`; commits `3962ef6d3`, `e588b795a` are ancestors | `__TALARIA_DISABLE_M24_ORDER_EVICTION_SCOPE_V1` **ABSENT** | **BLOCKS** |
| **P3** IndicatorPerf loader | **YES** | `indicator-performance.js` +8; shells load `indicator-performance.js?v=20260728b81` on dist-v9 + live; `885057cb5` / `54edafa8a` ancestors | `__TALARIA_DISABLE_INDICATOR_PERF_BRIDGE_V1` **ABSENT** | **BLOCKS** |
| **P4** module-presence tripwire | **YES** | `module-presence-runtime.js` **+108** (new); shells load it; draws `Degraded` badge; `90e0e0cf8` ancestor | `__TALARIA_DISABLE_MODULE_PRESENCE_TRIPWIRE_V1` **ABSENT** | **BLOCKS** |

**Settlement:** A was right. These are **in the assembled tip and unflagged**. B's "continuing work" reading was wrong — it described A's *queue*, not the *artifact*. Per 16:52 / this ruling: **they block the push.**

Reserved names (A journal, do not vary):

- `__TALARIA_DISABLE_M24_ORDER_EVICTION_SCOPE_V1`
- `__TALARIA_DISABLE_INDICATOR_PERF_BRIDGE_V1`
- `__TALARIA_DISABLE_MODULE_PRESENCE_TRIPWIRE_V1`

---

## 3. What does *not* hold (unchanged)

Train assembly B→C→A, stamp `20260728b81`, cache-stamp GREEN, R1 switches — still good. **Not unwound.**

FIX 1 cadence flag — still not in the train; still not a precondition.

---

## 4. Push gate (fail-closed)

| Gate | Status |
|---|---|
| P6 no consumers | **FAIL** — probe 200; A must restore file |
| P2 flagged | **FAIL** — present, no switch |
| P3 flagged | **FAIL** — present, no switch |
| P4 flagged | **FAIL** — present, no switch |

**No push until A lands restore + the three switches (or removes those changes from the tip), and B re-verifies on the new tip.**
