# FIX 1 — Background-panel render cadence delivery path (Manager B → Manager A)

**Role:** Manager B pre-clear (read-only investigation; this file is the only write for this item).  
**Worktree:** `c:\Users\user\Desktop\talaria1\manager-b-plan3`  
**Observed:** 2026-07-28 (local tree only).  
**TREE-02:** citations below are what was Glob’d/grepped/opened; no guessed paths.  
**Status:** **FIX NOT BUILT YET** — reserved kill-switch `__TALARIA_DISABLE_MC_BACKGROUND_RENDER_CADENCE_V1` is **ABSENT** from product trees. This document pre-clears where A should edit, which shells load those bytes, stamp movement, and homepage-twin discard at image build. It is **not** a train ship precondition (see `TRAIN-SHIP-GO-20260728-2000.md`, `KILL-SWITCH-INVENTORY.md`).

---

## Verdict for A (one screen)

| Question | Answer |
|---|---|
| Canonical product bytes (primary edit) | `chart v 1.4/chart/chart.js` — engine `scheduleRender` / rAF / MC host sync live here |
| Likely co-touch (confirm at impl) | `chart v 1.4/chart/multichart-prod/panel-cmd-bridge.js` (embed inject; peer cadence/cmd path); optionally `…/multichart-prod/multichart-manager.js` (focused-panel / layout host — **not** loaded by dist-v9 script tags; parent shell) |
| Committed homepage twin | `homepage/public/chart/chart.js` (+ twins under `homepage/public/chart/multichart-prod/…` if those files change) — **discarded at image build** (overwritten from chart tree) |
| What A must edit for the fix | Canonical chart tree only (below); never homepage twin as SoT |
| Reserved kill-switch | `window.__TALARIA_DISABLE_MC_BACKGROUND_RENDER_CADENCE_V1` — **not present in product yet**; A must add with the fix |
| What stamps move | Blanket shell `?v=` / `__TALARIA_CHART_BUILD_ID` / embed default / SW — **not** a per-file stamp inside `chart.js` |
| Who stamps | `npm run build:live:chart` → `bump-dist-v9-cache.mjs` (and Docker `v9_react` / `chart_assets` stages that run it); checkpoint also `bump-chart-engine-build.mjs` → `CHART_ENGINE_BUILD` in `chart.js` when `CHECKPOINT_BUILD=1` |
| Checkpoint / contract preflight | `scripts/lib/checkpoint-provenance.mjs` (shell lists + I8 `modules/` + **`chart.js`** mirror); `scripts/module-contract-preflight.mjs` exists but **does not** contract `chart.js` content (only ModulePresenceRuntime / IndicatorPerf) |

---

## 1. On-disk inventory (TREE-02 — only paths actually found)

### Primary: `chart.js` (2 hits)

| Path | Role | SHA-256 (this worktree) | Loaded by which shell | Stamp that must move |
|---|---|---|---|---|
| `chart v 1.4/chart/chart.js` | **Source of truth** (committed product). Docker `COPY ["chart v 1.4/chart", …]` then final overwrite from `v9_react` / `chart_assets` `/build/chart/chart.js`. | `9422392ec420e8da5f7fbaa1942cb9548c37fb41200d033f97d6205ff342b08b` | See shell table | Shell `?v=` / embed `V` / SW (via build); checkpoint may rewrite `CHART_ENGINE_BUILD` inside file |
| `homepage/public/chart/chart.js` | **Committed mirror** — byte-identical here to canonical (`9422392e…`). Regenerated by `sync-v9-to-homepage.mjs` (`chart.js` copy L57–61). **Discarded in image:** `homepage/Dockerfile` L72 `COPY homepage/` then L81 `COPY --from=chart_assets …/chart.js`. | same | Same URL `/chart/chart.js` if someone serves committed `public/` without Docker overwrite | Same as above if that path is what is served |

Glob / enumerate: exactly **2** `chart.js` files under the worktree (excluding `node_modules`).

### Multichart scheduling co-candidates (found; A confirms need at impl)

| Path | Role | Notes |
|---|---|---|
| `chart v 1.4/chart/multichart-prod/panel-cmd-bridge.js` | Prod panel cmd/cadence bridge | Injected from dist-v9/live embed shim (`inject('/chart/multichart-prod/panel-cmd-bridge.js')`); homepage twin exists |
| `homepage/public/chart/multichart-prod/panel-cmd-bridge.js` | Committed twin | Overwritten by Docker L82 `multichart-prod` COPY from `chart_assets` |
| `chart v 1.4/chart/multichart-prod/multichart-manager.js` | Prod layout / `focusedPanelId` host | Parent multichart surface; homepage twin exists; Docker overwrite via L82 |
| `chart v 1.4/chart/multichart/multichart-manager.js` | Legacy multichart tree | Separate from `multichart-prod/`; homepage twin under `homepage/public/chart/multichart/` — **not** in Dockerfile overwrite list (stale if edited there without sync discipline) |

**Kill-switch product search (this worktree):** `__TALARIA_DISABLE_MC_BACKGROUND_RENDER_CADENCE_V1` / `BACKGROUND_RENDER` / `backgroundRender` / `renderCadence` as FIX 1 feature — **ABSENT** from product trees (grep + prior train tip poll). Do not treat any existing `scheduleRender` call site as the FIX 1 implementation.

### Related non-file carriers (references only)

| Location | What was seen |
|---|---|
| `chart v 1.4/chart/scripts/build-chart-client-bundle.mjs` L62 | Bundle input includes `'chart.js'` — only if `BUILD_LEGACY_BUNDLE=1` (`Dockerfile.local` L116–120 skips by default). |
| `chart v 1.4/chart/api_server.py` ~L27004 | Static allowlist includes `"chart.js"`. |
| `chart v 1.4/chart/multichart-prod/harness/serve.mjs` L603 | Allowlist `'/chart/chart.js'`. |
| `scripts/module-contracts.json` | Orders other modules relative to `/chart/chart.js`; does **not** version/gate FIX 1 content. |

---

## 2. Shells that load it — exact `src` / stamp pattern

| Shell (canonical path) | Exact load pattern (committed stamps in this tree) | Homepage twin |
|---|---|---|
| `chart v 1.4/talaria-design/live/index.html` L1615 | `<script defer src="/chart/chart.js?v=20260728b81"></script>` | **No** `homepage/public/chart/talaria-design/live/index.html` in this worktree (Glob under `homepage/public/chart`). Docker still overwrites that path at build from `chart_assets` (`homepage/Dockerfile` L88–90) |
| `chart v 1.4/chart/dist-v9/index.html` L1615 | same absolute `?v=20260728b81` | `homepage/public/chart/dist-v9/index.html` L1613 — same `b81` |
| `chart v 1.4/chart/legacy-index.html` L44067 | `<script defer src="chart.js?v=20260728b81"></script>` (relative) | **No** committed `homepage/public/chart/legacy-index.html` in this worktree |
| `chart v 1.4/chart/multichart-prod/chart-embed.html` L9 + inject loop | Default: `window.__TALARIA_CHART_BUILD_ID = p.get('v') \|\| '20260728b81'`; path `'/chart/chart.js'` at L350 (**no literal `?v=` on that path string**; `q = '?v=' + V`) | `homepage/public/chart/multichart-prod/chart-embed.html` L9 / L348 — same pattern |
| dist-v9 / live embed shim | `inject('/chart/multichart-prod/panel-cmd-bridge.js')` with shim `V = '20260728b81'` (dist-v9 L114–130) | Homepage dist-v9 mirror carries same inject |

**Stamp machinery (what “must move” means):**

- V9 / live / dist HTML: every `/chart/…` script|link `?v=` plus `window.__TALARIA_CHART_BUILD_ID='…'` via `bump-dist-v9-cache.mjs`.
- Legacy: `LEGACY_SCRIPT_SRC_RE` stamps relative `chart.js` / `modules/…` and absolute `/chart/…`.
- Embed: default fallback id + module URLs inherit `__TALARIA_CHART_BUILD_ID`.
- SW: `SW_VERSION = "talaria-chart-<id>"`.
- Checkpoint: `CHART_ENGINE_BUILD` in `chart.js` via `bump-chart-engine-build.mjs` when `CHECKPOINT_BUILD=1`.

There is **no** FIX 1 module-body stamp today (feature absent). After A lands the fix, still rely on blanket shell stamps — do not invent a hand-maintained `chart.js?v=` only.

---

## 3. Source vs build-output vs discarded mirror (Dockerfile evidence)

### Director ruling — verified against current Dockerfiles

Homepage `public/chart` mirror discarded at image build. **Confirmed:**

**`homepage/Dockerfile`**

- L4–5 comment: chart static built in `chart_assets` so nginx does not serve stale committed `public/chart`.
- L29: `COPY ["chart v 1.4/chart", "./chart/"]` into build stage.
- L47: `npm run build:live:chart`.
- L73: `COPY homepage/ ./` (brings committed mirror).
- L78–90: `# Fresh chart bundle (overwrites committed homepage/public/chart/*)` then  
  `COPY --from=chart_assets /build/chart/chart.js ./public/chart/chart.js`  
  (also dist-v9, modules, multichart-prod, workers, vendor, fonts, sw, **and** `talaria-design/live` from `/build/talaria-design/live`).

**`chart v 1.4/chart/Dockerfile.local` (trading-chart)**

- L32: `COPY ["chart v 1.4/chart", "./chart/"]` into `v9_react`.
- L50: `npm run build:live:chart`.
- L91: final `COPY ["chart v 1.4/chart", "./"]`.
- L100–110: overwrite `./chart.js` (and modules / dist-v9 / multichart-prod / …) from `v9_react` `/build/chart/…` — comment L102–103: must match dist-v9 build.

**Not overwritten by those COPY lists:** `homepage/public/chart/multichart/**` (legacy multichart tree) survives `COPY homepage/` and is **not** in the chart_assets overwrite set. Prefer `multichart-prod/` for FIX 1 if a host-side change is required.

### Role summary

| Artifact | Role at ship time |
|---|---|
| `chart v 1.4/chart/chart.js` | Source → baked into image `chart.js` |
| `homepage/public/chart/chart.js` | Committed / local / test mirror; **discarded** when homepage image rebuilds |
| `dist-v9/index.html`, stamped live, legacy, embed, sw | **Build outputs** of `build:live:chart` + bump (then COPY’d into images) |
| Legacy minify `dist/chart-app-part*.min.js` | Optional build output; **not** default image path |

### nginx

`homepage/nginx.local.conf` L145–150: `location ^~ /chart/` → `try_files` on Next export root, `expires 1h`, `Cache-Control "public, must-revalidate"`; miss → `@chart_upstream` → trading-chart. So homepage nginx prefers baked `out/chart/chart.js`.

---

## 4. Rebuild steps → which copies regenerate

| Step | Regenerates |
|---|---|
| Edit product | Only canonical bytes until a sync/build runs |
| `cd "chart v 1.4/talaria-design" && npm run build:live:chart` | Vite → `chart/dist-v9/`; `bump-dist-v9-cache` stamps live/dist/legacy/embed/harness/SW; `sync-v9-to-homepage.mjs` **replaces** `homepage/public/chart/chart.js` (+ modules / multichart-prod / …), then re-bumps homepage dist |
| Docker `trading-chart` / `homepage` image build | Runs same `build:live:chart` inside `v9_react` / `chart_assets`; final image `chart.js` comes from that stage’s `/build/chart/chart.js` (canonical tree), **not** from pre-existing homepage mirror |
| `CHECKPOINT_BUILD=1` + `CHART_BUILD_ID=…` | Additionally runs `bump-chart-engine-build.mjs` with `BUILD_ID`; layout assert via `checkpoint-build-assert.mjs` → `checkpoint-provenance.mjs` |
| `BUILD_LEGACY_BUNDLE=1` | Would rebuild client bundle that concatenates `chart.js` — off by default |

---

## 5. Stamps / provenance / “module-contract”

### `scripts/lib/checkpoint-provenance.mjs`

- I8 / mirror dirs include **`chart.js`** (canonical ↔ homepage must hash-match inside layout assert’s homepage-chart-root).
- Shell list covers dist-v9, live (when `requireLive`), embed, legacy, harness `buildId`, SW — same machinery as FIX 3 handoff.
- `matchAllCacheIds` on listed shells; uniqueness of `?v=` / build id.

### `scripts/module-contract-preflight.mjs`

Present in this worktree (`scripts/module-contracts.json`). **Does not** gate FIX 1 / `chart.js` body change — only ModulePresenceRuntime + IndicatorPerf. Do not expect this preflight alone to fail a missing cadence switch.

### What must bump when FIX 1 lands in `chart.js` (and/or prod multichart files)

1. Active `CHART_BUILD_ID` / shell `?v=` on **dist-v9**, **live**, **legacy**.
2. Embed default `__TALARIA_CHART_BUILD_ID = p.get('v') \|\| '<id>'`.
3. `SW_VERSION`.
4. For strict checkpoint ships: `CHART_ENGINE_BUILD` in `chart.js` (same id).

**Known stamp pitfall (B-M4):** auto-increment from a local committed id can land **behind** live. Ship with explicit `CHART_BUILD_ID` **ahead of live** under `CHECKPOINT_BUILD=1`.

---

## 6. Live observation

Not re-probed for this pre-clear (feature absent; no post-fix bytes to match). Prior B-M4 evidence still applies to **any** `/chart/…` script on the test host:

- `?v=` may be **inert** (identical sha across query variants) — CENSUS-20260728-1626Z / FIX3 probe class.
- Post-push proof = **byte-identity** of `/chart/chart.js` body vs post-fix canonical, not “URL now says bNN”.

---

## What A must touch vs what the build stamps for free

### A must touch (when building FIX 1)

1. **`chart v 1.4/chart/chart.js`** — primary product surface for background-panel render cadence + kill-switch `__TALARIA_DISABLE_MC_BACKGROUND_RENDER_CADENCE_V1`.
2. Only if impl requires host/bridge scheduling: canonical **`multichart-prod/panel-cmd-bridge.js`** and/or **`multichart-prod/multichart-manager.js`** — not the legacy `multichart/` twin as SoT.
3. Ship via image rebuild of **homepage + trading-chart**.
4. For dual-tree / soak tests: run `build:live:chart` (syncs mirror) — **do not treat committed homepage twins as deploy SoT**.
5. Choose a **`CHART_BUILD_ID` ahead of live** when using checkpoint build args.

### Build stamps for free

- All shell `?v=` / embed defaults / harness `buildId` / SW via `bump-dist-v9-cache.mjs`.
- Homepage `chart.js` + `modules/` + `multichart-prod/` refresh via `sync-v9-to-homepage.mjs`.
- Docker stages that invoke `npm run build:live:chart` (+ optional `bump-chart-engine-build`).

### A should not rely on

- Editing only `homepage/public/chart/chart.js` (discarded at image build).
- Hand-bumping a single `chart.js?v=` without the bump script.
- Assuming `?v=` alone proves new bytes on the test host (may be inert).
- Waiting on this FIX for the current train ship (explicitly **not** a precondition).

---

## Residual risks

1. **FIX absent** — until A lands code + switch, probes for the kill-switch name will correctly report ABSENT.
2. **Inert `?v=` on test host** — stamp moves the cache key; proof must be sha256 of body.
3. **Wrong multichart tree** — editing `multichart/multichart-manager.js` may not reach Docker-baked `multichart-prod/` surfaces.
4. **Nginx 1h TTL** — old stamp URLs remain cacheable if still requested.
5. **Auto-increment behind live** — use explicit ahead-of-live `CHART_BUILD_ID` for checkpoint ships.
6. **Co-file ambiguity** — pre-clear names `chart.js` as primary; A must confirm whether panel-cmd-bridge / manager also need edits when implementing.

---

## One-command verification (A after change + deploy)

Run from repo root after FIX 1 image is live on the test host (PowerShell). Compares **local canonical bytes** to **served bytes** and prints live shell stamps for `chart.js`:

```powershell
$local="chart v 1.4/chart/chart.js"; $tmp="$env:TEMP\chart.live.js"; Invoke-WebRequest "http://31.97.192.82:3000/chart/chart.js" -OutFile $tmp -UseBasicParsing; $L=(Get-FileHash $local -Algorithm SHA256).Hash.ToLower(); $R=(Get-FileHash $tmp -Algorithm SHA256).Hash.ToLower(); "local=$L"; "live=$R"; "bytes_match=$($L -eq $R)"; foreach($s in @('/chart/dist-v9/index.html','/chart/legacy-index.html','/chart/multichart-prod/chart-embed.html','/chart/talaria-design/live/index.html')){ $c=(Invoke-WebRequest "http://31.97.192.82:3000$s" -UseBasicParsing).Content; $m=[regex]::Match($c,'chart\.js[^"''\s]*'); "$s -> $($m.Value)" }; $body = Get-Content $tmp -Raw; "kill_switch_present=$($body -match '__TALARIA_DISABLE_MC_BACKGROUND_RENDER_CADENCE_V1')"
```

**Pass criteria for A:** `bytes_match=True` against the post-fix local file; `kill_switch_present=True`; dist-v9 / legacy / embed advertise the new `CHART_BUILD_ID`. Dual-fetch two `?v=` variants and confirm whether stamp remains inert (deploy-gate / CENSUS class).

---

## Cite index (opened / grepped)

- Enumerate: `chart.js` → 2; `panel-cmd-bridge.js` → 2; `multichart-manager.js` → 4.
- Grep product trees: `__TALARIA_DISABLE_MC_BACKGROUND_RENDER_CADENCE_V1` → **0** hits.
- `homepage/Dockerfile` L4–5, L29, L47, L73, L78–90.
- `chart v 1.4/chart/Dockerfile.local` L32, L50, L91, L100–120.
- `chart v 1.4/talaria-design/scripts/sync-v9-to-homepage.mjs` L57–61, L80–90.
- `scripts/lib/checkpoint-provenance.mjs` `MIRROR_DIRECTORIES` includes `chart.js`.
- `scripts/module-contracts.json` / `module-contract-preflight.mjs` — present; not FIX 1 content gate.
- `homepage/nginx.local.conf` L145–150.
- Shell HTML lines cited in §2; prior inert-`?v=` class: `CENSUS-20260728-1626Z` / `FIX3-REPLAY-SYSTEM-DELIVERY-PATH.md`.
- Train status: `TRAIN-SHIP-GO-20260728-2000.md`, `KILL-SWITCH-INVENTORY.md`.
