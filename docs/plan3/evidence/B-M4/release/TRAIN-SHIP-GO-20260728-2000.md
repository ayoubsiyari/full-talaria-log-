# TRAIN SHIP GO — phantom gate cleared · 2026-07-28 ~20:05

**Authority:** `docs/plan3/UNBLOCK-B-PHANTOM-GATE-20260728-1958.md`  
**Owner:** Manager B (release)

---

## 1. Phantom gate — confirmed not a precondition

Director's three patterns + FIX 1 flag name, searched on `manager-a/critical-path` tip
`ba174d694` (includes R1 merge `a794613b4` @ 19:55):

| Pattern | Result in product tree |
|---|---|
| `backgroundRender` | **ABSENT** |
| `renderCadence` | **ABSENT** |
| `BACKGROUND_RENDER` | **ABSENT** |
| `DISABLE_MC_BACKGROUND_RENDER_CADENCE` | **ABSENT** |

`--deploy-gate` cannot prove source absence (it proves served bytes). Source `git grep`
on the tip is the right instrument for "is FIX 1 in the train?" — answer: **no**.

FIX 1 remains A's last item; its reserved switch name stays
`__TALARIA_DISABLE_MC_BACKGROUND_RENDER_CADENCE_V1` for when A builds it. **Not a
release precondition.**

---

## 2. Render / lifecycle items that *are* in the train — each has a runtime switch

| Item | Tip evidence | Switch |
|---|---|---|
| B hydration guard | B tip `order-manager.js` | `__TALARIA_DISABLE_B_W16_HYDRATION_GUARD_V1` |
| B journal parse guard | B tip `api_server.py` | `JOURNAL_SWEEP_PARSE_GUARD_ENABLED` |
| M26 panel replay destroy | A tip `multichart-manager.js` | `__TALARIA_DISABLE_M26_PANEL_REPLAY_DESTROY_V1` |
| M27 engine release | A tip `replay-system.js` | `__TALARIA_DISABLE_M27_ENGINE_RELEASE_V1` |
| M28 hidden-pause (FIX 3) | A tip `replay-system.js` | `__TALARIA_DISABLE_REPLAY_HIDDEN_PAUSE_V1` |
| R1 / M23 host-commit teardown | A tip `chart.js` @ R1 | `__TALARIA_DISABLE_M23_HOST_COMMIT_TEARDOWN_V1` |
| R1 / M20-Q9 counters | A tip `chart.js` @ R1 | `__TALARIA_DISABLE_M20_Q9_MCDIAG_COUNTERS_V1` |

C's contribution is build-time gates (cache-stamp coherence, legacy de-route) — no
runtime switch required.

---

## 3. Residuals (ship with eyes open — not blockers per Director)

A's train audit still lists P2/P3/P4 (and P6 decision) as missing switches on
**pre-existing** ancestry items. Director unblocked the train without waiting for
those or FIX 1. Residual risk is recorded, not used as a hold.

---

## 4. Go

Assemble B → C → A (`manager-a/critical-path` @ `ba174d694`) and ship under
`RELEASE-SHIP-REQUIREMENTS.md` (`CHECKPOINT_BUILD=1`, ahead-of-live `CHART_BUILD_ID`).
Post-push: `live-surface-probe --deploy-gate`.

---

## 5. Assembled tip (post-bump)

| | |
|---|---|
| Branch | `manager-b/plan3-20260727` |
| Tip | `f8a6c28a8` (stamp commit `87ab4ddaa`) |
| `CHART_BUILD_ID` | `20260728b81` |
| Cache-stamp gate | GREEN |
| Next | checkpoint image build; then `live-surface-probe --deploy-gate` |
