# Live-surface claim census — 2026-07-28T16:26Z

**Stream 3 (PAR-01).** Observer: [live-surface claim census](62bad0c8-5736-49ba-99f5-80e932f67cae).
**Target:** `http://31.97.192.82:3000` only · GET/HEAD · no credentials · no repo writes by the observer.

Prior baseline reconfirmed: `order-manager.js` identified, `journalVouchedFor` **ABSENT**; primary shells stamp `20260726b75`.

---

## Top 3 new findings

1. **Shell stamp incoherence + inert `?v=`.** `/chart/talaria-design/live/` advertises `20260723b12` / `b50` while origin serves one `chart.js` body with `CHART_ENGINE_BUILD='20260726b75'` under every query string (identical sha256 for unstamped, b75, b12, b50, and even `?v=99999999z99`). Same for `order-manager.js`. On this host the stamp is a cache key only; it does not select bytes. A client that cached under `?v=b12` can hold a stale module for up to `max-age=3600` after a push that only bumps the “real” stamp.

2. **Fourth unauthenticated full chart shell.** `/chart/talaria-design/live/index.html` (91,147 B) and `/chart/talaria-design/live/` return full V9 HTML (order-manager, preferences, chart.js) without auth. `/chart/dist-v9/` also serves the dist-v9 shell. The “three known shells” inventory was incomplete.

3. **Static allowlist hides 404s.** Unknown module paths (and `user-storage.js`) return **307 → login**, not 404. Unauthenticated probes cannot distinguish “not deployed” from “not on public allowlist.”

---

## Claim table (abridged)

| Claim | State | Fact |
|---|---|---|
| `chart.js` served at b75 | PRESENT | 200, 1,945,754 B, `CHART_ENGINE_BUILD='20260726b75'` |
| `?v=` changes asset bytes | ABSENT (inert) | sha256 identical across query variants |
| B-territory modules identity | PRESENT | all five + order-manager 200, JS, no HTML fallback |
| `GET /api/sessions/1/state` | UNDETERMINED | 401, `{"detail":"Not authenticated"}` |
| Extra unauth shells | PRESENT | `talaria-design/live/`, `dist-v9/` alias |
| No Cloudflare | PRESENT | `cf-cache-status` null; `cache-control: max-age=3600, public, must-revalidate` |
| Design shell vs engine stamp | PRESENT (disagree) | shell b12/b50, engine b75 |
| D-2 markers in order-manager | ABSENT | 0 occurrences |

---

## Release-plan consequences

- Post-push probe must include `--shell=/chart/talaria-design/live/index.html` and require it either absent/de-routed or stamped **b81** coherent with dist-v9 — a b12 leftover after push is a fail.
- Byte-identity check (served sha ≠ pre-push `ff6e9df1…` / `eb70d1a5…`) remains mandatory; stamp alone is insufficient on this host because `?v=` does not select content.
- Module ABSENT via 307→login must stay UNDETERMINED, never ABSENT — probe already does this for redirects; do not “fix” by treating login as missing asset.
