# C3 — Canvas UX & consistency batch
Status: DONE (worker)

## Symbols / line ranges
- `SectionNode.handleFileChange` @ `Sources Handoff/TalariaV16.jsx` ~1683-1692 now uses `validateStrategyImageFile(file)`.
- `ConditionCard.handleFileChange` @ ~2534-2543 now uses `validateStrategyImageFile(file)`.
- `StrategyCanvasWorkspaceInner` notice state/callback @ ~4216-4264 adds neutral `flowNotice` / `showFlowNotice`.
- Delete-last-group guard @ ~4531-4537 calls `showFlowNotice('At least one group is required.')`.
- `_cvCb.showFlowNotice` bridge @ ~4974 routes section-node delete notices to neutral styling.
- Outline blur/status helpers @ ~5098-5166 add `outlineGroupBlur` and outside-click close for the outline status menu.
- Neutral info toast render @ ~5475-5486 uses blue/info styling instead of the red image-error alert.
- `ReactFlow` board render @ ~5516-5525 keeps `edges={canvasEdges}` / `edgeTypes={CANVAS_EDGE_TYPES}` and removes user-reachable `onConnect` / `onEdgesChange` / default edge creation props.
- Outline group title input @ ~5615 uses `outlineGroupBlur(group.id)`.
- Outline status trigger/menu @ ~5671 and ~5691 are tagged with `data-outline-status-menu-root="1"` so inside clicks do not dismiss.
- `validateStrategyImageFile` @ ~6140-6154 remains the single strategy-builder image validator; the old broad `validateScreenshotUploadFile` helper was removed.

## Verification
| Check | Result |
| --- | --- |
| Delete last group shows neutral/info notice | Static pass: delete guards call `showFlowNotice`; render uses blue/info `role="status"` toast. |
| Outline status menu closes on outside click | Static pass: document `mousedown` closes unless inside `data-outline-status-menu-root="1"`. |
| Empty outline group label restores default | Static pass: `outlineGroupBlur` calls `renameSection(groupId, DEFAULT_GROUP_LABEL)` when trimmed value is empty. |
| Board upload validation matches outline | Static pass: section and condition board uploads call `validateStrategyImageFile(file)`. |
| No user-reachable edge drag/connect | Static pass: `onConnect`, `onEdgesChange`, `defaultEdgeOptions`, `addEdge`, `applyEdgeChanges`, `MarkerType`, `Handle`, and `Position` no longer match in the file; saved/template edges still render through `edges={canvasEdges}` and `edgeTypes={CANVAS_EDGE_TYPES}`. |
| Dead in-zone internals removed | Static pass: `statsOf`, `blankTpl`, `selectedNode`, and `doFit` no longer match. |
| `git diff` presence | Present for `Sources Handoff/TalariaV16.jsx`. |
| `git diff --check` | PASS. |
| `ReadLints` | Timed out twice after 10s on the 59k-line JSX file; no diagnostics returned. |

## ICR
None filed.
