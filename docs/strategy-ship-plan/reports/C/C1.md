# C1 — Undo must never restore an empty canvas (REWORK)
Status: DONE (worker) · **Manager verdict: VERIFIED (static) — clobber resolved, edits confirmed present**

## Manager verification note (added by Manager)
Re-apply confirmed in the live tree (grep + read):
- `HISTORY_LIMIT = 50` @4056; `initialHistorySnapshot = makeHistorySnapshot(canvasNodes, canvasEdges)` @4080; `useState([initialHistorySnapshot])` @4081 — **no longer the empty seed** ✓.
- `pushHistory` @4133–4142: key-dedupe, drops redo tail, `slice(-HISTORY_LIMIT)` cap ✓.
- Reseed `useLayoutEffect` @4144–4153: guarded to only replace a single empty initial entry with the mounted canvas (won't wipe mid-session history) ✓.
- `commitCanvasMutation` @4156 wired across add/delete group, add/delete/move condition, rename, status, template load (call sites 4325–4880) ✓.
- Zone: all hunks in 4052–4953 (C's zone) ✓. `MIN_STRATEGY_FLOW_GROUPS` intact ✓. `_cvCb` reassign-on-render kept ✓. No new deps ✓.
- Lint clean (Manager re-ran ReadLints). Build intentionally not run by C to avoid codegen overwrite — acceptable.
- **Runtime verification DEFERRED** to Phase 1 gate: mount-with-template → mixed ops → Ctrl+Z back to exact mounted state (never empty), redo, in board AND outline, no console errors.

Clobber root cause (concurrent full-file save) mitigated via exclusive-lock rework. A/B/D edits unaffected.
