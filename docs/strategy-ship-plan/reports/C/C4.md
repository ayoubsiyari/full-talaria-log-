# C4 — PDF export polish
Status: DONE (worker)

## Symbols / line ranges
- `escPrint` @ `Sources Handoff/TalariaV16.jsx` ~5225 is unchanged and still escapes `& < > " '`.
- `printOutlineDocument` @ ~5226-5237 still writes the printable document, keeps `/LOGO-07.png` in the header/watermark, and preserves the popup-blocked caller contract by accepting an already-opened window.
- `handlePrintPdf` @ ~5233-5252 now validates before `window.open`:
  - returns early when `isSaving`;
  - requires a non-empty `stratBName`;
  - requires `onSave` to be available;
  - awaits `onSave({ keepOpen: true })` before opening the print window;
  - only opens the popup after save succeeds;
  - keeps the existing popup-blocked message: `Allow pop-ups to print the strategy PDF.`

## Verification
| Check | Result |
| --- | --- |
| Print with no name shows clear message and no popup flash | Static pass: missing name returns before `window.open`. |
| Invalid/unsavable state does not flash a popup | Static pass: unavailable `onSave` and save failures return/show error before `window.open`. |
| Happy path still prints after save | Static pass: after `await onSave({ keepOpen: true })`, `window.open` is called and `printOutlineDocument(printWindow)` runs. |
| Popup-blocked message preserved | Static pass: same `Allow pop-ups to print the strategy PDF.` message remains after `window.open` failure. |
| `escPrint` intact | Static pass: escaping helper unchanged. |
| Logo retained | Static pass: print HTML still references `/LOGO-07.png`. |
| `git diff` presence | Present for `Sources Handoff/TalariaV16.jsx`. |
| `git diff --check` | PASS. |
| `ReadLints` | Timed out twice after 10s on the 59k-line JSX file; no diagnostics returned. |

## ICR
None filed.
