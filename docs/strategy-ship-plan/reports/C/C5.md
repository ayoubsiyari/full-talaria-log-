# C5 — Canvas undo-coverage gaps
Status: DONE for section move; BLOCKED for modal-level template load history bridge

Source traced/edited: `Sources Handoff/TalariaV16.jsx`

## Symbols / Current Line Ranges

- `StrategyCanvasWorkspaceInner` history helpers: `pushHistory` / `commitCanvasMutation` @ 4267-4302.
- Custom section drag finalizer: `startSectionDrag` mouseup branch @ 4763-4795.
- React Flow section drag finalizer: `onNodeDragStop` section branch @ 4937-4961.
- `applyStrategyTemplate` prop in canvas param list @ 4179; pass site @ 47221.

## Finding #2 — Section Reorder History

Result: DONE.

What changed:
- Replaced the custom section drag finalizer's plain `setCanvasNodes(...)` with `commitCanvasMutation(...)`.
- Replaced the React Flow `onNodeDragStop` section branch's plain `setCanvasNodes(...)` with `commitCanvasMutation(...)`.
- Preserved the existing `snapY` / section reflow math, z-index reset, drag side effects, and `setTimeout(() => setSliding(false), 350)`.

Static trace:
- Live drag still uses direct state updates for transient movement.
- On drag stop, final snapped positions now flow through `commitCanvasMutation`, which calls `pushHistory(nextNodes, nextEdges)`.
- The prior ordering is therefore one history entry back, so Ctrl+Z should restore the prior order and redo should restore the snapped order.

## Finding #1 — Template Load History

Result: BLOCKED by zone boundary.

Determination:
- `applyStrategyTemplate` is passed into `StrategyCanvasWorkspaceInner` but is not invoked anywhere inside the canvas component.
- There is no user-reachable in-canvas "apply/load template" control to wrap in `pushHistory` / `commitCanvasMutation`.
- The user-reachable template replacement path is modal-level (`onOpenTemplates` -> external `TemplatePickerModal` -> `fillStrategyBuilderFromTemplate`), not inside the canvas zone.

Action taken:
- No source edit was made outside the canvas zone.
- Filed `reports/C/ICR-4.md` requesting either end-to-end dead prop removal or a cross-zone history-aware template apply bridge for modal-level template loads while the canvas is mounted.

## Verification

| Check | Result |
| --- | --- |
| Static trace: section move goes through history | PASS: both final section snap paths now call `commitCanvasMutation`. |
| Static trace: previous section order is undo target | PASS: live drag mutates transient state, final snap is pushed as the next history entry. |
| Static trace: in-canvas template apply reachability | BLOCKED/OUT-OF-ZONE: `applyStrategyTemplate` is unused in the canvas; modal-level picker path owns replacement. |
| `ReadLints` | First attempt timed out after 10s; retry passed with no linter errors. |
| `git diff --check` | PASS. |
| `git diff` presence | PRESENT. Diff includes pre-existing unrelated changes outside C5; C5 source edits are limited to the canvas zone. |

## Notes

Runtime browser checks are DEFERRED to the final Docker pass.
