# A4 - Pre-save payload budget

Status: DONE

## What changed

| File | Symbol(s) | Current line range | Nature of change |
|---|---|---:|---|
| `Sources Handoff/TalariaV16.jsx` | `STRATEGY_SAVE_MAX_CONTENT_BYTES`, `STRATEGY_SAVE_PAYLOAD_BUDGET_BYTES`, `STRATEGY_IMAGE_DATA_URL_MAX_CHARS` | 100-102 | Added frontend constants aligned to backend `MAX_CONTENT_LENGTH` 16 MB and `MAX_COVER_IMAGE_LEN` 2,800,000 chars, with a 15 MB conservative save budget. |
| `Sources Handoff/TalariaV16.jsx` | `strategyImagePayloadTooLarge`, `estimateStrategySavePayloadBytes` | 3184-3231 | Added a preflight image cap check and conservative JSON payload byte estimator for the strategy save request shape. |
| `Sources Handoff/TalariaV16.jsx` | `saveBuilder` | 46588-46672 | Blocks oversized images or over-budget serialized payloads with `stratBuilderSaveError` before `setStratBuilderSaving(true)` and before any live `persist()` call. |

`saveBuilder` now builds the strategy object, checks embedded data-image lengths, estimates the JSON request payload, and returns a rejected Promise with in-modal error text before any network request can start. Normal-sized strategies continue into the existing save path unchanged.

## Zone compliance

- [x] All hunks inside Worker A ownership zone.
- [x] No security/limits weakened.
- [x] No new deps.
- [x] ICRs raised: none.

## Verification evidence

| Steps run | Expected | Observed | Pass |
|---|---|---|---|
| Read backend/shared limits (`shared/constants.json`, `journal-backend/config.py`, `journal-backend/schemas/strategy_lab.py`). | Frontend guard aligns with 16 MB request limit and 2.8M image data-url cap. | Added 16 MB max, 15 MB conservative budget, and 2.8M data-url cap constants. | Yes |
| Code-path inspection of `saveBuilder`. | Oversized payload returns before live `persist()` and before image upload/network path. | Guard runs before `setStratBuilderSaving(true)` and before `persist(strat, apiId)`. | Yes |
| IDE diagnostics via `ReadLints` on touched files. | No diagnostics introduced. | No linter errors found. | Yes |
| `npm exec -- tsc --noEmit --pretty false` from `homepage`. | TypeScript check exits 0. | Exited 0 with no output. | Yes |
| `npm run lint` from `homepage`. | Lint should run or report repo setup limitation. | `next lint` exited 1 because it prompted to configure ESLint; no lint findings were emitted. | Partial - blocked by repo lint setup prompt |
| `git diff -- Sources Handoff/TalariaV16.jsx homepage/src/app/dashboard/v16/v16JournalMappers.ts ...`. | Diff shows A4 hunks present. | Diff output included payload constants/helpers and `saveBuilder` preflight guard. | Yes |

Console errors introduced: none observed from static checks. Browser oversized-image network simulation was not run in this environment.

## Risks & notes

The payload check is conservative because it estimates the JSON payload before image uploads replace data URLs with stored image URLs. This intentionally blocks very large embedded-image strategies before any upload request, matching the task requirement to prevent network activity for over-budget saves.

## Blocked?

Not blocked.
