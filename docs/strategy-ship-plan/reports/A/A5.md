# A5 - Canvas conditions, tree restore, and builder lifecycle backstops

Status: DONE

## What changed

| File | Symbol(s) | Current line range | Nature of change |
|---|---|---:|---|
| `Sources Handoff/TalariaV16.jsx` | `canonicalStrategyTimeframe`, `normalizeStrategyTimeframes`, `deriveCustomStrategyTimeframes` | 397-424 | Added shared timeframe normalization/backstop helpers matching B2 units (`m`, `H/D/W/M`) and custom-TF derivation. |
| `Sources Handoff/TalariaV16.jsx` | `deriveStrategyBuilderGroupsFromCanvas`, `deriveStrategyBuilderConditionsFromCanvas` | 3147-3182 | Extracted the Review-step canvas group/condition derivation into reusable helpers and added a flattened condition list for persistence. |
| `Sources Handoff/TalariaV16.jsx` | `ReviewStepContent` | 7919 | Switched Review to use the shared group derivation so save and review read the same canvas structure. |
| `Sources Handoff/TalariaV16.jsx` | `resetStrategyBuilderForm`, `fillStrategyBuilderFromTemplate`, `openBuilder` | 46219-46378 | Reset/restored `stratBTree`, normalized/restored timeframes, populated/reset lifted `stratBTfCustom`, added `skipConfirm=false`, and implemented ICR-4 saved-market precedence. |
| `Sources Handoff/TalariaV16.jsx` | `saveBuilder` | 46588-46672 | Writes flattened canvas-derived conditions to the saved strategy root, falls back to legacy `stratBConditions` when no canvas conditions exist, and implements ICR-1 timeframe normalization/cap enforcement before persist. |
| `docs/strategy-ship-plan/reports/B/ICR-1.md` | Section 5 | 32-36 | Filled Worker A implementation notes for the timeframe save backstop. |
| `docs/strategy-ship-plan/reports/D/ICR-4.md` | Section 5 | 55-59 | Filled Worker A implementation notes for saved-market restore precedence. |

`saveBuilder` no longer persists stale empty root conditions when the actual strategy flow lives in `canvasNodes`; it now flattens the same section/condition ordering used by Review and stores that list in `conditions`. Editing restores `tree`, saved markets now win over derived markets, and the save path enforces normalized/deduped <=6 timeframes as the final persistence backstop.

## Zone compliance

- [x] All hunks inside Worker A ownership zone.
- [x] No security/limits weakened.
- [x] No new deps.
- [x] ICRs raised: none.

## Verification evidence

| Steps run | Expected | Observed | Pass |
|---|---|---|---|
| Read current Review derivation and `saveBuilder` before editing. | Save uses same section/condition ordering as Review. | Shared `deriveStrategyBuilderGroupsFromCanvas` now feeds Review; `deriveStrategyBuilderConditionsFromCanvas` feeds save. | Yes |
| Read `openBuilder(editStrat)` market restore path and ICR-4. | Saved `editStrat.markets` preferred when non-empty. | `setStratBMarkets(editSavedMarkets.length ? editSavedMarkets : editDerivedMarkets)`. | Yes |
| Read B ICR-1 and B2 timeframe behavior. | `saveBuilder` canonicalizes/dedupes, blocks empty/over-cap arrays. | Added `normalizeStrategyTimeframes` guard and writes normalized list. | Yes |
| IDE diagnostics via `ReadLints` on touched files. | No diagnostics introduced. | No linter errors found. | Yes |
| `npm exec -- tsc --noEmit --pretty false` from `homepage`. | TypeScript check exits 0. | Exited 0 with no output. | Yes |
| `npm run lint` from `homepage`. | Lint should run or report repo setup limitation. | `next lint` exited 1 because it prompted to configure ESLint; no lint findings were emitted. | Partial - blocked by repo lint setup prompt |
| `git diff -- Sources Handoff/TalariaV16.jsx ...` and `git status --short --branch`. | Diff/status shows A5/ICR hunks present. | Diff output included canvas derivation helpers, lifecycle changes, and save backstops; status showed ICR/report files present as untracked docs. | Yes |

Console errors introduced: none observed from static checks. Browser save/reopen flow was not run in this environment.

## Risks & notes

`fillStrategyBuilderFromTemplate(tpl, afterApply, skipConfirm=false)` now bypasses the A3 confirm gate only when `skipConfirm` is truthy. I did not edit D's picker call site, per prompt; that call still invokes `fillStrategyBuilderFromTemplate(tpl)` until D updates it in the verify loop.

Custom-TF decision: Worker A lifecycle now treats lifted `stratBTfCustom` as the authoritative custom-timeframe state for reset/open/edit/template hydration. It resets to `[]` on blank/template reset and is populated from saved/template timeframes not in preset `["1m","5m","15m","30m","1H","4H","1D","1W","1M"]`. The older `stratBCustomTfs` is still reset for compatibility, but lifecycle does not populate it. `GeneralInfoStepContent` still has an internal `sbTfCustom`; B4 should consume the lifted `stratBTfCustom` / `setStratBTfCustom` props that are already passed into the component.

Legacy strategies still load because save falls back to existing `stratBConditions` when there are no canvas-derived conditions.

## Blocked?

Not blocked.
