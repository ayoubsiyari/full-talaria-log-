# A6 - Surface bank fetch failures

Status: DONE

## What changed

| File | Symbol(s) | Current line range | Nature of change |
|---|---|---:|---|
| `homepage/src/app/dashboard/v16/v16JournalMappers.ts` | `JournalApiData`, `responseErrorMessage` | 722-740 | Added optional `strategyBankError` and a helper to capture `/strategies` non-OK/null messages without throwing the whole payload. |
| `homepage/src/app/dashboard/v16/v16JournalMappers.ts` | `fetchJournalApiData` strategies fetch branch | 809-838 | Removed the whole-payload throw. On `/strategies` failure, returns `strategies: []` plus `strategyBankError`; entries, connections, active profile, and live accounts still return normally. |
| `homepage/src/app/dashboard/v16/useV16LiveBootstrap.ts` | `fetchJournalPayload` | 59-67 | Propagates `payload.strategyBankError` into the A1 `errorMessage` path so only the strategy bank is marked stale. Genuine whole-fetch exceptions still fall back to `EMPTY_JOURNAL_PAYLOAD`. |

The rejected A6 version made `/strategies` non-OK throw from `fetchJournalApiData`, which caused `fetchJournalPayload` to replace the entire journal payload with `EMPTY_JOURNAL_PAYLOAD`. This rework isolates the failure to the strategy-bank section: successful journal/list, broker, profile, and live-account data continue through the real payload, while A1 still receives `strategyBankError` and sets `strategyBankStale`.

## Zone compliance

- [x] All hunks inside Worker A ownership zone.
- [x] No security/limits weakened.
- [x] No new deps.
- [x] ICRs raised: none.

## Verification evidence

| Steps run | Expected | Observed | Pass |
|---|---|---|---|
| Code-path simulation: `/strategies` returns 500/null while `/journal/list` succeeds. | Entries/journal data remain in `JournalApiData`; only `strategyBankError` is set and `strategies` is `[]`. | `fetchJournalApiData` now processes entries/connections/profile/live accounts before returning `{ entries, connections, strategies: [], strategyBankError, activeProfile, liveAccounts }`; `fetchJournalPayload` maps that field to A1 stale flags without replacing the payload. | Yes |
| Code-path simulation: `/strategies` returns `200` with `{ strategies: [] }`. | Authoritative empty bank, no stale flag. | `strategiesRes.ok` branch parses `strategiesData.strategies || []`, leaves `strategyBankError` as `null`, and `fetchJournalPayload` returns `errorMessage: null`. | Yes |
| IDE diagnostics via `ReadLints` on `v16JournalMappers.ts` and `useV16LiveBootstrap.ts`. | No diagnostics introduced. | No linter errors found. | Yes |
| `npm exec -- tsc --noEmit --pretty false` from `homepage`. | TypeScript check exits 0. | Exited 0 with no output. | Yes |
| `git diff -- homepage/src/app/dashboard/v16/v16JournalMappers.ts homepage/src/app/dashboard/v16/useV16LiveBootstrap.ts docs/strategy-ship-plan/reports/A/A6.md`. | Diff shows isolation and no whole-payload throw. | Diff shows `strategyBankError` field, non-throwing strategies branch, and bootstrap propagation from `payload.strategyBankError`. | Yes |

Console errors introduced: none observed from static checks. Browser DevTools 401/500 simulation was not run in this environment; the two outcomes above are code-path simulations against the edited branches.

## Risks & notes

A strategies-only failure now preserves all other successful journal boot sections. A cold first boot with `/strategies` failing will have `strategyBankStale: true`, `strategyBankError` set, and an empty bank because no last-known fallback exists yet. A true empty 200 response remains authoritative and does not set stale flags.

## Blocked?

Not blocked.
