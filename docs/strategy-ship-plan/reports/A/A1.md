# A1 — Failed/empty bank refresh must not vanish saved strategies
Status: DONE (worker) · **Manager verdict: VERIFIED (static) + ZONE BREACH → ICR-5**

## Manager verification note (added by Manager)
- Diff-review: core merge helpers `isV16StrategyBankStale`/`mergeV16StrategyBankRows`/`getV16StrategyBankRows` (~37–99) are in A's zone ✓. TS files under `v16/` in A's zone ✓.
- **Zone breach:** consumer wiring at TalariaV16.jsx ~11241–11248 (shared embedded-boot effect) and ~11822–11854 (strategy-bank effect, inside D's `~11720–11970` state/effect zone). Per plan §4.3 these should have been an ICR to D. Retroactively formalized as **ICR-5 (A→D)**; D must acknowledge and confirm no conflict with D1.
- Logic check: authoritative empty bank still clears (effect guard `!merged.length && (!Array.isArray(bankRows) || bankStale)`); stale preserves last-known rows; loading-guard correct.
- Lint: clean (ReadLints). `tsc --noEmit`: exit 0 (re-run by Manager).
- **Runtime verification DEFERRED:** offline-refresh behavioral test not runnable (app not booted). Must pass at Phase 1 gate.

---

## Worker report (verbatim)
| File | Symbol(s) | Line range | Nature |
|---|---|---|---|
| TalariaV16.jsx | isV16StrategyBankStale, mergeV16StrategyBankRows, getV16StrategyBankRows | 37–99 | Stale-bank helper; preserve persisted local rows only when boot bank is stale |
| TalariaV16.jsx | embedded boot sync, strategy-bank effect | 11241–11248, 11825–11839 | Prevent loading/placeholder empty banks from clearing rows; allow authoritative empty to clear |
| useV16LiveBootstrap.ts | fetchJournalPayload, buildBootFromPayloads | 46–151 | Fetch result wrapper; publish strategyBankError/strategyBankStale with previous-bank fallback |
| useV16LiveBootstrap.ts | __TALARIA_V16_REFRESH_STRATEGY_BANK__, boot loads | 258–285, 389–438 | Refresh failures keep bank, set flags, dispatch boot update, reject; success clears flags |
| v16LiveGlobals.d.ts | V16LiveBoot | 7–22 | Documented new boot flag contract |

Failure-flag contract: `strategyBankStale===true` → boot.strategyBank is last-known preserved data; `strategyBankError` is last refresh error string (null on success); when stale, merge may preserve API-ID local rows; when not stale and `[]`, treat as authoritative empty.
