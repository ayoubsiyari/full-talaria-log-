# A7 - Persistence polish

Status: DONE

## What changed

| File | Symbol(s) | Current line range | Nature of change |
|---|---|---:|---|
| `Sources Handoff/TalariaV16.jsx` | `normalizeStrategyBankNameKey`, `findStrategyBankNameDuplicate` | 445-458 | Aligned duplicate-name detection with the existing Strategy Bank/session linking normalizer, so punctuation/case-only variants such as `My Strat!` and `My Strat` resolve to the same duplicate key. |
| `Sources Handoff/TalariaV16.jsx` | session bank state declarations | 11967-11974 | Removed the unused `sessSortOpen` / `setSessSortOpen` state declaration after confirming no references remained. |

## Partial Worker-A work

Before editing, `git diff -- "Sources Handoff/TalariaV16.jsx"` showed existing uncommitted Strategy Bank hunks from earlier work: `stratSortOpen` state, removal of `normalizeStrategyBankName`, Strategy Bank sort/dropdown changes, tab count changes, and template menu label changes. It did not include the A7 normalized duplicate check or the A7b `sessSortOpen` removal, so no partial A7 work needed finishing.

## Duplicate decision

The duplicate check remains a hard block, not a warn-only path. The app already uses `normalizeStrategyBankNameKey` for session-metrics linking, and this task explicitly asked the save guard to match that punctuation-insensitive identity. Names that differ by punctuation or case therefore collide consistently before persistence.

## Zone compliance

- [x] All code hunks stayed inside the temporary Worker A zone named by the prompt.
- [x] No backend uniqueness was added.
- [x] No security, JWT, CSRF, redirect, rate-limit, or size-limit behavior was touched.
- [x] No new dependencies.
- [x] ICRs raised: none.

## Verification evidence

| Steps run | Expected | Observed | Pass |
|---|---|---|---|
| Inspected `findStrategyBankNameDuplicate`, `normalizeStrategyBankNameKey`, and `saveBuilder` by name. | Duplicate save guard uses the shared Strategy Bank name key. | `findStrategyBankNameDuplicate` now compares `normalizeStrategyBankNameKey(name)` against `normalizeStrategyBankNameKey(row?.name)`, and `saveBuilder` continues to call that helper before saving. | Yes |
| Static duplicate scenario: `My Strat!` vs `My Strat`. | Names differing only by punctuation/case are detected as duplicates. | Both normalize to the same key (`my strat`) before comparison. | Yes |
| Static distinct-name scenario: `My Strat 2` vs `My Strat`. | Legitimately distinct names still save. | Normalized keys differ (`my strat 2` vs `my strat`), so no duplicate row is returned. | Yes |
| `rg "sessSortOpen\|setSessSortOpen" "Sources Handoff/TalariaV16.jsx"`. | Zero remaining references after removal. | No matches found. | Yes |
| `ReadLints` on `Sources Handoff/TalariaV16.jsx`. | No new diagnostics. | First request timed out on the large JSX file; retry returned no linter errors. | Yes |
| `git diff --check`. | Clean whitespace/apply check. | Exited 0 with no output. | Yes |
| `git diff -- "Sources Handoff/TalariaV16.jsx"`. | A7 hunks visible and scoped; pre-existing unrelated hunks preserved. | Diff shows the normalized duplicate helper update and `sessSortOpen` removal, plus the pre-existing Strategy Bank hunks noted above. | Yes |

Runtime/browser verification is deferred to Phase 4 per prompt.

## Blocked?

Not blocked.
