# A3 — Template application must not silently destroy an edit session
Status: DONE (worker) · **Manager verdict: VERIFIED (static) — IN ZONE, copy lock satisfied**

## Manager verification note (added by Manager)
- Diff-review: `strategyBuilderHasUnsavedChanges` + gated `fillStrategyBuilderFromTemplate` (46011–46092) are inside A's own lifecycle symbols ✓ (no zone breach; 45978 is drift).
- **Confirm-copy lock satisfied:** `openAppConfirm` with confirm="Replace", cancel="Keep editing", danger:true, message discards-unsaved-changes. Matches the A3↔B1 lock. Pending reconciliation against B1 when B's report lands.
- Declining changes nothing (returns false, applyTemplate not called); confirming applies then opens builder. Correct.
- Lint clean; `tsc` exit 0 (Manager re-ran).
- **Runtime verification DEFERRED:** decline/accept UI flow not runnable (app not booted). Must pass at Phase 1 gate.

---

## Worker report (verbatim)
| File | Symbol(s) | Line range | Nature |
|---|---|---|---|
| TalariaV16.jsx | strategyBuilderHasUnsavedChanges, fillStrategyBuilderFromTemplate, applyTemplateToBuilder | 45978–46092 | Dirty/edit detection + openAppConfirm gate before template replaces builder fields |

Dirty detection = active stratEditId OR non-empty name/desc/instruments/timeframes/tags/markets/conditions/images/support/logo OR non-default variables OR canvas edges/content. Clean initial + default divider = not dirty.
