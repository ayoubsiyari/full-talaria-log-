# A2 — Pessimistic delete with real error UX
Status: DONE (worker) · **Manager verdict: VERIFIED (static) + ZONE BREACH → ICR-6**

## Manager verification note (added by Manager)
- Diff-review: `deleteStrategyFromBank`/`runDelete` (~46263–46329) in A's zone ✓. TS delete helper in `v16/` ✓.
- **Zone breach:** `strategyDeleteInFlightRef` added at ~11803 (inside D's `~11720–11970` state zone). New parent state → should have been ICR to D. Retroactively formalized as **ICR-6 (A→D)**.
- Logic: awaits API before local removal; failure keeps row + `openAppConfirm` notice; in-flight Set keyed by `strategyRowKey`, cleared in finally (double-fire guard). Looks correct.
- Lint clean; `tsc` exit 0 (Manager re-ran).
- **Runtime verification DEFERRED:** DELETE 500/offline simulation not runnable (app not booted). Must pass at Phase 1 gate.

---

## Worker report (verbatim)
| File | Symbol(s) | Line range | Nature |
|---|---|---|---|
| TalariaV16.jsx | strategyDeleteInFlightRef | 11792–11797 | Per-row in-flight delete guard (double-fire prevention) |
| TalariaV16.jsx | deleteStrategyFromBank, runDelete | 46263–46329 | Await API before local removal; failure keeps row + openAppConfirm |
| useV16LiveBootstrap.ts | __TALARIA_V16_DELETE_STRATEGY__ | 306–317 | Report refresh failure separately after DELETE succeeds |
| v16LiveGlobals.d.ts | __TALARIA_V16_DELETE_STRATEGY__ | 94–98 | Return type allows { refreshError } |
