# B1 — Unsaved-changes confirmation on close
Status: DONE (worker) · **Manager verdict: VERIFIED (static) — copy lock OK; ZONE breach (1 prop) → ICR-7**

## Manager verification note (added by Manager)
- Diff-review: dirty-signature + `requestBuilderClose` inside `StrategyBuilderModal` (8130–8189) — B's zone ✓. Confirmed live: `openAppConfirm` destructured at 8131; guard at 8175–8189.
- **Copy lock (A3↔B1) satisfied:** confirm="Discard", cancel="Keep editing", danger:true, message "Your unsaved changes to this strategy will be lost." Parallel to A3 (Replace/Keep editing). Reconciled ✓.
  - **Minor note (not REWORK):** A3 localizes to Arabic via `profileLang`; B1 dialog is English-only. Fold Arabic strings into B1 during B5 (Phase 3) for parity.
- **Zone breach:** one prop `openAppConfirm={openAppConfirm}` at the `StrategyBuilderModal` instantiation (~46878, D's zone). Correct + minimal → retroactive **ICR-7 (B→D)**. B self-flagged (B1-ICR-001).
- Backdrop still ignored ✓ (no close handler on backdrop). Dirty snapshot captured on first render via `builderInitialSignatureRef`; ReactFlow UI-only keys excluded from signature — good (avoids false-dirty).
- Lint clean (ReadLints on live tree). Worker also ran `vite build` on the handoff preview: success.
- **Runtime verification DEFERRED:** in-browser dirty/pristine close flow not run (app not booted). Must pass at Phase 1 gate.

---

## Worker report (verbatim excerpt)
| File | Symbol | Line range | Nature |
|---|---|---|---|
| TalariaV16.jsx | StrategyBuilderModal | 8128–8476 | Open-time dirty signature; dirty/pristine close routing; X + step-1 Cancel via close guard |
| TalariaV16.jsx | StrategyBuilderModal call site | ~46878 | Passed `openAppConfirm` prop into modal (out-of-zone → ICR-7) |

Dirty signature = useMemo JSON of normalized form+canvas (name, desc, style, direction, complexity, markets, timeframes, instruments, support, tags, variables, images, logo, canvasNodes, canvasEdges); sorts keys, skips fns/undefined, ignores ReactFlow keys (selected/dragging/resizing/measured/positionAbsolute); first signature stored in ref as snapshot.
