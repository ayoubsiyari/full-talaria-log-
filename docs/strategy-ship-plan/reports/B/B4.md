# Worker B Report - B4 Edit-Mode Restoration

Status: DONE

## Scope
- File: `Sources Handoff/TalariaV16.jsx`
- Zone: `GeneralInfoStepContent` and `StrategyBuilderModal`
- No new dependencies.
- Existing uncommitted A/C/D and earlier B work preserved.

## Symbols and Line Ranges
- `GeneralInfoStepContent` props and lifted state wiring: lines 6407-6427
- Step-1 classification controls and dynamic style restore option: lines 6522-6524, 6861-6877, 6953-6958
- `stratBMarketsManualRef` edit restore and symbol-sync guard: lines 6627-6660
- Lifted custom timeframe source of truth: lines 6793-6825, 7406-7432
- `StrategyBuilderModal` step-1 prop pass-through: line 8643

## Restore Verification Table
| Field | Restore path | Result |
| --- | --- | --- |
| Name | `stratBName` remains parent-owned and passed to `GeneralInfoStepContent`. | Restores visibly in the name input. |
| Description | `stratBDesc` remains parent-owned and passed to `GeneralInfoStepContent`. | Restores visibly in the textarea. |
| Style | `stratBStyle` / `setStratBStyle` now pass into step 1 and render as classification controls. Unknown saved style values are injected as the selected option. | Restores visibly and remains editable. |
| Direction | `stratBDirection` / `setStratBDirection` now pass into step 1 and render as classification controls. | Restores visibly and remains editable. |
| Complexity | `stratBComplexity` / `setStratBComplexity` now pass into step 1 and render as classification controls. | Restores visibly and remains editable. |
| Markets | `stratBMarketsManualRef` is consumed from props. Restored edit markets mark the ref manual before symbol auto-derive runs. | Saved markets are not clobbered by instrument-derived markets on edit reopen. |
| Traded instruments | `stratBInstruments` remains parent-owned and feeds the selected-symbol grid. | Restores visibly. |
| Supporting instruments | `stratBSupportInst` remains parent-owned and feeds the selected-symbol grid. | Restores visibly. |
| Timeframes | `stratBTimeframes` remains parent-owned, with B2 normalization/cap behavior preserved. | Preset selections restore visibly. |
| Custom timeframes | Removed the competing local `sbTfCustom` state. `stratBTfCustom` is now the sole custom-TF state used for option lists, add, and delete. | Restored custom TFs appear in the picker and persist through parent state. |
| Tags | `stratBTags` remains parent-owned and renders selected tags. | Restores visibly. |
| Images | `stratBImages` remains parent-owned and renders cover slots. | Restores visibly. |
| Logo | `stratBLogoEmoji` remains parent-owned and renders the emoji picker button. | Restores visibly. |

## Verification
- `ReadLints` on `Sources Handoff/TalariaV16.jsx`: no linter errors found.
- `node` + `esbuild.transformSync` JSX syntax check on `Sources Handoff/TalariaV16.jsx`: passed.
- `git diff --check -- "Sources Handoff/TalariaV16.jsx"`: passed.
- `rg "sbTfCustom|setSbTfCustom|marketsManualFilterRef" "Sources Handoff/TalariaV16.jsx"`: no matches.
- `git diff -- "Sources Handoff/TalariaV16.jsx"`: present and reviewed. The full file diff also includes earlier uncommitted worker changes outside B4; B4 changes are limited to the ranges above.

## Notes
- `stratBTfCustom` is now the single custom-timeframe source in Worker B's zone.
- Browser UI interaction was not executed in this session; static and syntax verification completed.
